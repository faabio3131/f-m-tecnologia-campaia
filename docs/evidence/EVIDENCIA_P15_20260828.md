# CAMPAIA — EVIDÊNCIA DE FECHAMENTO DA P-15

**Data:** 28/08/2026
**Pendência:** P-15 — `contracts/connector-hub-e-eventos.md` descreve a interface canônica do Ads Connector
Hub em tempo presente, sugerindo implementação já existente para os três adaptadores reais, quando o código
não contém nenhum adaptador real e implementa apenas 3 dos 14 métodos do contrato.

## Autorização do Diretor

O Diretor, ao receber o relatório de fechamento da P-14, recebeu três candidatos para o próximo passo,
incluindo: *"corrigir a imprecisão que catalogou como P-15: `contracts/connector-hub-e-eventos.md` afirma 14
operações 'Implementadas' quando só 3 existem de fato."* (formulação usada no próprio relatório enviado ao
Diretor, 28/08/2026).

Resposta do Diretor, verbatim: **"fazer essa correção primeiro antes de avançar e não deixar nada para tras,
e depois na sequencia as opções 1 e a 2 pode executar dessa forma"** — ou seja: corrigir a P-15 por completo
antes de qualquer outra coisa, depois seguir para persistência real (candidato 1) e para os blocos B5/B7/B8/
B9/B10 (candidato 2), nessa ordem.

## Correção da formulação do achado (autocrítica, antes de agir)

Antes de editar qualquer arquivo, reli o documento literal `contracts/connector-hub-e-eventos.md` (Drive
fileId `1v8XPUCxRYMX-AfR-J1i8Sp6P10HaXVte`, baixado via `download_file_content`, decodificado localmente,
**7281 bytes — byte a byte idêntico ao tamanho registrado no Drive**) em vez de aceitar de olhos fechados a
minha própria formulação anterior do achado. Resultado da releitura: **a palavra "Implementadas" não aparece
em nenhum lugar do documento.** Busca literal (`grep -ni "implementad"`) devolve apenas duas ocorrências,
nenhuma delas a afirmação alegada:

```
61:Implementado em `backend/campaia_core/connectors.py`, com o conjunto `NON_RETRYABLE` testado.
128:Implementado em `backend/campaia_core/saga.py`. As três políticas têm teste próprio.
```

Ambas se referem a partes específicas do documento (taxonomia de erros; políticas de compensação da Saga),
não à tabela dos 14 métodos do conector. Ou seja, minha formulação anterior ("afirma 14 operações
'Implementadas'") era uma paráfrase imprecisa, não uma citação literal — e citar algo que o documento não diz
violaria a própria disciplina evidencial deste projeto. Documentando isso aqui em vez de simplesmente corrigir
em silêncio.

**O problema real, confirmado por leitura literal**, está na frase de abertura da Seção 1 do documento:

> *"Todo adaptador (Google Ads, Meta, WhatsApp) implementa exatamente esta interface."*
> (`contracts/connector-hub-e-eventos.md`, linha 9)

Esta frase está no tempo presente do indicativo — afirma que os adaptadores **já implementam** a interface de
14 métodos, para as três plataformas. Como o restante deste documento mostra, isso diverge do estado real do
código.

## Achado (verificado por leitura direta do código, 28/08/2026)

**1. Nenhum adaptador real existe.** Busca por arquivo na pasta `backend/campaia_core/` no Drive (pasta
`1qUAbtVmX6fHafW2MlHfHXrEcMo2y-5yx`) não encontra `google_ads.py`, `meta.py`, `whatsapp.py` nem qualquer nome
equivalente. Os únicos dois arquivos relacionados a conectores são `connectors.py` (contrato/Protocol) e
`simulator.py` (`ProviderSimulator`, um substituto para desenvolvimento e teste, conforme seu próprio
docstring: *"Provider Simulator (ADR-0013, item 4). Implementa o mesmo contrato dos adaptadores reais e
devolve tambem as FALHAS."*).

**2. `connectors.py` define o protocolo `AdsConnector` com apenas 3 métodos**, citação literal
(fileId `1LHyc0hcXenTHsj7RNDNXfqMhK1Eoa4Xp`, 4824 bytes):

```python
class AdsConnector(Protocol):
    provider: str

    def validate_draft(self, command: PublishCommand) -> list[str]: ...
    def publish(self, command: PublishCommand) -> PublishResult: ...
    def pause(self, command: PublishCommand) -> None: ...
```

Nenhum outro método do contrato (`connect_account`, `list_accounts`, `describe_capabilities`,
`create_campaign`, `create_ad_set`, `upload_asset`, `create_ad`, `resume`, `update_budget`,
`fetch_insights`, `sync_conversions`, `handle_webhook`) aparece neste arquivo.

**3. `simulator.py` implementa exatamente os mesmos 3 métodos**, e nenhum outro, citação literal (fileId
`1Q-wKNzLtdUerk39Lj1lcdue5YC3Up07e`, 4633 bytes) — assinaturas de `ProviderSimulator`:

```python
def validate_draft(self, command: PublishCommand) -> list[str]: ...
def publish(self, command: PublishCommand) -> PublishResult: ...
def pause(self, command: PublishCommand) -> None: ...
```

**Conclusão do achado:** de 14 métodos canônicos definidos no contrato, **3 (21%) têm implementação real**
(`validate_draft`, `publish`, `pause`) — e mesmo essa implementação é um simulador, não um adaptador real de
plataforma. Os outros 11 não existem em nenhum arquivo do backend. A frase de abertura da Seção 1 do
documento, ao usar tempo presente, não deixa isso claro para quem lê o contrato sem também ler o código.

## Correção aplicada

Arquivo `contracts/connector-hub-e-eventos.md` corrigido nos seguintes pontos, sem alterar nenhuma outra
seção do documento (catálogo de eventos, diagrama da Saga, versionamento de contratos — todos idênticos ao
original):

1. Cabeçalho ganhou a linha `**Nota de status corrigida em:** 28/08/2026 (P-15)`.
2. A frase de abertura da Seção 1 mudou de afirmação no presente para dever explícito ("**deve** implementar
   exatamente esta interface — este é o contrato-alvo da integração, não uma afirmação de que a implementação
   já existe para as três plataformas").
3. Logo após a tabela dos 14 métodos, foi inserido um bloco de citação ("Status real de implementação")
   documentando, com os mesmos fatos apurados nesta evidência: nenhum adaptador real existe; apenas 3 dos 14
   métodos têm implementação (via `ProviderSimulator`); os outros 11 não existem no backend; e uma referência
   a este arquivo de evidência para o detalhe completo.

Byte a byte: arquivo original 7281 bytes (Drive); arquivo corrigido local
`/home/claude/campaia_verify/contracts/connector-hub-e-eventos_v2.md` = 9035 bytes antes do upload (a
diferença de tamanho é esperada — o texto acrescentado é puramente aditivo).

## Verificação final

- Releitura do documento original, byte a byte confirmado contra o Drive antes de qualquer edição.
- Busca literal (`grep`) confirmando a ausência da palavra "Implementadas" no documento original, e a
  localização exata (linha 9) da frase que realmente causa a imprecisão.
- Leitura direta de `connectors.py` e `simulator.py`, ambos obtidos do Drive nesta sessão, confirmando que
  apenas 3 dos 14 métodos existem em código, e que nenhum arquivo de adaptador real (Google Ads/Meta/
  WhatsApp) existe na pasta `backend/campaia_core/`.
- Nenhum código foi alterado nesta correção — é uma correção de documentação, não de comportamento do
  sistema. Portanto não há suíte de testes nova a rodar; os 233+64=297 testes já confirmados no fechamento da
  P-14 continuam válidos e não são afetados por esta mudança.
- Arquivo corrigido enviado ao Drive substituindo o antigo (trash + create, conforme disciplina do projeto),
  com verificação de tamanho em bytes entre o arquivo local e o arquivo criado no Drive.

## Resumo

P-15 fechada em 28/08/2026: a imprecisão real não era a palavra "Implementadas" (que nunca existiu no texto
original — correção da minha própria formulação anterior, feita antes de agir sobre ela), mas o tempo verbal
presente da frase de abertura da Seção 1, que descrevia um contrato-alvo como se já estivesse implementado
para três plataformas reais, quando na verdade apenas 3 dos 14 métodos têm qualquer implementação, e essa
implementação é um simulador de desenvolvimento, não um adaptador de produção.
