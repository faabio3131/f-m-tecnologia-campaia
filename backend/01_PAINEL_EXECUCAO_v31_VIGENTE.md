# CAMPAIA — PAINEL DE EXECUÇÃO

**Atualizado em:** 21/09/2026 (EXECUÇÃO REAL DO WP-11 — Métricas honestas da campanha, bloco
não previsto no roadmap original, definido nesta mesma missão por reconciliação de CURRENT,
mesma autorização do WP-09/10 ("pode sim construa mais 3 blocos"). Nova seção "Métricas" em
`/campaigns/{id}` exibe a resposta real de `GET /campaigns/{id}/insights` — rota que já
existia desde antes deste bloco, já testada por fixtures Bearer, mas nunca tinha uma tela;
quando `points` está vazio (sempre, hoje — nenhuma camada de analytics existe em
`campaia_core`), mostra o `note` real devolvido pela API, nunca um gráfico vazio nem um
placeholder inventado. Achado real de contrato corrigido: `InsightSeries.note` estava
ausente do schema apesar de sempre presente na resposta real. Diferente do WP-10, este
bloco não encontrou nenhum outro achado real — nem de contrato adicional, nem de bug de UI
pré-existente. 178 testes de API (176 preexistentes + 2 novos), 267 de domínio e AsyncAPI
24/24 sem regressão; 135 testes Vitest (3 novos) e 13 E2E cross-stack reais (12
preexistentes + 1 novo). Certificado em
`docs/web/18_CERTIFICACAO_WP11_METRICAS_HONESTAS.md` — **revisão de FM QA Engineer
permanece pendente**. Mobile em quarentena, sem evolução. **Com o WP-11, a autorização do
Diretor de construir mais 3 blocos ("pode sim construa mais 3 blocos") está integralmente
executada** (WP-09, WP-10, WP-11); nenhum bloco além do WP-11 foi definido)
**Regra:** este painel é a fonte de verdade da execução. Ao retomar qualquer sessão, reconstruir o estado a
partir de arquivos, código e testes reais — nunca apenas da memória da conversa (Ordem Mestra, item 18).

> **VERSÃO VIGENTE.** Substitui `01_PAINEL_EXECUCAO_v30_VIGENTE.md` (e as anteriores v1-v29). Versões antigas
> preservadas neste repositório Git para histórico — não apagar do controle de versão.

---

## PONTO DE RETOMADA

1. Ler este painel.
2. **Repositório canônico do CampaIA é `faabio3131/f-m-tecnologia-campaia` (este repositório), branch `main`.**
   `faabio3131/campaia` (`CampaIA`) é fonte histórica temporária, **somente leitura** — não commitar lá
   (ADR-0015).
3. Clonar/restaurar `backend/` a partir deste repositório Git — 5 subpastas: `campaia_core/` (agora com
   `fiscal_handoff.py`, importado nesta reconciliação), `tests/`, `api/`, `tests_api/`, `db/`.
4. Rodar `cd backend && python3 -m unittest discover -s tests` (sem `-t .` — `tests/` não tem `__init__.py`) → esperado: **267 aprovados** (domínio; 263 preexistentes + 4 novos de `test_fiscal_handoff.py`, importado nesta reconciliação — números confirmados por execução real na Etapa 8 da matriz de validação, não presumidos).
5. Rodar `cd backend && python3 -m unittest discover -s tests_api` (sem `-t .`) → esperado: **178 aprovados** (176 do WP-10 + 2 de `test_insights_web_session.py`, WP-11).
5. Persistência é opcional: `create_app()` (sem argumento) continua 100% em memória; `create_app(db_path="...")` liga a um arquivo SQLite real. Ver `api/db.py` e `docs/evidence/EVIDENCIA_PERSISTENCIA_20260828.md`.
6. Catálogo de eventos formalizado como AsyncAPI 3.0 em `contracts/events.asyncapi.yaml` (24 eventos — contagem corrigida de uma caracterização anterior incorreta de 22). Verificar com `python3 contracts/validate_events_asyncapi.py`. Ver `docs/evidence/EVIDENCIA_B9_ASYNCAPI_20260829.md`.
7. Catálogo de 21 mensagens de erro em linguagem de usuário publicado em `docs/09_CATALOGO_ERROS_USUARIO.md` (v1.1.0) — os 3 códigos do Connector Hub que faltavam mapeamento (`AUTH_EXPIRED`, `VALIDATION_REJECTED`, `PARTIAL_FAILURE`) foram corrigidos em código em `api/errors.py` (`STATUS_BY_CODE`), sob autorização do Diretor ("pode corrigir não vamos deixar nenhum erro para trás"). P-19 **FECHADA** — ver `docs/evidence/EVIDENCIA_P19_FIX_20260904.md`.
8. B5 (motor de otimização e pacing) **CONCLUÍDO** (04/09) — `campaia_core/pacing.py` e `campaia_core/optimizer.py`, camada de sugestão apenas (F8.1); nenhuma execução automática (F8.2 permanece fora de escopo, confirmado pelo Diretor). Ver `docs/evidence/EVIDENCIA_B5_OTIMIZACAO_PACING_20260904.md`.
9. B7 (DDL e migrations do Postgres) **CONCLUÍDO** (04/09) — `backend/db/001_initial_schema.sql` (7 tabelas já persistidas hoje via `api/db.py`, RLS por tenant, CHECK nos enums do contrato, FK real `approvals→campaigns`) e `backend/db/verify_ddl_postgres.sh`, verificados de fato contra um Postgres 16 real descoberto nesta sandbox (não apenas sintaxe). Ver `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md`.
10. C1 (repositório Git + CI) **CONCLUÍDO E 100% OPERACIONAL** (05/09) — repositório publicado em `https://github.com/faabio3131/CampaIA` (privado), com histórico completo, `.gitignore`, `requirements.txt` (backend e contracts) e workflow do GitHub Actions (`.github/workflows/backend-tests.yml`) rodando as 2 suítes de teste + o validador do AsyncAPI a cada push. O Diretor escolheu GitHub como host remoto e autenticou o `gh` (GitHub CLI) e o Git na própria máquina via login por navegador (device code flow), sem nenhuma credencial passando por mim. A primeira execução real do CI no GitHub falhou (`ModuleNotFoundError: No module named 'httpx'` — dependência do `starlette.testclient` ausente de `backend/requirements.txt`); diagnosticado a partir do log real (`gh run view --log-failed`), corrigido (`httpx==0.28.1` adicionado, versão confirmada real no PyPI antes de fixar), e a execução seguinte confirmada com sucesso (`"conclusion": "success"`). Ver `docs/evidence/EVIDENCIA_C1_CI_HTTPX_FIX_20260905.md`.
11. Opção 2 (decisões D-03/D-05/D-06/D-08/D-09) — **RECONCILIAÇÃO CONCLUÍDA** (05/09): as 5 decisões já haviam sido tomadas pelo Diretor em 27/08/2026 (ver evidências correspondentes), mas este painel continuava listando-as como "Bloqueios" desde então. Corrigido em v17 — ver "Decisões registradas" e "Situação atual". Restam apenas 2 itens residuais, nenhum deles uma decisão do Diretor: **P-11** (preço exato do Memorystore/Redis para São Paulo, página do Google Cloud não confirma sem seleção manual de região) e a confirmação formal da emissão do CNPJ da F&M (em andamento com a contabilidade do Diretor, conforme seu próprio relato).
12. **B8 (especificação das telas do app) CONCLUÍDO** (05/09) — `docs/product/13_ESPECIFICACAO_TELAS_APP.md`, cobrindo F1–F10, com pesquisa de mercado de concorrentes (Madgicx, Revealbot, Smartly.io, AdCreative.ai, Meta Advantage+) e a especificação completa do Modo Automático de autonomia (Nível 2, com limite definido pelo cliente e ativação por senha de administrador), decisão de escopo tomada pelo Diretor em 05/09 e registrada em `docs/product/DECISOES_DIRETOR.md` item 5. Ver "Registro de blocos executados" desta versão.
13. Próximo bloco recomendado, pendente de autorização humana explícita: **System Design da aplicação Web real (Ponto Zero Web)**. O app `mobile/` permanece em quarentena arquitetural, sem evolução, até decisão posterior ao System Design. Não iniciar implementação de frontend Web ou mobile antes da aprovação do desenho arquitetural.
14. **WP-01 (Fundação do frontend Web) IMPLEMENTADO** (19/09) — `web/` (Next.js 16.3.5/React 19.2.8/TypeScript estrito). Ver `docs/web/08_CERTIFICACAO_WP01_FUNDACAO_WEB.md`.
15. **WP-02 (Autenticação e sessão Web real) IMPLEMENTADO, revisão de segurança humana pendente** (20/09). Ver `docs/web/09_CERTIFICACAO_WP02_AUTENTICACAO_SESSAO_WEB.md`.
16. **WP-03 (Contexto de tenant/unidade e shell do dashboard) IMPLEMENTADO, mesma revisão de segurança humana pendente** (21/09) — `campaia_memberships` opcional no ID token, `GET /session/memberships`/`POST /session/switch-tenant`, `/dashboard` com seletor de tenant e estados padronizados; `CAMPAIA_WEB_ORIGIN` novo (redirect + CORS corretos entre origens diferentes). Ver `docs/web/10_CERTIFICACAO_WP03_TENANCY_SHELL.md`.
17. **WP-04 (Onboarding e Brand Kit) IMPLEMENTADO, revisão de FM QA Engineer pendente** (21/09) — `/onboarding` com Brand Kit real e conectar contas simulado, rota nova `POST /connections/oauth/complete` fechando um gap real herdado do WP-02. Ver `docs/web/11_CERTIFICACAO_WP04_ONBOARDING_BRAND_KIT.md`.
18. **WP-05 (Briefing, estratégia e aprovação) IMPLEMENTADO, mesma revisão de FM QA Engineer pendente** (21/09) — fecha o Gate 5: `/campaigns` (briefing + estratégia via IA), `/approvals` (validação + fila de aprovação com segregação de funções real). Nenhuma rota de backend nova (as 6 já existiam); contrato corrigido aditivamente (`ApprovalRequest.requested_by`). Ver `docs/web/12_CERTIFICACAO_WP05_BRIEFING_APROVACAO.md`.
19. **WP-06 (Alteração de orçamento) IMPLEMENTADO, mesma revisão de FM QA Engineer pendente** (21/09) — bloco não previsto no roadmap original, definido por reconciliação de CURRENT; `BudgetPanel` em `/campaigns/[id]` reutiliza a fila `/approvals` do WP-05 por completo (`kind: BUDGET_CHANGE`) sobre `PATCH /campaigns/{id}/budget`, já existente. Nenhuma rota de backend nova; contrato corrigido aditivamente (`ApprovalRequest.kind`/`.amount`, `POST /approvals` documentado). Ver `docs/web/13_CERTIFICACAO_WP06_ALTERACAO_ORCAMENTO.md`. Com este bloco, a autorização de 3 blocos do Diretor ("pode avançar mais 3 blocos") foi integralmente executada.
20. **WP-07 (Nível de autonomia/Modo Manual-Automático) IMPLEMENTADO, mesma revisão de FM QA Engineer pendente** (21/09) — bloco não previsto no roadmap original, definido por reconciliação de CURRENT, sob **nova autorização do Diretor** para continuar a construção enquanto a cota do CI do GitHub Actions está esgotada (`FATO CONFIRMADO`, renovação prevista para o dia 31); `AutonomyPanel` em `/dashboard` reutiliza a fila `/approvals` do WP-05 por completo (`kind: AUTONOMY_CHANGE`) sobre `GET/PUT /autonomy`, já existente. Nenhuma rota de backend nova; contrato corrigido aditivamente (`AutonomySettings.max_level_allowed`, `Campaign.business_unit_id`). Ver `docs/web/14_CERTIFICACAO_WP07_NIVEL_AUTONOMIA.md`.
21. **WP-08 (Parada de emergência/Kill Switch) IMPLEMENTADO, mesma revisão de FM QA Engineer pendente** (21/09) — bloco não previsto no roadmap original, definido por reconciliação de CURRENT, mesma autorização do WP-07; `KillSwitchPanel` em `/dashboard` aciona `POST /kill-switch` diretamente, SEM passar pela fila `/approvals` (o domínio dispensa aprovação, kill switch só reduz efeito) e SEM `X-Step-Up-Token` (`Permission.KILL_SWITCH` fora de `REQUIRES_STEP_UP`). Escopos `CAMPAIGN`/`TENANT` apenas — `ACCOUNT`/`PLATFORM`/`GLOBAL` deliberadamente não expostos. Nenhuma rota de backend nova; contrato corrigido aditivamente (`KillSwitchResult`, schema de resposta inteiro ausente). Ver `docs/web/15_CERTIFICACAO_WP08_PARADA_EMERGENCIA.md`.
22. **Diretor autoriza construir mais 3 blocos** ("pode sim construa mais 3 blocos"), reafirmando a autorização contínua de construção durante o bloqueio de cota do CI.
23. **WP-09 (Desconectar conta e ver capacidades da conexão) IMPLEMENTADO, mesma revisão de FM QA Engineer pendente** (21/09) — bloco não previsto no roadmap original, definido por reconciliação de CURRENT; `ConnectAccountCard.tsx` (WP-04) ganha "Desconectar" (`DELETE /connections/{id}`) e a lista real de capacidades (`GET /connections/{id}/capabilities`, buscada no servidor, nunca no cliente). Nenhuma rota de backend nova; contrato corrigido aditivamente (`Capability.provider`/`.country`/`.api_version`). Ver `docs/web/16_CERTIFICACAO_WP09_DESCONECTAR_E_CAPACIDADES.md`.
24. **WP-10 (Trilha de auditoria) IMPLEMENTADO, mesma revisão de FM QA Engineer pendente** (21/09) — bloco não previsto no roadmap original, definido por reconciliação de CURRENT, mesma autorização do WP-09; nova página `/audit` (Server Component puro) exibe `GET /audit-events`, já existente. Nenhuma rota de backend nova; **nenhum achado de contrato** — único bloco desta sequência sem correção aditiva de schema. Achado real de bug de UI pré-existente (interação WP-04/WP-09: `onboarding/page.tsx` contava conexões revogadas como conectadas), encontrado e corrigido por causa raiz durante a própria regressão E2E deste bloco. Ver `docs/web/17_CERTIFICACAO_WP10_TRILHA_AUDITORIA.md`.
25. **WP-11 (Métricas honestas da campanha) IMPLEMENTADO, mesma revisão de FM QA Engineer pendente** (21/09) — bloco não previsto no roadmap original, definido por reconciliação de CURRENT, mesma autorização do WP-09/10; nova seção "Métricas" em `/campaigns/{id}` exibe `GET /campaigns/{id}/insights`, já existente. Nenhuma rota de backend nova; 1 achado real de contrato corrigido (`InsightSeries.note` ausente); nenhum outro achado real. Rodar `cd web && npm ci && npm run lint && npm run typecheck && npx vitest run && npm run build` → esperado: todos verdes, 135 testes Vitest. E2E sem backend: `CAMPAIA_CHROMIUM_PATH=<chromium> npm run test:e2e` → esperado: 14 testes Playwright verdes (sem novo spec dedicado). E2E cross-stack real: `CAMPAIA_CHROMIUM_PATH=<chromium> npm run test:e2e:crossstack` → esperado: 13 testes Playwright verdes. Ver `docs/web/18_CERTIFICACAO_WP11_METRICAS_HONESTAS.md`. **Com o WP-11, a autorização do Diretor de construir mais 3 blocos ("pode sim construa mais 3 blocos") está integralmente executada** (WP-09, WP-10, WP-11); **nenhum bloco além do WP-11 foi definido**; qualquer trabalho futuro exige nova autorização explícita do Diretor E nova reconciliação de CURRENT.

Se qualquer suíte não passar, corrigir antes de qualquer coisa nova.

---

## Situação atual

| Campo | Valor |
|---|---|
| **Fase atual** | **WP-11 (Métricas honestas da campanha) IMPLEMENTADO E AUTOTESTADO** (21/09) — bloco não previsto no roadmap original, definido por reconciliação de CURRENT, mesma autorização do WP-09/10; nova seção "Métricas" em `/campaigns/{id}` exibe `GET /campaigns/{id}/insights`. 1 achado real de contrato corrigido (`InsightSeries.note` ausente), nenhum outro achado real. **Revisão de FM QA Engineer pendente**. WP-01 a WP-10 permanecem implementados (19–21/09), Gate 5 tecnicamente completo. App mobile em **QUARENTENA ARQUITETURAL**, intocado. **Com este bloco, a autorização do Diretor de construir mais 3 blocos está integralmente executada** (WP-09, WP-10, WP-11) |
| **Última tarefa concluída** | Implementação e autoteste completo do WP-11 (métricas honestas da campanha), por autorização humana atual e explícita ("pode sim construa mais 3 blocos"), continuação de "PROMPT MESTRE — CAMPAIA SaaS V1 COMPLETO". Ver "Registro de blocos executados" desta versão |
| **Status** | Backend `campaia_core/`/`db/`/`permissions.py`/`routes_campaigns.py`/`models.py` **inalterados** (revalidados sem regressão: 267 testes de domínio, AsyncAPI 24/24; nenhuma rota nova) — 178 testes de API (176 preexistentes intocados + 2 novos), todos verdes; **frontend Web: WP-01/02/03/04/05/06/07/08/09/10 intocados, WP-11 adicionado**; **app mobile em QUARENTENA ARQUITETURAL**, sem alteração; **bloco fiscal FISC V2-16.5** permanece bloqueado, sem alteração |
| **Última evidência** | `docs/web/18_CERTIFICACAO_WP11_METRICAS_HONESTAS.md` — comandos, resultados e HEAD reais desta execução, incluindo os 13 testes E2E cross-stack reais (12 preexistentes + 1 novo). CI da PR continua sem poder ser confirmado verde: **FATO CONFIRMADO pelo Diretor** — cota gratuita de minutos do GitHub Actions da conta esgotada, renovação prevista para "o dia 31" (citação literal do Diretor, sem mês especificado; setembro/2026 não tem dia 31 — a formalização anterior deste painel como data completa "31/09/2026" continha um erro de calendário introduzido durante a documentação desta missão, não uma data informada pelo Diretor; **a data exata de renovação é PENDÊNCIA DE DECISÃO/CONFIRMAÇÃO**, corrigido nesta auditoria em 22/09/2026); consistente com a assinatura já observada em todo run desde o push do WP-03 (`runner_id: 0`, 0ms billable, conclusão em ~2-4s, log 404 — reconfirmado independentemente em 22/09/2026 na auditoria de reconciliação, inclusive no primeiro run que falhou nesta sequência, 21/09 01:50Z, e no HEAD atual `fbb5d01`). Não é regressão de código; nenhuma correção adicional fará o CI passar enquanto a cota estiver esgotada. Toda a evidência deste bloco vem de execução local real, reconfirmada em 22/09/2026 (267 domínio, 178 API, 24/24 AsyncAPI, 135 Vitest, 14 E2E smoke, 13 E2E cross-stack — 100% verde). Ver comentários da PR #6 |
| **Bloqueios** | Nenhum bloqueio técnico novo. Pendências reais remanescentes: **revisão de segurança humana do WP-02/WP-03 (FM Security Engineer)**, **revisão de FM QA Engineer do WP-04 a WP-11**, provedor específico de identidade/ads comercial, seletor de UNIDADE (só tenant foi exercitado, nenhuma rota de troca de unidade existe — achado real do WP-09, P-43), cadastro de empresa/unidade de negócio (sem endpoint), upload de logo (sem endpoint), publicação real (`POST .../publish`, depende de adaptador de provider real, e nenhuma tela do Web app aciona esta rota, achado do WP-08), alterar `max_level_allowed` (teto de autonomia — configuração comercial/plano, fora de qualquer tela), retomar campanha pausada (nenhuma rota HTTP existe, nem operação canônica nos conectores — achado real do WP-08, P-40), escopos `ACCOUNT`/`PLATFORM`/`GLOBAL` do kill switch não expostos na Web, `evidence_url` das capacidades sempre `null` (achado 7, pré-existente), paginação real da trilha de auditoria (`next_cursor` sempre `null`, achado 3/15, pré-existente), camada de analytics real inexistente (`points` de `/insights` sempre vazio, achado pré-existente ao WP-11), serviço de container específico (Work Package de CI/CD), orçamento mensal exato e confirmação jurídica de residência de dados (D-08), destino final do `mobile/`, **nenhum bloco além do WP-11 definido — a autorização de "mais 3 blocos" do Diretor está integralmente executada**, **cota do CI do GitHub Actions esgotada até o dia 31**. Bloco fiscal e mobile mantêm seus bloqueios já registrados nas versões anteriores deste painel |
| **Próxima ação** | Nenhum bloco novo autorizado no momento — a autorização de "mais 3 blocos" do Diretor foi integralmente executada (WP-09, WP-10, WP-11). Qualquer trabalho futuro exige nova autorização explícita do Diretor E nova reconciliação de CURRENT. Confirmar CI verde da PR assim que a cota do GitHub Actions renovar (dia 31). Revisão de segurança humana do WP-02/WP-03 e de FM QA Engineer do WP-04 a WP-11 seguem pendentes, independentemente de qualquer bloco futuro |
| **Gate para avançar** | Gate 1 (Arquitetura) — aprovado (19/09/2026); Gate 2 (Scaffold Web/WP-01) — aprovado; **Gate 3 (Autenticação/WP-02) — implementação concluída, aprovação formal pendente de revisão de segurança humana**; **Gate 4 (Tenancy/WP-03) — implementação concluída, aprovação formal pendente da mesma revisão**; **Gate 5 (Primeira jornada) — implementado por completo pelo WP-04+WP-05, aprovação formal pendente de FM QA Engineer**; **WP-06/WP-07/WP-08/WP-09/WP-10/WP-11 (extensões do Gate 5) — implementados, mesma revisão pendente** (ver `docs/web/13_CERTIFICACAO_WP06_ALTERACAO_ORCAMENTO.md` §10, `docs/web/14_CERTIFICACAO_WP07_NIVEL_AUTONOMIA.md` §9, `docs/web/15_CERTIFICACAO_WP08_PARADA_EMERGENCIA.md` §10, `docs/web/16_CERTIFICACAO_WP09_DESCONECTAR_E_CAPACIDADES.md` §10, `docs/web/17_CERTIFICACAO_WP10_TRILHA_AUDITORIA.md` §9 e `docs/web/18_CERTIFICACAO_WP11_METRICAS_HONESTAS.md` §9) |

---

## Decisões registradas

| ID | Decisão | Resultado | ADR |
|---|---|---|---|
| — | Nome do produto | **CAMPAIA** | ADR-0001 **APROVADA** |
| D-02 | Relação com o Kordena | **Independente, com integração futura opcional via API pública** | ADR-0002 **APROVADA** |
| D-04 | Ordem de canais | **Google → Meta → conjunto → WhatsApp** | ADR-0007 **APROVADA** |
| D-07 | Ambiente de desenvolvimento | Construir em Python puro sem PC; PC com VS Code fica para o fim (Bloco C) | `10_PLANO_AQUI_VS_PC.md` |
| — | Credenciais e configurabilidade | Modos PLATFORM e BYO; travas não desativáveis | ADR-0013 PROPOSTA |
| — | Autorização | RBAC (6 papéis) + ABAC (tenant, unidade, valor, idade da autenticação) | ADR-0014 PROPOSTA |
| — | Política de salvamento | Um bloco só está concluído quando está salvo. Sem acumular para o fim do dia | Decisão do Diretor, 26/08 |
| — | Correção de painel + priorização BFF/API | Diretor: *"sim siga sua sugestão eu aprovo"* | Decisão do Diretor, 27/08 — `docs/evidence/GAP_ANALISE_BACKEND_CAMPAIA_20260827.md` |
| — | Fechar a lacuna P-12 (testes de IA/agentes/PII) | Diretor: *"fechar a lacuna"* | Decisão do Diretor, 27/08 — `docs/evidence/EVIDENCIA_P12_20260827.md` |
| — | Conferir a API contra o contrato literal (recomendação de engenharia B) | Diretor: *"faça assim segundo a sua recomendação B"* | Decisão do Diretor, 28/08 — `docs/evidence/EVIDENCIA_P14_20260828.md` |
| — | Corrigir todas as divergências encontradas na conferência | Diretor: *"corrigir tudo"* | Decisão do Diretor, 28/08 — `docs/evidence/EVIDENCIA_P14_20260828.md` |
| — | Corrigir a imprecisão de `connector-hub-e-eventos.md` antes de qualquer avanço, depois seguir para persistência e para os blocos pendentes | Diretor: *"fazer essa correção primeiro antes de avançar e não deixar nada para tras, e depois na sequencia as opções 1 e a 2 pode executar dessa forma"* | Decisão do Diretor, 28/08 — `docs/evidence/EVIDENCIA_P15_20260828.md` |
| — | Achado de processo comunicado ao Diretor de forma transparente assim que descoberto (código da P-14 nunca salvo no Drive), com remediação iniciada imediatamente, sem aguardar resposta | Comunicação enviada ao Diretor, 28/08 — `docs/evidence/EVIDENCIA_REMEDIACAO_P14_20260828.md` | — |
| — | Persistência real (opção 1) construída sobre a base corrigida, não sobre uma restauração do Drive; verificada independentemente antes de aceitar o trabalho do agente delegado | Execução conforme a sequência autorizada pelo Diretor, 28/08 — `docs/evidence/EVIDENCIA_PERSISTENCIA_20260828.md` | — |
| — | Instrução de processo: como engenheiro sênior, sempre apresentar as opções com uma recomendação claramente marcada, em vez de um menu neutro | Diretor: *"vc é o eng senior sempre de as opções com a marcação de recomendo na melhor entre as opções"* | Decisão de processo do Diretor, 28/08 |
| — | Recomendação de engenharia para "opção 2": priorizar B9 (AsyncAPI) sobre B5/B7/B8/B10 — B7 não verificável sem credenciais reais; B8/B10 dependem de D-03/D-05 em aberto; B5 tem o maior escopo e menor base já verificada; B9 formaliza um catálogo já corrigido e verificado (P-15) | Recomendação apresentada e executada sob a autorização "pode executar dessa forma" do Diretor, 28-29/08 — `docs/evidence/EVIDENCIA_B9_ASYNCAPI_20260829.md` | — |
| — | Recomendação de engenharia para o bloco seguinte a B9: priorizar B10 (catálogo de erros em linguagem de usuário) sobre B5/B7/B8 — B7 não verificável sem credenciais reais; B8 depende de D-03 em aberto; B5 tem o maior escopo restante; B10 é documentação sobre uma taxonomia de erro já estável no código | Diretor: *"b10"*, 03/09 — `docs/evidence/EVIDENCIA_B10_CATALOGO_ERROS_20260903.md` | — |
| — | Corrigir o gap da P-19 (3 códigos do Connector Hub sem mapeamento HTTP), sem deixar nenhuma inconsistência entre código e documentação | Diretor: *"pode corrigir não vamos deixar nenhum erro para trás"*, 04/09 — `docs/evidence/EVIDENCIA_P19_FIX_20260904.md` | — |
| — | Construir B5 (motor de otimização e pacing), seguindo a recomendação apresentada ao final do fechamento da P-19 | Diretor: *"siga sua recomendação"*, 04/09 — `docs/evidence/EVIDENCIA_B5_OTIMIZACAO_PACING_20260904.md` | — |
| — | Escopo do B5: motor que apenas calcula e sugere otimizações (F8.1); execução automática (F8.2) permanece fora de escopo do MVP — decisão tomada mediante pergunta explícita, após a pesquisa de documentação revelar a distinção F8.1/F8.2 já registrada na Fase 0 de produto | Diretor: *"Motor que só sugere/calcula (Recomendado)"*, 04/09 — `docs/evidence/EVIDENCIA_B5_OTIMIZACAO_PACING_20260904.md` | — |
| — | Construir B7 (DDL e migrations do Postgres), seguindo a recomendação apresentada ao final do fechamento do B5, com a ressalva (que se mostrou incorreta — ver Seção 5 da evidência) de que a verificação seria apenas estática | Diretor: *"sim concordo"*, 04/09 — `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` | — |
| — | Escopo do B7 (parte 1): DDL cobrindo apenas as entidades já persistidas hoje via `api/db.py` (7 tabelas), não um modelo de dados especulativo para módulos que hoje só existem em memória | Diretor: *"DDL apenas do que já é persistido (Recomendado)"*, 04/09 — `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` | — |
| — | Escopo do B7 (parte 2): multi-tenancy construída para o caso simples (tenant_id obrigatório + RLS em toda tabela), com `business_unit_id` nullable e extensível, sem aguardar a resolução da D-03 | Diretor: *"Construir para o caso simples, deixando extensível (Recomendado)"*, 04/09 — `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` | — |
| D-03 | Segmento-alvo do MVP | **Qualquer pequeno negócio local** | Diretor, 27/08/2026 — `docs/evidence/DIRETOR_DECISOES_D03_D05_D06_D09_20260827.md`; reconciliado neste painel em 05/09 |
| D-05 | Objetivo do MVP (metas de conversão) | Diretor: *"os 4 já na primeira versão"* — as 4 metas de conversão completas já na v1, não um subconjunto | Diretor, 27/08/2026 — `docs/evidence/DIRETOR_DECISOES_D03_D05_D06_D09_20260827.md`; reconciliado neste painel em 05/09 |
| D-06 | Modelo comercial de IA | Diretor: *"Franquia inclusa no plano + créditos extras pagos"* — modelo híbrido | Diretor, 27/08/2026 — `docs/evidence/DIRETOR_DECISOES_D03_D05_D06_D09_20260827.md`; reconciliado neste painel em 05/09 |
| D-08 | Infraestrutura de nuvem | Google Cloud, região São Paulo. Diretor: *"pode seguir sua recomendação e depois faremos a pesquisa exata dos valores"* | Diretor, 27/08/2026 — `docs/evidence/DIRETOR_DECISAO_D08_INFRAESTRUTURA_20260827.md`; valores exatos de Cloud SQL, Pub/Sub, Cloud Workflows, Secret Manager e Cloud Storage confirmados em `docs/evidence/PESQUISA_VALORES_EXATOS_GOOGLE_CLOUD_20260827.md` — só o preço do Memorystore/Redis para São Paulo segue sem confirmação exata (P-11, página do GCP exige seleção manual de região); reconciliado neste painel em 05/09 |
| D-09 | Papéis de LGPD / entidade legal | Cliente = Controlador; F&M = Operadora | Diretor, 27/08/2026 — `docs/evidence/DIRETOR_DECISOES_D03_D05_D06_D09_20260827.md`; pré-requisito formal (CNPJ da F&M) em andamento — Diretor: *"a abertura do cnpj já esta em andamento já resolvi isso com minha contabilidade"* (`docs/evidence/DIRETOR_ATUALIZACAO_CNPJ_FM_20260827.md`); reconciliado neste painel em 05/09 |
| — | Workflow de orquestração (infra) | Cloud Workflows (não Temporal auto-hospedado) — ADR-0010. Diretor: *"Cloud Workflows (recomendação técnica)"* | Diretor, 27/08/2026 — `docs/evidence/DIRETOR_DECISAO_ADR0010_WORKFLOW_20260827.md`; reconciliado neste painel em 05/09 |
| — | Marcos de cadastro em plataformas: Google Ads Manager criado ("F&M Tecnologia", ID 975-498-3401), acesso de teste ao developer token | Marco registrado, sem token literal guardado (segredo nunca registrado, por disciplina do projeto) | Diretor, 27/08/2026 — `docs/evidence/DIRETOR_MARCO_GOOGLE_ADS_MANAGER_CRIADO_20260827.md`; reconciliado neste painel em 05/09 |
| — | Construir C1 (repositório Git + CI) e, em sequência, resolver as decisões então listadas como bloqueio (D-03/D-05/D-06/D-08/D-09) | Diretor: *"faça a opção 1 e depois na sequencia a opção 2 para não ficar nada pendente para trás"* | Decisão do Diretor, 05/09 — ver "Registro de blocos executados" |
| — | Host Git remoto do C1: GitHub | Diretor criou o repositório `https://github.com/faabio3131/CampaIA` (privado) e autenticou `gh`/`git` na própria máquina via login por navegador, sem nenhuma credencial passando por mim | Decisão do Diretor, 05/09 |
| — | Construir B8 (especificação das telas do app), com pesquisa real de mercado antes de fechar a especificação, priorizando funcionalidade sobre visual premium | Diretor: *"importante agora no momento é a funcionalidade. Depois nós vamos dar aquele talento num visual premium final. Depois que tiver tudo pronto testado."* | Decisão do Diretor, 05/09 — `docs/product/13_ESPECIFICACAO_TELAS_APP.md` |
| — | Autonomia no MVP: oferecer Modo Manual (Nível 1) E Modo Automático (Nível 2, limite definido pelo cliente), como diferencial competitivo | Diretor: *"Sobre nós deixarmos tudo com, autorização humana... Mas também vamos ter a opção do cliente querer escolher deixar no automático... assim nos coloca à frente, está concorrente."* | Decisão do Diretor, 05/09 — `docs/product/DECISOES_DIRETOR.md` item 5 |
| — | Salvaguarda do Modo Automático: ativação exige senha de administrador, criando prova de autorização contra disputas futuras | Diretor: *"essa opção de colocar no automatico precisará ser validada por senha de adm assim não teremos riscos de clientes reclamar que não fez ou que não autorizou"* | Decisão do Diretor, 05/09 — `docs/product/DECISOES_DIRETOR.md` item 5 |
| — | Campanha nova sempre exige aprovação manual, mesmo com Modo Automático ativo — a senha + limite autorizam apenas otimização de campanhas já aprovadas, nunca publicação inicial | Diretor: *"não o automatico só funcionará com aprovação humana mediante senha de adm e valor pré-definido"*, confirmado como "Campanha nova sempre manual (Recomendado)" em pergunta de fechamento | Decisão do Diretor, 05/09 — `docs/product/DECISOES_DIRETOR.md` item 5, seção "Esclarecimento adicional" |
| — | Repositório canônico do CampaIA: `faabio3131/f-m-tecnologia-campaia`; `faabio3131/CampaIA` vira fonte histórica somente leitura | Decisão do Diretor, baseada em auditoria completa de 19/09/2026 | ADR-0015 **APROVADA** — `docs/13_ADR_0015_RECONCILIACAO_REPOSITORIO_CANONICO.md` |
| — | App mobile (`mobile/`) em quarentena arquitetural — sem evolução, sem declaração de prontidão, decisão de destino adiada para depois do System Design Web | Decisão do Diretor, 19/09/2026 | `docs/evidence/MOBILE_QUARENTENA_ARQUITETURAL_20260919.md` |
| — | Ponto Zero Web e System Design completo produzidos; 4 ADRs propostas (frontend, autenticação, implantação, precedência Web First sobre Charter); nenhuma aprovada | Prompt mestre do Diretor "PONTO ZERO WEB E SYSTEM DESIGN COMPLETO", 19/09/2026 | `docs/web/00_INDICE_PONTO_ZERO_WEB.md` |
| — | ADRs 0016, 0017, 0018 e 0019 aprovadas (Lei Web First vs. Charter, framework de frontend, autenticação/sessão Web, implantação TARGET sobre D-08); Product Charter e NFR reconciliados literalmente com a Lei Web First; WP-01 explicitamente NÃO iniciado | Prompt mestre do Diretor "APROVAÇÃO ARQUITETURAL E MERGE CONTROLADO", 19/09/2026 | ADR-0016 **APROVADA**, ADR-0017 **APROVADA**, ADR-0018 **APROVADA**, ADR-0019 **APROVADA** |
| — | WP-01 (Fundação do frontend Web) implementado integralmente em `web/` (Next.js/React/TypeScript estrito, lint, typecheck, testes, CI, fixture tipada do contrato, sem chamada real ao BFF, sem token); WP-02 em diante explicitamente NÃO iniciados | Prompt mestre do Diretor "CAMPAIA WEB FIRST — EXECUÇÃO REAL E COMPLETA DO WP-01", 19/09/2026 | ADR-0017 (framework) executada — ver `docs/web/08_CERTIFICACAO_WP01_FUNDACAO_WEB.md` |
| — | WP-02 (Autenticação e sessão Web real) implementado: cliente OIDC provider-neutral, provedor de identidade de teste fail-closed, sessão real, CSRF, logout, `permissions.py` intocado; revisão de segurança humana (FM Security Engineer) explicitamente NÃO satisfeita por esta execução; WP-03 em diante NÃO iniciados | Prompt mestre do Diretor "CAMPAIA SaaS V1 COMPLETO", 20/09/2026 | ADR-0018 (autenticação) executada — ver `docs/web/09_CERTIFICACAO_WP02_AUTENTICACAO_SESSAO_WEB.md` |
| — | Confirmação do Diretor de que a execução do V1 completo deveria continuar sequencialmente pelos Work Packages restantes, um após o outro, após pergunta direta sobre se o prompt anterior havia sido 100% executado (resposta honesta: não — só WP-01/WP-02 estavam prontos) | Diretor: *"sim continue e siga as tarefas do prompt na sequencia um após o outro"* | Decisão do Diretor, 20-21/09/2026 |
| — | WP-03 (Contexto de tenant/unidade e shell do dashboard) implementado: memberships opcionais no ID token, `GET /session/memberships`/`POST /session/switch-tenant` (nunca via fixture Bearer), `/dashboard` com seletor de tenant e estados padronizados, `permissions.py` intocado; achado real de CORS/redirect cross-origin do WP-02 corrigido via `CAMPAIA_WEB_ORIGIN`, descoberto pela primeira suíte E2E cross-stack real (backend + frontend reais) deste projeto; revisão de segurança humana (FM Security Engineer) explicitamente NÃO satisfeita por esta execução; WP-04 em diante NÃO iniciados | Continuação de "CAMPAIA SaaS V1 COMPLETO", 21/09/2026 | ADR-0018 (autenticação/sessão, estendida) executada — ver `docs/web/10_CERTIFICACAO_WP03_TENANCY_SHELL.md` |
| — | Confirmação do Diretor para continuar a execução sequencial por mais 3 Work Packages (WP-04, WP-05 e um terceiro a ser definido por reconciliação, já que o roadmap só detalha WP-01 a WP-05 explicitamente) | Diretor: *"pode avançar mais 3 blocos"* | Decisão do Diretor, 21/09/2026 |
| — | WP-04 (Onboarding e Brand Kit) implementado: `/onboarding` com Brand Kit real (`POST/GET /brand-profiles`) e conectar contas simulado, `permissions.py` intocado; achado real corrigido: `POST /connections/oauth/start` nunca criava uma `Connection` de fato (`ConnectionRepository.create()` só chamado em testes) — fechado com `POST /connections/oauth/complete`, rota nova aditiva, sem a qual o critério de aceitação do próprio roadmap seria impossível de satisfazer; achado de design (consumo do `state` pendente fora do bloco de idempotência) pego por teste antes do deploy; revisão de FM QA Engineer explicitamente NÃO satisfeita por esta execução; WP-05 em diante NÃO iniciados | Continuação de "CAMPAIA SaaS V1 COMPLETO", 21/09/2026 | ver `docs/web/11_CERTIFICACAO_WP04_ONBOARDING_BRAND_KIT.md` |
| — | WP-05 (Briefing, estratégia e aprovação) implementado, fechando o Gate 5: `/campaigns` (briefing + estratégia via IA) e `/approvals` (validação + fila de aprovação com segregação de funções real), `permissions.py` intocado; CURRENT reconstruído por leitura direta confirmou que as 6 rotas de backend do escopo já existiam — nenhuma rota nova criada, só a camada Web; 3 achados reais corrigidos: contrato (`ApprovalRequest.requested_by` ausente), CSRF ausente em `ValidationPanel.tsx`, e shape estruturado (não texto livre) do output do agente `strategist`; jornada de dois usuários reais (`owner` propõe, `approver` decide) testada ponta a ponta via E2E cross-stack, incluindo a rejeição visível de autoaprovação; revisão de FM QA Engineer explicitamente NÃO satisfeita por esta execução; WP-06 em diante NÃO definidos | Continuação de "CAMPAIA SaaS V1 COMPLETO", 21/09/2026 | ver `docs/web/12_CERTIFICACAO_WP05_BRIEFING_APROVACAO.md` |
| — | WP-06 (Alteração de orçamento) definido por reconciliação de CURRENT (não previsto no roadmap original — 3º e último bloco de "pode avançar mais 3 blocos") e implementado: `BudgetPanel` em `/campaigns/[id]` reutiliza por completo a fila `/approvals` do WP-05 sobre `PATCH /campaigns/{id}/budget`, já existente, `permissions.py` intocado; nenhuma rota de backend nova; 3 achados reais de contrato corrigidos (`ApprovalRequest.kind`/`.amount` ausentes, `POST /approvals` nunca documentado) + 1 achado real de tipo (campos `Decimal` do orçamento serializam como string, não `number` como o contrato declara — corrigido no ponto de uso, divergência de tipo em si registrada como pendência); jornada de dois usuários reais (propor→aprovar→aplicar) testada ponta a ponta via E2E cross-stack; revisão de FM QA Engineer explicitamente NÃO satisfeita por esta execução; com este bloco, a autorização de 3 blocos do Diretor foi integralmente executada | Continuação de "CAMPAIA SaaS V1 COMPLETO", 21/09/2026 | ver `docs/web/13_CERTIFICACAO_WP06_ALTERACAO_ORCAMENTO.md` |
| — | Confirmação, pelo Diretor, do fato de que a cota gratuita de minutos do GitHub Actions da conta foi esgotada, e nova autorização explícita para continuar a construção enquanto a cota não renova (dia 31), com a confirmação do CI remoto adiada para depois disso — testes locais seguem obrigatórios a cada passo, não suspensos | Diretor: *"a minha cota gratuita no github foi alcançada e os testes CI creio que não será possivel concluir"*, seguido de *"a cota será renovada no dia 31 então vamos continuar trabalhando na construção e fazer tudo que for possível sem atrasar o termino e no final faremos os testes necessários"* | Decisão do Diretor, 21/09/2026 — registrada como P-38 |
| — | WP-07 (Nível de autonomia/Modo Manual-Automático) definido por reconciliação de CURRENT (não previsto no roadmap original, sob a nova autorização acima) e implementado: `AutonomyPanel` em `/dashboard` reutiliza por completo a fila `/approvals` do WP-05 sobre `GET/PUT /autonomy`, já existente, `permissions.py` intocado; nenhuma rota de backend nova; 2 achados reais de contrato corrigidos (`AutonomySettings.max_level_allowed`, `Campaign.business_unit_id` ausentes) + correção de uma regressão E2E pré-existente do WP-05 exposta pelo novo spec cross-stack; jornada de dois usuários reais (propor→aprovar→aplicar) testada ponta a ponta via E2E cross-stack, incluindo a recusa visível de um nível acima do teto contratado (invariante I-11); revisão de FM QA Engineer explicitamente NÃO satisfeita por esta execução; nenhum bloco além do WP-07 definido | Continuação de "CAMPAIA SaaS V1 COMPLETO", 21/09/2026 | ver `docs/web/14_CERTIFICACAO_WP07_NIVEL_AUTONOMIA.md` |
| — | Reafirmação explícita, pelo Diretor, da autorização de continuar a construção sob o mesmo bloqueio de cota do CI que autorizou o WP-07, servindo de base para definir e implementar o WP-08 | Diretor: *"sim provalvelmente as cotas não vai permitir rodar mais testes mas vamos continuar a construção eu autorizo prosseguir e no final iremos auditar e corrigir o que for necessário"* | Decisão do Diretor, 21/09/2026 |
| — | WP-08 (Parada de emergência/Kill Switch) definido por reconciliação de CURRENT (não previsto no roadmap original, sob a reafirmação acima) e implementado: `KillSwitchPanel` em `/dashboard` aciona `POST /kill-switch` diretamente sobre uma rota já existente, `permissions.py` intocado; nenhuma rota de backend nova; 1 achado real de contrato corrigido (`KillSwitchResult`, schema de resposta inteiro ausente); decisão de escopo registrada antes de qualquer código: kill switch escolhido sobre "pausar campanha" por uma lacuna arquitetural real (nenhuma rota nem operação canônica de retomada existe), e apenas os escopos CAMPAIGN/TENANT expostos na Web (GLOBAL cruza tenants por desenho, ACCOUNT/PLATFORM afetam múltiplas campanhas sem seleção individual); campanha realmente pausada testada via sessão Web real (backend), único caminho capaz de alcançar um estado pausável, já que nenhuma tela do Web app aciona publicação; revisão de FM QA Engineer explicitamente NÃO satisfeita por esta execução; nenhum bloco além do WP-08 definido | Continuação de "CAMPAIA SaaS V1 COMPLETO", 21/09/2026 | ver `docs/web/15_CERTIFICACAO_WP08_PARADA_EMERGENCIA.md` |
| — | Diretor autoriza construir mais 3 blocos, reafirmando a continuidade da construção durante o bloqueio de cota do CI | Diretor: *"pode sim construa mais 3 blocos"* | Decisão do Diretor, 21/09/2026 |
| — | WP-09 (Desconectar conta e ver capacidades da conexão) definido por reconciliação de CURRENT (não previsto no roadmap original, sob a autorização acima) e implementado: `ConnectAccountCard.tsx` (WP-04) ganha "Desconectar" (`DELETE /connections/{id}`) e a lista real de capacidades (`GET /connections/{id}/capabilities`, buscada no servidor, decisão revisada durante a implementação a partir de uma ideia inicial de GET client-side), `permissions.py` intocado; nenhuma rota de backend nova; 1 achado real de contrato corrigido (`Capability.provider`/`.country`/`.api_version` ausentes); 1 achado real de E2E (estado compartilhado entre specs do mesmo arquivo, mesma causa raiz da P-34, corrigido usando o provider META); revisão de FM QA Engineer explicitamente NÃO satisfeita por esta execução; WP-10/WP-11 seguem em execução | Continuação de "CAMPAIA SaaS V1 COMPLETO", 21/09/2026 | ver `docs/web/16_CERTIFICACAO_WP09_DESCONECTAR_E_CAPACIDADES.md` |
| — | WP-10 (Trilha de auditoria) definido por reconciliação de CURRENT (não previsto no roadmap original, mesma autorização do WP-09) e implementado: nova página `/audit` (Server Component puro, zero chamada de rede client-side) exibe `GET /audit-events`, já existente e já escrita por praticamente todo bloco desde o WP-04; `permissions.py` intocado; nenhuma rota de backend nova; nenhum achado de contrato (único bloco desta sequência sem correção de schema); 1 achado real de bug de UI pré-existente corrigido por causa raiz (`onboarding/page.tsx` contava conexões revogadas como conectadas para habilitar "Concluir Onboarding" — bug de interação com o WP-09, encontrado pela própria limpeza de estado do novo spec E2E deste bloco); revisão de FM QA Engineer explicitamente NÃO satisfeita por esta execução; WP-11 segue definido, sob a mesma autorização, mas ainda não implementado | Continuação de "CAMPAIA SaaS V1 COMPLETO", 21/09/2026 | ver `docs/web/17_CERTIFICACAO_WP10_TRILHA_AUDITORIA.md` |
| — | WP-11 (Métricas honestas da campanha) definido por reconciliação de CURRENT (não previsto no roadmap original, mesma autorização do WP-09/10) e implementado: nova seção "Métricas" em `/campaigns/{id}` exibe `GET /campaigns/{id}/insights`, já existente e já testada por fixtures Bearer; `permissions.py` intocado; nenhuma rota de backend nova; 1 achado real de contrato corrigido (`InsightSeries.note` ausente apesar de sempre presente na resposta real); nenhum outro achado real (diferente do WP-10, esta regressão não encontrou nenhum bug de UI pré-existente); revisão de FM QA Engineer explicitamente NÃO satisfeita por esta execução; **com este bloco, a autorização do Diretor de construir mais 3 blocos ("pode sim construa mais 3 blocos") está integralmente executada** (WP-09, WP-10, WP-11); nenhum bloco além do WP-11 foi definido | Continuação de "CAMPAIA SaaS V1 COMPLETO", 21/09/2026 | ver `docs/web/18_CERTIFICACAO_WP11_METRICAS_HONESTAS.md` |

---

## Estados por fase

Estados permitidos: `NÃO INICIADA`, `EM ANÁLISE`, `EM EXECUÇÃO`, `EM CORREÇÃO`, `BLOQUEADA`,
`IMPLEMENTADA — AGUARDANDO TESTES`, `EM VALIDAÇÃO`, `AGUARDANDO DEPENDÊNCIA EXTERNA`, `VALIDADA`, `CONCLUÍDA`.

| Fase | Escopo | Status | Gate de saída |
|---|---|---|---|
| 0 | Reconhecimento | **CONCLUÍDA** | — |
| 1 | Plano Mestre, ADRs, contratos, threat model, capability matrix | **EM VALIDAÇÃO** — contrato do BFF conferido campo a campo; catálogo de eventos agora formalizado em AsyncAPI 3.0 (ver Fase 3) | G1 Arquitetural |
| 2 | Fundação: projeto, auth, banco, segredos, CI | **EM EXECUÇÃO** — CI do backend concluído e 100% operacional (C1); persistência da camada API real (SQLite opcional); **fundação do frontend Web (WP-01) IMPLEMENTADA**, **autenticação/sessão Web real (WP-02) IMPLEMENTADA**, **shell de tenancy (WP-03) IMPLEMENTADA** (`web/`, CI próprio `frontend-tests.yml` com job cross-stack novo); restam revisão de segurança humana, segredos geridos e provisionamento real de infraestrutura de nuvem (Fase 5+) | G2 Segurança |
| 3 | Núcleo de campanhas + camada HTTP | **EM EXECUÇÃO** — 19 módulos de domínio (263 testes, incluindo `pacing.py`/`optimizer.py` do B5) + camada BFF/API sobre Starlette conforme ao contrato literal, com persistência real opcional (SQLite) e P-19 corrigida (80 testes), DDL do Postgres construído e verificado contra servidor real (B7, `backend/db/`), catálogo de eventos formalizado em AsyncAPI 3.0 (24 eventos), catálogo de erros de usuário publicado e completo (21 códigos, 10/10 valores de conector mapeados) e motor de otimização/pacing (B5) como camada de sugestão apenas, tudo efetivamente salvo no Drive | G6 parcial |
| 4 | IA: gateway multimodelo, agentes, evals | **EM EXECUÇÃO** — código testado (88 testes, P-12 fechada 27/08, inalterado); apenas evals seguem pendentes | G3 IA |
| 5 | Google Ads | NÃO INICIADA | G4 Integração |
| 6 | Meta (Facebook + Instagram) | NÃO INICIADA | G4 Integração |
| 7 | WhatsApp Business | NÃO INICIADA | G4 Integração |
| 8 | Aplicativo mobile completo | **EM ANÁLISE** — especificação de telas (B8) **CONCLUÍDA** (`docs/product/13_ESPECIFICACAO_TELAS_APP.md`); implementação real do app ainda NÃO INICIADA (já tem uma API real e conforme ao contrato para consumir, ainda que sem persistência) | G5 Mobile |
| 9 | Analytics e otimização | NÃO INICIADA — `GET /insights` da API devolve lista vazia, honestamente, sem inventar dado; formato do ponto agora bate com o contrato | G6 Financeiro |
| 10 | Segurança e robustez | NÃO INICIADA | G2 final |
| 11 | Homologação | NÃO INICIADA | G7 Final |
| 12 | Produção | NÃO INICIADA | Autorização expressa do Diretor |

---

## Código construído (`backend/campaia_core/` — B5 novo nesta sessão)

| Módulo | Responsabilidade | Testes confirmados no Drive |
|---|---|---|
| `errors.py` | Taxonomia canônica de erros | — |
| `states.py` | Máquina de estados com guardas | 8 (dentro de `test_invariantes.py`) |
| `autonomy.py` | Níveis 0–3 e gatilhos de aprovação | (dentro de `test_invariantes.py`) |
| `budget.py` | Reservas de verba e tetos | (dentro de `test_invariantes.py`) |
| `policy.py` | Policy Engine e emissão de autorização | (dentro de `test_invariantes.py`) |
| `infra.py` | Idempotência e Capability Registry | (dentro de `test_invariantes.py`) |
| `connectors.py` | Contrato dos adaptadores (3 de 14 operações canônicas) + `SecretRef` | (dentro de `test_saga.py`) |
| `simulator.py` | Provider Simulator com falhas programáveis | (dentro de `test_saga.py`) |
| `saga.py` | Publicação multicanal e compensação | `test_saga.py` — 24 testes |
| `permissions.py` | RBAC (6 papéis) + ABAC, segregação de funções | `test_permissions.py` — 32 testes |
| `webhooks.py` | Assinatura HMAC, replay, dedupe | (dentro de `test_webhooks_outbox.py`) |
| `outbox.py` | Outbox/Inbox, dual write | `test_webhooks_outbox.py` — 23 testes |
| `reconciliation.py` | **B3** — reconciliador de divergência | `test_reconciliation.py` — 26 testes |
| `ai_gateway.py` | Seleção de provedor, custo, schema, fallback, circuit breaker, trava de credencial | `test_ai_gateway.py` — 42 testes |
| `ai_simulator.py` | Provedor de IA simulado com falhas programáveis | `test_ai_simulator.py` — 13 testes |
| `sanitizer.py` | Pseudonimização de PII (CPF, CNPJ, e-mail, telefone, cartão, CEP) | `test_sanitizer.py` — 19 testes |
| `agents.py` | 6 agentes como funções puras; nunca inventa contexto faltante | `test_agents.py` — 14 testes |
| `pacing.py` | **B5 (04/09)** — cálculo de ritmo de gasto vs. período orçamentário, com sugestão de teto diário ajustado | `test_pacing.py` — 14 testes |
| `optimizer.py` | **B5 (04/09)** — 4 regras de sugestão (F8.1); nunca executa, apenas emite `Recommendation` imutável com `ActionKind` que exige aprovação humana via `autonomy.py` | `test_optimizer.py` — 12 testes |
| `fiscal_handoff.py` | **FISC V2-16.5 (importado 19/09 por cherry-pick de `faabio3131/CampaIA`)** — boundary fail-closed para handoff fiscal de billing próprio da CampaIA; aceita somente um `SettledOwnBillingFact` já assentado por domínio autoritativo de billing (inexistente hoje); estado sempre `PENDING_CAPABILITY`; deliberadamente **não conectado** a campanha, orçamento ou gasto de mídia — nenhum provider, município, alíquota, homologação ou `PRODUCTION_APPROVED` | `test_fiscal_handoff.py` — 4 testes |

**Total domínio: ver contagem real confirmada na Etapa 8 da matriz de validação desta reconciliação (abaixo), não presumida a partir de somas de versões anteriores.**

## Código construído (`backend/api/` e `backend/tests_api/`)

| Arquivo | Papel | Alterado nesta sessão? |
|---|---|---|
| `api/main.py` | App Starlette, montagem de rotas, `create_app(db_path=...)` | Não |
| `api/deps.py` | Autenticação por token fixo (fixture local, não é credencial real), step-up, idempotência | Não |
| `api/state.py` | Estado da aplicação, `AppState(db_path=...)` | Não |
| `api/repositories.py` | Repositórios (memória ou SQLite via `_attach_persistence`/`_wrap_loaded`) | Não |
| `api/db.py` | Persistência SQLite opcional (stdlib apenas) | Não |
| `api/errors.py` | Taxonomia de erro canônica | **Sim (04/09)** — P-19: 3 entradas novas em `STATUS_BY_CODE` (`AUTH_EXPIRED`, `VALIDATION_REJECTED`, `PARTIAL_FAILURE`); 3500→4509 bytes |
| `api/models.py` | Schemas Pydantic | Não |
| `api/helpers.py` | Serializers | Não |
| `api/routes_*.py` (9 arquivos) | Rotas HTTP | Não |
| `tests_api/*.py` (5 arquivos, 72 testes) | Testes da camada HTTP + persistência | Não |
| `tests_api/test_errors_p19.py` | **(04/09)** — 8 testes provando a correção da P-19 de ponta a ponta via `from_domain_error()` | — |
| `tests_api/test_smoke_endpoints.py` | **Alterado (19/09, reconciliação)** — 1 teste novo: `test_budget_patch_accepts_contract_daily_cap_alias`, cobrindo o alias do Achado 18 (`daily_cap` literal do contrato) end-to-end via cliente HTTP real, cobertura que estava genuinamente ausente (toda a cobertura anterior usava `new_daily_cap`) | Novo nesta reconciliação |

**Total camada HTTP: ver contagem real confirmada na Etapa 8 da matriz de validação desta reconciliação (abaixo).** Construída sobre Starlette, não FastAPI —
`pypi.org` está fora da lista de permissão de rede do sandbox onde foi construída; ver
`docs/evidence/EVIDENCIA_BFF_API_20260827.md` para o desvio completo.

## Código construído (`backend/db/` — B7 novo nesta sessão)

| Arquivo | Papel | Verificação |
|---|---|---|
| `db/001_initial_schema.sql` | DDL do Postgres (migration única) para as 7 tabelas já persistidas hoje via `api/db.py`, com RLS por tenant, `CHECK` nos enums do contrato, FK real `approvals→campaigns`, chave composta em `idempotency`, `business_unit_id` sempre nullable (D-03 em aberto) | Aplicado e testado contra Postgres 16 real (não apenas sintaxe) |
| `db/verify_ddl_postgres.sh` | Script reproduzível que cria banco/papel de teste próprios, aplica a migration e prova com queries reais: schema aplica sem erro, RLS isola tenants (inclusive o caso "sem contexto = zero linhas", falha fechado), `CHECK` rejeita/aceita corretamente, FK rejeita referência inexistente, chave composta de `idempotency` rejeita duplicata | Executado com sucesso 2x seguidas (reprodutibilidade), zero rastro deixado no servidor após cada execução |

Não altera nenhum módulo de `campaia_core/` ou `api/` — é puramente SQL + shell novos. Ver
`docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` para a transcrição completa da verificação real.

## Contratos (`contracts/` — B9 fechado nesta sessão)

| Arquivo | Papel | Novo/alterado nesta sessão? |
|---|---|---|
| `contracts/event-envelope.schema.json` | Envelope canônico de eventos (JSON Schema draft 2020-12) | Não — apenas lido e reproduzido |
| `contracts/campaign-brief.schema.json`, `campaign-plan.schema.json`, `ai-gateway.schema.json` | Schemas de payload referenciados pela descrição do AsyncAPI | Não — apenas lidos para contexto |
| `contracts/connector-hub-e-eventos.md` | Fonte de verdade do catálogo de 24 eventos (Seção 2) | Não — apenas lido; usado para extrair o catálogo programaticamente |
| `contracts/events.asyncapi.yaml` | **Novo** — especificação AsyncAPI 3.0 formalizando os 24 eventos (24 `channels`/`operations`/`components.messages`), todos referenciando o `EventEnvelope` | Novo nesta sessão (B9) |
| `contracts/validate_events_asyncapi.py` | **Novo** — script de verificação reproduzível (sintaxe YAML, cobertura 1:1, integridade de `$ref`, fidelidade do schema, exemplo validável) | Novo nesta sessão (B9) |

## Código construído (`web/` — WP-01 novo nesta sessão)

| Item | Papel | Verificação |
|---|---|---|
| `web/src/app/` | Layout raiz + tela de fundação ("CampaIA Web Foundation — WP-01"), sem lógica de negócio | Vitest (`tests/foundation-page.test.tsx`), Playwright (`e2e/foundation.spec.ts`) |
| `web/src/components/` | `Button`, `Card`, `Badge`, `PageContainer`, `StatusPanel` — únicos componentes usados nesta fase | `tests/components.test.tsx` |
| `web/src/styles/tokens.css` | Tokens de design (cor, espaçamento, tipografia, raio, sombra, motion, dark mode, `prefers-reduced-motion`) | Revisão manual + build |
| `web/src/contracts/bff-openapi.generated.ts` | Tipos gerados deterministicamente de `contracts/bff-openapi.yaml` via `openapi-typescript` (cabeçalho de origem, não editar manualmente) | `npm run contracts:check` — detecta drift; CI roda a cada push |
| `web/src/fixtures/me.local.ts` | Fixture local fictícia, tipada contra `Me`, consumida pela tela de fundação — nenhuma chamada de rede | `tests/me-fixture.test.ts` |
| `web/scripts/` (+ `scripts/lib/`) | CLIs e lógica compartilhada de verificação de drift de contrato e de fronteiras de segurança do WP-01 | `npm run contracts:check`, `npm run boundary:check`, `tests/boundaries.test.ts` |
| `web/tests/` | 5 arquivos, 15 testes Vitest (unitário, componente, acessibilidade via `jest-axe`, fronteiras) | `npm run test` |
| `web/e2e/foundation.spec.ts` | Smoke E2E Playwright (desktop + mobile), sem login, com captura real de requisições de rede e erros de console | `npm run test:e2e` |
| `.github/workflows/frontend-tests.yml` | CI do frontend: lint, typecheck, contract-drift, boundary-check, testes, build, E2E | Roda a cada push/PR para `main`; `backend-tests.yml` inalterado |

**Nenhuma chamada real ao BFF, nenhum token, nenhum segredo** — confirmado por `npm run boundary:check` (0 violações em 17 arquivos) e pelo teste E2E (0 requisições de rede ao BFF capturadas). Não altera nenhum módulo de `backend/campaia_core/`, `backend/api/`, `backend/db/` ou `contracts/`. Ver `docs/web/08_CERTIFICACAO_WP01_FUNDACAO_WEB.md` para comandos e resultados completos.

---

## Código construído (`backend/api/` e `web/` — WP-02 novo nesta sessão)

| Item | Papel | Verificação |
|---|---|---|
| `backend/api/oidc.py` | Cliente OIDC provider-neutral: PKCE, state, nonce, construção de authorize URL, verificação de ID token (assinatura via JWKS, issuer, audience, expiração, nonce) | `tests_api/test_oidc_unit.py` — 18 testes |
| `backend/api/test_idp.py` | Provedor de identidade de teste real (discovery, authorize com picker HTML, token, JWKS), fail-closed pelo mesmo interruptor dos fixtures do WP-01 | `tests_api/test_auth_session.py`, `test_auth_fail_closed.py` |
| `backend/api/routes_auth.py` | `/auth/login`, `/auth/callback` (troca PKCE real + verificação real de ID token), `/auth/logout` | `tests_api/test_auth_session.py` — 19 testes |
| `backend/api/csrf.py` | Middleware CSRF double-submit cookie, só ativo quando há cookie de sessão | `tests_api/test_auth_session.py::TestCsrf` |
| `backend/api/state.py`, `backend/api/deps.py`, `backend/api/main.py` | Sessão real (`AppState.sessions`), `require_auth` sessão-primeiro-fixture-depois, gate fail-closed dos fixtures do WP-01 (achado real: eram seedados incondicionalmente antes desta sessão), wiring das rotas/middleware | `tests_api/test_auth_fail_closed.py` — 8 testes |
| `web/src/lib/session.ts` | Leitura de sessão real, server-only (nunca no bundle do cliente) | `tests/account-page.test.tsx` |
| `web/src/components/LogoutButton.tsx` | Único ponto client-side de chamada de rede — logout protegido por CSRF | `tests/logout-button.test.tsx` |
| `web/src/app/account/` | Página `/account`: não configurado / sem sessão / sessão real, 3 estados testados | `tests/account-page.test.tsx`, `e2e/account.spec.ts` |

**`permissions.py` intocado** (confirmado por `git diff --stat`), conforme exigido pelo roadmap. `backend/campaia_core/` e `backend/db/` intocados. Ver `docs/web/09_CERTIFICACAO_WP02_AUTENTICACAO_SESSAO_WEB.md` para comandos e resultados completos, e para as pendências reais (revisão de segurança humana, CORS, step-up via sessão Web, provedor comercial).

## Código construído (`backend/api/` e `web/` — WP-03 novo nesta sessão)

| Item | Papel | Verificação |
|---|---|---|
| `backend/api/oidc.py` (`Membership`, `_parse_memberships`) | Claim opcional `campaia_memberships` no ID token, validada contra o membership ativo já verificado | `tests_api/test_oidc_unit.py::TestMemberships` — 8 testes |
| `backend/api/test_idp.py` (`multi_tenant_owner`) | Única identidade de teste com >1 membership real (OWNER demo-tenant, VIEWER other-tenant) | Usada por todos os testes de troca de tenant |
| `backend/api/state.py` (`SessionRecord.memberships`, `TokenPrincipal.with_membership`, `AppState.switch_tenant`) | Sessão carrega memberships reais; troca reemite sessão e invalida step-up em todos os tenants | `tests_api/test_tenant_switch.py` — 12 testes |
| `backend/api/deps.py` (`require_web_session`) | Memberships/troca exigem sessão real — nunca o fixture Bearer | `test_tenant_switch.py::TestGetMemberships` |
| `backend/api/routes_session.py` | `GET /session/memberships`, `POST /session/switch-tenant` | `test_tenant_switch.py` |
| `backend/api/routes_auth.py` (`_frontend_url`), `backend/api/main.py` (`_build_middleware`, CORS) | `CAMPAIA_WEB_ORIGIN` opcional: redirect pós-login/logout para a origem real do frontend + CORS restrito a essa origem exata | `tests_api/test_cross_origin.py` — 9 testes |
| `web/src/app/dashboard/` | Shell autenticado: tenant/unidade da sessão, estados não configurado/não autenticado/erro/vazio | `tests/dashboard-page.test.tsx`, `e2e/dashboard.spec.ts` |
| `web/src/components/TenantSwitcher.tsx` | Terceiro (e último) ponto client-side de chamada de rede — troca de tenant protegida por CSRF | `tests/tenant-switcher.test.tsx` |
| `web/src/components/{Loading,Error,Empty}State.tsx` | Estados padronizados usados por `/dashboard` | `tests/components.test.tsx` |
| `web/e2e-crossstack/`, `web/playwright.crossstack.config.ts` | Suíte E2E real: backend (`uvicorn`) + frontend (`next dev`) reais, HTTPS autoassinado, duas origens distintas | `npm run test:e2e:crossstack` — 3 testes |

**`permissions.py` intocado** (confirmado por leitura direta, sem alteração). `backend/campaia_core/` e `backend/db/` intocados. Ver `docs/web/10_CERTIFICACAO_WP03_TENANCY_SHELL.md` para comandos e resultados completos, e para as pendências reais (revisão de segurança humana, provedor comercial, seletor de unidade).

## Código construído (`backend/api/` e `web/` — WP-04 novo nesta sessão)

| Item | Papel | Verificação |
|---|---|---|
| `backend/api/state.py` (`PendingOAuth`, `AppState.oauth_pending`, `create_pending_oauth`/`pop_pending_oauth`) | Rastreia tentativas de `/connections/oauth/start` server-side, single-use, por tenant — mesmo padrão do `PendingLogin` do WP-02 | `tests_api/test_oauth_complete.py` |
| `backend/api/routes_connections.py` (`oauth_complete`, `oauth_start` atualizado) | `POST /connections/oauth/complete` — fecha o gap real: `oauth_start` sozinho nunca criava uma `Connection` | `tests_api/test_oauth_complete.py` — 7 testes |
| `web/src/app/onboarding/` | `/onboarding`: Brand Kit real + conectar contas simulado, regra "concluir com ≥1 canal" | `tests/onboarding-page.test.tsx`, `e2e/onboarding.spec.ts` |
| `web/src/components/BrandKitForm.tsx` | Quarto ponto client-side de chamada de rede — `POST /brand-profiles`, CSRF + Idempotency-Key | `tests/brand-kit-form.test.tsx` |
| `web/src/components/ConnectAccountCard.tsx` | Quinto ponto client-side de chamada de rede — encadeia `oauth/start`+`complete`, primeiro uso do header `X-Step-Up-Token` pelo frontend (marcador honesto, não MFA real) | `tests/connect-account-card.test.tsx` |
| `web/e2e-crossstack/onboarding.spec.ts` | E2E real: nada conectado no início, conectar habilita "Concluir Onboarding", Brand Kit persiste após reload | `npm run test:e2e:crossstack` — 3 testes novos |

**`permissions.py` intocado** (confirmado por leitura direta, sem alteração). `backend/campaia_core/` e `backend/db/` intocados. Ver `docs/web/11_CERTIFICACAO_WP04_ONBOARDING_BRAND_KIT.md` para comandos e resultados completos, e para as pendências reais (revisão de FM QA Engineer, cadastro de empresa/unidade, upload de logo, provedor comercial).

---

## Código construído (`contracts/` e `web/` — WP-05 novo nesta sessão)

| Item | Papel | Verificação |
|---|---|---|
| `contracts/bff-openapi.yaml` (`ApprovalRequest.requested_by` adicionado) | Achado real: campo sempre devolvido pelo backend (`serialize_approval`) mas ausente do contrato — corrigido aditivamente | `npm run contracts:check` — zero drift |
| `web/src/lib/session.ts` (`getServerCampaigns`/`getServerCampaign`/`getServerPlan`/`getServerApprovals`) | Leituras server-side de campanhas, plano e aprovações | `tests/{campaigns-page,campaign-detail-page,approvals-page}.test.tsx` |
| `web/src/components/BriefForm.tsx` | Sexto ponto client-side de chamada de rede — `POST /briefs`, CSRF + Idempotency-Key | `tests/brief-form.test.tsx` |
| `web/src/components/PlanPanel.tsx` | Sétimo ponto client-side — `POST .../plan/regenerate`; renderiza o output estruturado real do agente `strategist` (achado: não é texto livre) | `tests/plan-panel.test.tsx` |
| `web/src/components/ValidationPanel.tsx` | Oitavo ponto client-side — `POST .../validate` + `POST /approvals`; achado real corrigido: CSRF ausente na primeira versão | `tests/validation-panel.test.tsx` |
| `web/src/components/ApprovalDecisionCard.tsx` | Nono ponto client-side — `POST /approvals/{id}/decision`; segregação de funções só do servidor, nunca reimplementada na UI | `tests/approval-decision-card.test.tsx` |
| `web/src/app/campaigns/`, `web/src/app/approvals/` | `/campaigns`, `/campaigns/[id]`, `/approvals` — a jornada crítica completa | `tests/{campaigns-page,campaign-detail-page,approvals-page}.test.tsx`, `e2e/{campaigns,approvals}.spec.ts` |
| `backend/tests_api/test_briefing_approval_web_session.py` | Jornada completa via cookie de sessão Web real (não Bearer fixture), dois `TestClient` sobre o mesmo `AppState` | `python3 -m unittest discover -s tests_api` — 2 testes novos |
| `web/e2e-crossstack/briefing-approval-journey.spec.ts` | E2E real de dois usuários: `owner` propõe, tenta autoaprovar (recusado, visível), `approver` (BrowserContext separado) decide de verdade | `npm run test:e2e:crossstack` — 1 teste novo |

**`permissions.py`, `backend/campaia_core/`, `backend/db/`, `backend/api/routes_campaigns.py`, `backend/api/routes_approvals.py`, `backend/api/models.py` intocados** (confirmado por leitura direta e `git status` — nenhuma rota de backend nova, todas as 6 já existiam). Ver `docs/web/12_CERTIFICACAO_WP05_BRIEFING_APROVACAO.md` para comandos e resultados completos, e para as pendências reais (revisão de FM QA Engineer, publicação real, aprovação dupla não exercitada ponta a ponta).

---

## Código construído (`contracts/` e `web/` — WP-06 novo nesta sessão)

| Item | Papel | Verificação |
|---|---|---|
| `contracts/bff-openapi.yaml` (`ApprovalRequest.kind`/`.amount` adicionados; `POST /approvals` documentado) | 3 achados reais: dois campos sempre devolvidos pelo backend mas ausentes do contrato; uma rota já implementada (WP-05) nunca documentada — todos corrigidos aditivamente | `npm run contracts:check` — zero drift |
| `web/src/components/BudgetPanel.tsx` | Décimo ponto client-side de chamada de rede — `POST /approvals` (propor) + `PATCH /campaigns/{id}/budget` (aplicar, CSRF + step-up + idempotency-key); coerção `Number(...)` defensiva contra o achado de tipo (Decimal serializado como string) | `tests/budget-panel.test.tsx` |
| `web/src/components/ApprovalDecisionCard.tsx` (estendido) | `data-testid` estável por `kind`+`campaign_id`, adicionado para confiabilidade de teste E2E com múltiplas aprovações na mesma fila | `e2e-crossstack/budget-change.spec.ts` |
| `web/src/app/campaigns/[campaignId]/page.tsx` (estendido) | `BudgetPanel` integrado; achado real corrigido ao integrar: `hasPendingApproval` de `ValidationPanel` não filtrava por `kind`, quebraria com uma aprovação `BUDGET_CHANGE` pendente | `tests/campaign-detail-page.test.tsx` |
| `backend/tests_api/test_budget_change_web_session.py` | Jornada completa propor→aprovar→aplicar via cookie de sessão Web real, dois `TestClient` sobre o mesmo `AppState`; variação além do limite percentual recusada com `422 BUDGET_LIMIT` mesmo já aprovada | `python3 -m unittest discover -s tests_api` — 2 testes novos |
| `web/e2e-crossstack/budget-change.spec.ts` | E2E real de dois usuários: propor (owner) → aprovar (approver, BrowserContext separado, fila `/approvals` existente) → aplicar (owner) → orçamento exibido reflete o valor real | `npm run test:e2e:crossstack` — 1 teste novo |

**`permissions.py`, `backend/campaia_core/`, `backend/db/`, `backend/api/routes_campaigns.py`, `backend/api/routes_approvals.py`, `backend/api/models.py` intocados** (confirmado por leitura direta e `git status` — nenhuma rota de backend nova, `PATCH .../budget` e `POST /approvals` já existiam). Ver `docs/web/13_CERTIFICACAO_WP06_ALTERACAO_ORCAMENTO.md` para comandos e resultados completos, e para as pendências reais (revisão de FM QA Engineer, `total_amount` fora de escopo, serialização de `Decimal` como string em toda a API não auditada exaustivamente).

---

## Código construído (`contracts/` e `web/` — WP-07 novo nesta sessão)

| Item | Papel | Verificação |
|---|---|---|
| `contracts/bff-openapi.yaml` (`AutonomySettings.max_level_allowed`, `Campaign.business_unit_id` adicionados) | 2 achados reais: campos sempre devolvidos pelo backend mas ausentes do contrato — corrigidos aditivamente; `business_unit_id` descoberto pelo próprio `tsc` ao tipar explicitamente um fixture `Campaign` em teste | `npm run contracts:check` — zero drift |
| `web/src/components/AutonomyPanel.tsx` | Décimo primeiro ponto client-side de chamada de rede — `POST /approvals` (propor) + `PUT /autonomy` (aplicar, CSRF + step-up + idempotency-key); níveis acima do teto contratado desabilitados, nunca escondidos | `tests/autonomy-panel.test.tsx` — 10 testes novos |
| `web/src/app/dashboard/page.tsx` (estendido) | `AutonomyPanel` integrado ao shell do dashboard, com estado de erro próprio (degrada sem quebrar o resto do shell) | `tests/dashboard-page.test.tsx` — 1 teste novo |
| `web/src/lib/session.ts` (estendido) | `getServerAutonomy` — leitura server-side de `GET /autonomy` | `tests/dashboard-page.test.tsx` |
| `backend/tests_api/test_autonomy_change_web_session.py` | Jornada completa propor→aprovar→aplicar via cookie de sessão Web real, dois `TestClient` sobre o mesmo `AppState`, nível 1→0; nível acima do teto contratado aceito na proposta/aprovação mas recusado com `422 VALIDATION_FAILED` na aplicação (guarda anti-auto-promoção do domínio, invariante I-11) | `python3 -m unittest discover -s tests_api` — 2 testes novos |
| `web/e2e-crossstack/autonomy-change.spec.ts` | E2E real de dois usuários: propor (owner, `/dashboard`) → aprovar (approver, BrowserContext separado, fila `/approvals` existente) → aplicar (owner) → nível exibido reflete o valor real | `npm run test:e2e:crossstack` — 1 teste novo |
| `web/e2e-crossstack/briefing-approval-journey.spec.ts` (corrigido) | Regressão real exposta pela suíte cross-stack completa: duas asserções `getByText("APPROVED")` sem escopo, ambíguas com o card `APPROVED` que o novo spec de autonomia deixa na mesma fila compartilhada — delimitadas por `data-testid`, mesma disciplina do WP-06 | `npm run test:e2e:crossstack` — suíte completa 9/9 |

**`permissions.py`, `backend/campaia_core/`, `backend/db/`, `backend/api/routes_autonomy.py`, `backend/api/routes_approvals.py`, `backend/api/models.py` intocados** (confirmado por leitura direta e `git status` — nenhuma rota de backend nova, `GET/PUT /autonomy` e `POST /approvals` já existiam). Ver `docs/web/14_CERTIFICACAO_WP07_NIVEL_AUTONOMIA.md` para comandos e resultados completos, e para as pendências reais (revisão de FM QA Engineer, outros campos reais de `CampaignResponse` ainda ausentes do contrato, `max_level_allowed` não ajustável por nenhuma tela).

---

## Código construído (`contracts/` e `web/` — WP-08 novo nesta sessão)

| Item | Papel | Verificação |
|---|---|---|
| `contracts/bff-openapi.yaml` (`KillSwitchResult` adicionado) | 1 achado real: a resposta 202 de `/kill-switch` não tinha schema nenhum definido, apesar de sempre devolver `{scope, affected_campaign_ids}` — corrigido aditivamente | `npm run contracts:check` — zero drift |
| `web/src/components/KillSwitchPanel.tsx` | Décimo segundo ponto client-side de chamada de rede — `POST /kill-switch` direto, SEM fila de aprovação (dispensada pelo próprio domínio) e SEM `X-Step-Up-Token` (`Permission.KILL_SWITCH` fora de `REQUIRES_STEP_UP`); escopos CAMPAIGN/TENANT apenas, decisão do lado Web | `tests/kill-switch-panel.test.tsx` — 9 testes novos |
| `web/src/app/dashboard/page.tsx` (estendido) | `KillSwitchPanel` integrado ao shell do dashboard, com estado de erro próprio | `tests/dashboard-page.test.tsx` — 1 teste novo |
| `backend/tests_api/test_kill_switch_web_session.py` | Escopo CAMPAIGN pausa de verdade uma campanha `APPROVED`/publicada via sessão Web real (chegando lá pelo mesmo fluxo completo do WP-05: brief→plano→validação→aprovação→decisão→publicação), sem step-up; escopo TENANT sobre campanha DRAFT devolve lista vazia, não erro; identidade sem `KILL_SWITCH` recusada com 403 | `python3 -m unittest discover -s tests_api` — 3 testes novos |
| `web/e2e-crossstack/kill-switch.spec.ts` | E2E real de um usuário: escopo TENANT sobre uma campanha DRAFT (único estado alcançável via UI, já que nenhuma tela aciona publicação) — resultado real "nenhuma campanha afetada" exibido | `npm run test:e2e:crossstack` — 1 teste novo |

**`permissions.py`, `backend/campaia_core/`, `backend/db/`, `backend/api/routes_campaigns.py`, `backend/api/models.py` intocados** (confirmado por leitura direta e `git status` — nenhuma rota de backend nova, `POST /kill-switch` já existia). Ver `docs/web/15_CERTIFICACAO_WP08_PARADA_EMERGENCIA.md` para comandos e resultados completos, e para as pendências reais (revisão de FM QA Engineer, retomar campanha pausada sem rota nem operação canônica de conector, escopos ACCOUNT/PLATFORM/GLOBAL não expostos na Web).

---

## Código construído (`contracts/` e `web/` — WP-09 novo nesta sessão)

| Item | Papel | Verificação |
|---|---|---|
| `contracts/bff-openapi.yaml` (`Capability.provider`/`.country`/`.api_version` adicionados) | 1 achado real: campos sempre devolvidos pelo backend mas ausentes do contrato — corrigido aditivamente | `npm run contracts:check` — zero drift |
| `web/src/components/ConnectAccountCard.tsx` (estendido) | "Desconectar" (`DELETE /connections/{id}`, CSRF + step-up + idempotency-key) e exibição da lista real de capacidades (prop, não fetch próprio) | `tests/connect-account-card.test.tsx` — 4 testes novos |
| `web/src/lib/session.ts` (estendido) | `getServerConnectionCapabilities` — leitura server-side de `GET /connections/{id}/capabilities`, mantendo o boundary de rede já estabelecido (nenhuma chamada GET client-side nesta aplicação) | `tests/onboarding-page.test.tsx` |
| `web/src/app/onboarding/page.tsx` (estendido) | Busca capacidades para cada conexão ativa (`Promise.all`) e passa como prop a `ConnectAccountCard` | `tests/onboarding-page.test.tsx` — 1 teste novo |
| `backend/tests_api/test_connection_lifecycle_web_session.py` | Conectar → capacidades reais via sessão Web real → desconectar (204) → `GET /connections` confirma `status: REVOKED`; identidade sem `CONNECTION_MANAGE` recusada com 403 | `python3 -m unittest discover -s tests_api` — 2 testes novos |
| `web/e2e-crossstack/onboarding.spec.ts` (estendido) | Novo teste no spec já existente do WP-04: conectar (provider META) → ver capacidades reais → desconectar → volta a "Não conectado" | `npm run test:e2e:crossstack` — 1 teste novo |

**`permissions.py`, `backend/campaia_core/`, `backend/db/`, `backend/api/routes_connections.py`, `backend/api/models.py` intocados** (confirmado por leitura direta e `git status` — nenhuma rota de backend nova, `DELETE /connections/{id}` e `GET /connections/{id}/capabilities` já existiam). Ver `docs/web/16_CERTIFICACAO_WP09_DESCONECTAR_E_CAPACIDADES.md` para comandos e resultados completos, e para as pendências reais (revisão de FM QA Engineer, `evidence_url` sempre null).

---

## Código construído (`web/` — WP-10 novo nesta sessão)

| Item | Papel | Verificação |
|---|---|---|
| `web/src/app/audit/page.tsx` (novo, Server Component puro) | Nova página `/audit`: lista `GET /audit-events` (quando, quem, ação, alvo), mais recente primeiro, filtro opcional `campaign_id` | `tests/audit-page.test.tsx` — 6 testes novos |
| `web/src/lib/session.ts` (estendido) | `getServerAuditEvents` — leitura server-side de `GET /audit-events`, mantendo o boundary de rede já estabelecido | coberto pelos testes da página |
| `web/src/app/dashboard/page.tsx` (estendido) | Novo link "Trilha de auditoria" no card de navegação já existente | — |
| `backend/tests_api/test_audit_trail_web_session.py` | Eventos reais de outro bloco aparecem na trilha; filtro `campaign_id` restringe corretamente; identidade sem `AUDIT_VIEW` recusada com 403 | `python3 -m unittest discover -s tests_api` — 3 testes novos |
| `web/e2e-crossstack/audit-trail.spec.ts` (novo spec dedicado) | Conectar WHATSAPP → ver eventos reais em `/audit` → desconectar (higiene de estado compartilhado) | `npm run test:e2e:crossstack` — 1 teste novo |
| `web/src/app/onboarding/page.tsx` (corrigido — achado real, não escopo original do WP-10) | `canFinishOnboarding` usava `connections.length > 0` (contava conexões revogadas); corrigido para `activeConnections.length > 0` | `tests/onboarding-page.test.tsx` — 1 teste de regressão novo |

**`permissions.py`, `backend/campaia_core/`, `backend/db/`, `backend/api/routes_audit.py`, `backend/api/models.py`, `contracts/bff-openapi.yaml` intocados** (confirmado por leitura direta e `git status` — nenhuma rota de backend nova, `GET /audit-events` já existia; nenhum achado de contrato). Ver `docs/web/17_CERTIFICACAO_WP10_TRILHA_AUDITORIA.md` para comandos e resultados completos, incluindo o achado real do bug em `onboarding/page.tsx` (§2) e as pendências reais (revisão de FM QA Engineer, paginação real).

---

## Código construído (`web/` — WP-11 novo nesta sessão)

| Item | Papel | Verificação |
|---|---|---|
| `contracts/bff-openapi.yaml` (`InsightSeries.note` adicionado) | 1 achado real: campo sempre devolvido pelo backend mas ausente do contrato — corrigido aditivamente | `npm run contracts:check` — zero drift |
| `web/src/app/campaigns/[campaignId]/page.tsx` (estendido) | Nova seção "Métricas": nota real quando `points` vazio, pontos reais quando não; markup puro, sem novo Client Component | `tests/campaign-detail-page.test.tsx` — 3 testes novos |
| `web/src/lib/session.ts` (estendido) | `getServerInsights` — leitura server-side de `GET /campaigns/{id}/insights`, mantendo o boundary de rede já estabelecido | coberto pelos testes da página |
| `backend/tests_api/test_insights_web_session.py` | Placeholder honesto com `note` real via sessão Web real; `CAMPAIGN_VIEW` é ampla (toda role a possui) | `python3 -m unittest discover -s tests_api` — 2 testes novos |
| `web/e2e-crossstack/insights.spec.ts` (novo spec dedicado) | Submeter brief real → ver a nota honesta real na seção "Métricas" | `npm run test:e2e:crossstack` — 1 teste novo |

**`permissions.py`, `backend/campaia_core/`, `backend/db/`, `backend/api/routes_campaigns.py`, `backend/api/models.py` intocados** (confirmado por leitura direta e `git status` — nenhuma rota de backend nova, `GET /campaigns/{id}/insights` já existia). Ver `docs/web/18_CERTIFICACAO_WP11_METRICAS_HONESTAS.md` para comandos e resultados completos, e para as pendências reais (revisão de FM QA Engineer, camada de analytics real inexistente).

---

## Blocos ainda construíveis sem PC

| # | Item | Estado |
|---|---|---|
| — | Camada BFF/API (HTTP) | **CONCLUÍDA** (27/08) |
| — | Testes automatizados para `ai_gateway.py`, `ai_simulator.py`, `sanitizer.py`, `agents.py` (P-12) | **CONCLUÍDA** (27/08) |
| — | Conferência campo a campo entre `api/` e o YAML literal + correção de todas as divergências (P-14) | **CONCLUÍDA** (28/08) |
| — | Persistência real (banco de dados) para os Stores hoje em memória | **CONCLUÍDA** (28/08) — SQLite opcional via `api/db.py`, 8 novos testes |
| B3 | Reconciliador de divergência | **CONCLUÍDO** (confirmado 27/08, 26 testes) |
| B9 | AsyncAPI do catálogo de eventos | **CONCLUÍDO** (29/08) — `contracts/events.asyncapi.yaml`, 24 eventos, ver evidência |
| B10 | Catálogo de erros em linguagem de usuário | **CONCLUÍDO** (03/09) — `docs/09_CATALOGO_ERROS_USUARIO.md`, atualizado para v1.1.0 em 04/09 após a correção da P-19, 21 códigos cobertos, ver evidência |
| — | Correção da P-19 (gap de mapeamento HTTP no Connector Hub) | **CONCLUÍDA** (04/09) — `api/errors.py`, 3 entradas novas + 8 testes, ver evidência |
| B5 | Motor de otimização e pacing | **CONCLUÍDO** nesta sessão (04/09) — `pacing.py` + `optimizer.py`, camada de sugestão apenas (F8.1); F8.2 fora de escopo por confirmação do Diretor; 26 testes; ver evidência |
| B7 | DDL e migrations do Postgres | **CONCLUÍDO** nesta sessão (04/09) — `db/001_initial_schema.sql` + `db/verify_ddl_postgres.sh`, verificado de fato contra um Postgres 16 real descoberto nesta sandbox (não apenas sintaxe); ver evidência |
| B8 | Especificação das telas do app | **CONCLUÍDO** (05/09) — `docs/product/13_ESPECIFICACAO_TELAS_APP.md`, cobrindo F1–F10 com pesquisa real de mercado de concorrentes e a especificação do Modo Manual/Automático de autonomia (decisão do Diretor de 05/09); ver `docs/product/DECISOES_DIRETOR.md` item 5 |
| C1 | Repositório Git + CI | **CONCLUÍDO E 100% OPERACIONAL** (05/09) — publicado em `https://github.com/faabio3131/CampaIA` (privado), histórico Git, `.gitignore`, `requirements.txt` (backend e contracts) e workflow do GitHub Actions (`.github/workflows/backend-tests.yml`) rodando e verde a cada push; dois defeitos de portabilidade corrigidos durante a construção + um erro real de dependência de CI (`httpx` ausente) corrigido após a primeira execução real no GitHub (ver evidência) |
| — | Achado 18 — `BudgetPatchRequest` (`api/models.py`) não aceitava o nome de campo `daily_cap` exigido pelo contrato (`bff-openapi.yaml`), só `new_daily_cap` | **CORRIGIDO** (05/09) — alias `daily_cap`/`new_daily_cap` com `populate_by_name=True`; ambos os nomes validam, `extra="forbid"` preservado; **teste de regressão end-to-end adicionado em 19/09** (cobertura que estava ausente) |
| B8-mobile | Implementação do app mobile (Fase 8: onboarding + fluxo de nova campanha) | **PARCIALMENTE IMPLEMENTADO, EM QUARENTENA ARQUITETURAL** (construído antes de 19/09, fora desta sessão) — `mobile/` presente, mas `flutter analyze`/`flutter test` nunca executados de fato (SDK ausente, confirmado novamente em 19/09); não certificado, não evoluído nesta fase; decisão de destino pendente do System Design Web; ver `docs/evidence/MOBILE_QUARENTENA_ARQUITETURAL_20260919.md` |
| FISC | `FISC V2-16.5` — boundary fail-closed de billing próprio da CampaIA | **IMPLEMENTADO E IMPORTADO** (19/09, cherry-pick de `faabio3131/CampaIA`) — `fiscal_handoff.py` + 4 testes; **permanece formalmente bloqueado**: sem autoridade real de billing/pagamento próprio, não deve ser conectado a campanha/orçamento; ver `docs/evidence/V2_16_5_FISCAL_INTEGRATION_BLOCKER_20260913.md` |
| RECON | Reconciliação de repositórios (`faabio3131/CampaIA` → `faabio3131/f-m-tecnologia-campaia`) | **CONCLUÍDA** (19/09) — ADR-0015, bloco fiscal importado, fix `daily_cap` confirmado coerente + teste de regressão adicionado, mobile em quarentena formal; ver "Registro de blocos executados" |
| WP-01 | Fundação do frontend Web (`web/`) | **IMPLEMENTADO E VALIDADO** (19/09) — Next.js/React/TypeScript estrito, lint/typecheck/testes/build/E2E verdes a partir de checkout limpo, fixture tipada do contrato, CI próprio criado, sem chamada real ao BFF, sem token; ver `docs/web/08_CERTIFICACAO_WP01_FUNDACAO_WEB.md` |
| WP-02 | Autenticação e sessão Web real (`backend/api/{oidc,test_idp,routes_auth,csrf}.py` + `web/src/{lib/session.ts,components/LogoutButton.tsx,app/account/}`) | **IMPLEMENTADO E AUTOTESTADO** (20/09) — OIDC PKCE real, sessão `HttpOnly`/`Secure`, CSRF, logout, 126 testes de API verdes (45 novos), `permissions.py` intocado; **revisão de segurança humana (FM Security Engineer) pendente**; ver `docs/web/09_CERTIFICACAO_WP02_AUTENTICACAO_SESSAO_WEB.md` |
| WP-03 | Contexto de tenant/unidade e shell do dashboard (`backend/api/{routes_session,oidc,state,deps,main,routes_auth}.py` + `web/src/{app/dashboard/,components/TenantSwitcher.tsx,components/*State.tsx}`) | **IMPLEMENTADO E AUTOTESTADO** (21/09) — memberships opcionais, troca de tenant real com reemissão de sessão + invalidação de step-up, shell `/dashboard`, 155 testes de API verdes (12 de troca de tenant + 8 de memberships + 9 de CORS), 3 E2E cross-stack reais, `permissions.py` intocado; achado real de CORS/redirect cross-origin do WP-02 corrigido; **revisão de segurança humana (FM Security Engineer) pendente**; ver `docs/web/10_CERTIFICACAO_WP03_TENANCY_SHELL.md` |
| WP-04 | Onboarding e Brand Kit (`backend/api/{routes_connections,state}.py` + `web/src/{app/onboarding/,components/{BrandKitForm,ConnectAccountCard}.tsx}`) | **IMPLEMENTADO E AUTOTESTADO** (21/09) — Brand Kit real, conectar contas simulado, regra "concluir com ≥1 canal", 162 testes de API verdes (7 novos), 6 E2E cross-stack reais (3 do WP-03 + 3 novos), `permissions.py` intocado; achado real corrigido: `POST /connections/oauth/start` nunca criava uma `Connection`, fechado com `POST /connections/oauth/complete`; **revisão de FM QA Engineer pendente**; ver `docs/web/11_CERTIFICACAO_WP04_ONBOARDING_BRAND_KIT.md` |
| WP-05 | Briefing, estratégia e aprovação (`contracts/bff-openapi.yaml` + `web/src/{app/{campaigns,approvals}/,components/{BriefForm,PlanPanel,ValidationPanel,ApprovalDecisionCard}.tsx}`) | **IMPLEMENTADO E AUTOTESTADO** (21/09) — fecha o Gate 5; briefing → estratégia (IA) → validação → fila de aprovação, segregação de funções real testada com dois usuários; 164 testes de API verdes (2 novos, via sessão Web real), 7 E2E cross-stack reais (6 dos WP-03/04 + 1 novo), `permissions.py` intocado; nenhuma rota de backend nova (as 6 já existiam); 3 achados reais corrigidos: contrato (`requested_by` ausente), CSRF ausente em `ValidationPanel.tsx`, shape estruturado do output do agente `strategist`; **revisão de FM QA Engineer pendente**; ver `docs/web/12_CERTIFICACAO_WP05_BRIEFING_APROVACAO.md` |
| WP-06 | Alteração de orçamento (`contracts/bff-openapi.yaml` + `web/src/{app/campaigns/[campaignId]/page.tsx,components/BudgetPanel.tsx}`) | **IMPLEMENTADO E AUTOTESTADO** (21/09) — bloco não previsto no roadmap original, definido por reconciliação de CURRENT; reutiliza a fila `/approvals` do WP-05 por completo sobre `PATCH /campaigns/{id}/budget`, já existente; 166 testes de API verdes (2 novos, via sessão Web real), 8 E2E cross-stack reais (7 dos WP-03/04/05 + 1 novo), `permissions.py` intocado; nenhuma rota de backend nova; 3 achados reais de contrato corrigidos (`kind`/`amount` ausentes, `POST /approvals` nunca documentado) + 1 achado real de tipo (`Decimal` serializado como string, não `number`); **revisão de FM QA Engineer pendente**; ver `docs/web/13_CERTIFICACAO_WP06_ALTERACAO_ORCAMENTO.md` |
| WP-07 | Nível de autonomia/Modo Manual-Automático (`contracts/bff-openapi.yaml` + `web/src/{app/dashboard/page.tsx,components/AutonomyPanel.tsx}`) | **IMPLEMENTADO E AUTOTESTADO** (21/09) — bloco não previsto no roadmap original, definido por reconciliação de CURRENT sob nova autorização do Diretor (cota do CI esgotada); reutiliza a fila `/approvals` do WP-05 por completo sobre `GET/PUT /autonomy`, já existente; 168 testes de API verdes (2 novos, via sessão Web real), 9 E2E cross-stack reais (8 dos WP-03/04/05/06 + 1 novo), `permissions.py` intocado; nenhuma rota de backend nova; 2 achados reais de contrato corrigidos (`max_level_allowed`, `business_unit_id` ausentes) + correção de uma regressão E2E pré-existente do WP-05; **revisão de FM QA Engineer pendente**; ver `docs/web/14_CERTIFICACAO_WP07_NIVEL_AUTONOMIA.md` |
| WP-08 | Parada de emergência/Kill Switch (`contracts/bff-openapi.yaml` + `web/src/{app/dashboard/page.tsx,components/KillSwitchPanel.tsx}`) | **IMPLEMENTADO E AUTOTESTADO** (21/09) — bloco não previsto no roadmap original, definido por reconciliação de CURRENT, mesma autorização do WP-07; aciona `POST /kill-switch` diretamente, sem fila de aprovação (dispensada pelo domínio) e sem step-up (`KILL_SWITCH` fora de `REQUIRES_STEP_UP`); escopos CAMPAIGN/TENANT apenas; 171 testes de API verdes (3 novos, via sessão Web real, incluindo uma campanha realmente pausada), 10 E2E cross-stack reais (9 dos WP-03/04/05/06/07 + 1 novo), `permissions.py` intocado; nenhuma rota de backend nova; 1 achado real de contrato corrigido (schema de resposta inteiro ausente) + 1 achado real de lacuna arquitetural (retomar campanha pausada, não corrigido, P-40); **revisão de FM QA Engineer pendente**; ver `docs/web/15_CERTIFICACAO_WP08_PARADA_EMERGENCIA.md` |
| WP-09 | Desconectar conta e ver capacidades da conexão (`contracts/bff-openapi.yaml` + `web/src/{app/onboarding/page.tsx,components/ConnectAccountCard.tsx}`) | **IMPLEMENTADO E AUTOTESTADO** (21/09) — bloco não previsto no roadmap original, definido por reconciliação de CURRENT sob autorização de "mais 3 blocos"; `ConnectAccountCard.tsx` (WP-04) ganha "Desconectar" e capacidades reais, buscadas no servidor; 173 testes de API verdes (2 novos, via sessão Web real), 11 E2E cross-stack reais (10 dos WP-03 a WP-08 + 1 novo), `permissions.py` intocado; nenhuma rota de backend nova; 1 achado real de contrato corrigido (`provider`/`country`/`api_version` ausentes de `Capability`) + 1 achado real de estado compartilhado entre specs de E2E (P-34); **revisão de FM QA Engineer pendente**; ver `docs/web/16_CERTIFICACAO_WP09_DESCONECTAR_E_CAPACIDADES.md` |
| WP-10 | Trilha de auditoria (`web/src/app/audit/page.tsx`) | **IMPLEMENTADO E AUTOTESTADO** (21/09) — bloco não previsto no roadmap original, definido por reconciliação de CURRENT, mesma autorização do WP-09; nova página `/audit` (Server Component puro) exibe `GET /audit-events`, já existente; 176 testes de API verdes (3 novos, via sessão Web real), 12 E2E cross-stack reais (11 dos WP-03 a WP-09 + 1 novo), `permissions.py` intocado; nenhuma rota de backend nova; **nenhum achado de contrato** + 1 achado real de bug de UI pré-existente (`onboarding/page.tsx`, interação WP-04/WP-09), corrigido por causa raiz; **revisão de FM QA Engineer pendente**; ver `docs/web/17_CERTIFICACAO_WP10_TRILHA_AUDITORIA.md` |
| WP-11 | Métricas honestas da campanha (`web/src/app/campaigns/[campaignId]/page.tsx`) | **IMPLEMENTADO E AUTOTESTADO** (21/09) — bloco não previsto no roadmap original, definido por reconciliação de CURRENT, mesma autorização do WP-09/10; nova seção "Métricas" exibe `GET /campaigns/{id}/insights`, já existente; 178 testes de API verdes (2 novos, via sessão Web real), 13 E2E cross-stack reais (12 dos WP-03 a WP-10 + 1 novo), `permissions.py` intocado; nenhuma rota de backend nova; 1 achado real de contrato corrigido (`InsightSeries.note` ausente), nenhum outro achado real; **revisão de FM QA Engineer pendente**; ver `docs/web/18_CERTIFICACAO_WP11_METRICAS_HONESTAS.md` |

**Recomendação de engenharia (corrigida em v31 — a versão abaixo estava desatualizada desde v30):**
o System Design da aplicação Web real (Ponto Zero Web) foi produzido, as ADRs 0016–0019 foram
**aprovadas** (19/09/2026, PR #4 mergeada em `main`@`41b521f`), e o **WP-01 a WP-11 já foram
implementados e autotestados** (ver linhas correspondentes acima e as respectivas
certificações) — o Gate 5 (Primeira jornada) está **tecnicamente completo**, com revisão de FM QA
Engineer formalmente pendente, e o WP-06/WP-07/WP-08/WP-09/WP-10/WP-11 (extensões do Gate 5)
também implementados com a mesma pendência. **Com o WP-06, a autorização de 3 blocos do Diretor
("pode avançar mais 3 blocos") foi integralmente executada** (WP-04, WP-05, WP-06); o WP-07 e o
WP-08 foram autorizados separadamente, pela confirmação do bloqueio de cota do CI; **com o WP-11,
a nova autorização de "mais 3 blocos" ("pode sim construa mais 3 blocos") também está
integralmente executada** (WP-09, WP-10, WP-11). O roadmap
(`docs/web/06_ROADMAP_WORK_PACKAGES.md` §3) ainda cita candidatos não detalhados como Work
Package (publicação sandbox, reconciliação, otimização, hardening formal, acessibilidade formal,
observabilidade formal, segurança formal, staging, produção controlada), a maioria bloqueada por
decisões ou credenciais externas não resolvidas nesta missão. **Qualquer bloco além do WP-11
exigirá nova autorização explícita do Diretor E nova reconciliação de CURRENT com definição
formal de escopo**, pela mesma disciplina usada para definir cada bloco até aqui — implementar
sem isso antecipa escopo sem autoridade nem base real. A revisão de segurança humana (FM Security
Engineer) do WP-02/WP-03 e a revisão de FM QA Engineer do WP-04 a WP-11 seguem pendentes,
independentemente de qualquer bloco futuro. A decisão sobre o destino final do app mobile
(`mobile/`, em quarentena) segue pendente de decisão humana futura, sem prazo vinculado a nenhum
Work Package específico. O CI remoto (GitHub Actions) permanece sem confirmação de verde nesta
versão, por cota esgotada da conta — não uma regressão de código; a confirmação fica para depois
da renovação da cota, prevista para o dia 31.

---

## Artefatos de documentação e contratos

| Artefato | Arquivo | Status |
|---|---|---|
| Diagnóstico inicial | `docs/00_DIAGNOSTICO_INICIAL.md` | CONCLUÍDO |
| Plano Mestre | `docs/02_PLANO_MESTRE.md` | CONCLUÍDO (v0.1) |
| ADRs | `docs/03_ADR_INICIAIS.md` | 3 aprovadas, 5 propostas (inclui ADR-0014, ainda não escrita como arquivo próprio) |
| ADR-0015 reconciliação de repositórios | `docs/13_ADR_0015_RECONCILIACAO_REPOSITORIO_CANONICO.md` | **APROVADA** (19/09) |
| ADR-0016 precedência Web First vs. Charter | `docs/14_ADR_0016_LEI_WEB_FIRST_VS_NATUREZA_MOBILE_CHARTER.md` | **APROVADA** (19/09) |
| ADR-0017 framework de frontend Web | `docs/15_ADR_0017_FRAMEWORK_FRONTEND_WEB.md` | **APROVADA** (19/09) |
| ADR-0018 autenticação e sessão Web | `docs/16_ADR_0018_AUTENTICACAO_SESSAO_WEB.md` | **APROVADA** (19/09) |
| ADR-0019 implantação TARGET | `docs/17_ADR_0019_IMPLANTACAO_TARGET.md` | **APROVADA** (19/09) |
| Ponto Zero Web (8 documentos) | `docs/web/00_INDICE_PONTO_ZERO_WEB.md` a `07_CERTIFICACAO_PONTO_ZERO_WEB.md` | **PRODUZIDO, ARQUITETURA APROVADA, MERGE CERTIFICADO** (19/09) |
| Certificação do WP-01 | `docs/web/08_CERTIFICACAO_WP01_FUNDACAO_WEB.md` | **CONCLUÍDO** (19/09) — fundação do frontend Web implementada e validada |
| Certificação do WP-02 | `docs/web/09_CERTIFICACAO_WP02_AUTENTICACAO_SESSAO_WEB.md` | **IMPLEMENTADO E AUTOTESTADO** (20/09) — autenticação e sessão Web real; revisão de segurança humana pendente |
| Certificação do WP-03 | `docs/web/10_CERTIFICACAO_WP03_TENANCY_SHELL.md` | **IMPLEMENTADO E AUTOTESTADO** (21/09) — tenant/unidade e shell do dashboard; revisão de segurança humana pendente |
| Certificação do WP-04 | `docs/web/11_CERTIFICACAO_WP04_ONBOARDING_BRAND_KIT.md` | **IMPLEMENTADO E AUTOTESTADO** (21/09) — onboarding e Brand Kit; revisão de FM QA Engineer pendente |
| Certificação do WP-05 | `docs/web/12_CERTIFICACAO_WP05_BRIEFING_APROVACAO.md` | **IMPLEMENTADO E AUTOTESTADO** (21/09) — briefing, estratégia e aprovação, fecha o Gate 5; revisão de FM QA Engineer pendente |
| Certificação do WP-06 | `docs/web/13_CERTIFICACAO_WP06_ALTERACAO_ORCAMENTO.md` | **IMPLEMENTADO E AUTOTESTADO** (21/09) — alteração de orçamento, bloco definido por reconciliação de CURRENT; revisão de FM QA Engineer pendente |
| Certificação do WP-07 | `docs/web/14_CERTIFICACAO_WP07_NIVEL_AUTONOMIA.md` | **IMPLEMENTADO E AUTOTESTADO** (21/09) — nível de autonomia (Modo Manual/Automático), bloco definido por reconciliação de CURRENT sob nova autorização do Diretor; revisão de FM QA Engineer pendente |
| Certificação do WP-08 | `docs/web/15_CERTIFICACAO_WP08_PARADA_EMERGENCIA.md` | **IMPLEMENTADO E AUTOTESTADO** (21/09) — parada de emergência (kill switch), bloco definido por reconciliação de CURRENT, mesma autorização do WP-07; revisão de FM QA Engineer pendente |
| Certificação do WP-09 | `docs/web/16_CERTIFICACAO_WP09_DESCONECTAR_E_CAPACIDADES.md` | **IMPLEMENTADO E AUTOTESTADO** (21/09) — desconectar conta e ver capacidades da conexão, bloco definido por reconciliação de CURRENT sob autorização de "mais 3 blocos"; revisão de FM QA Engineer pendente |
| Certificação do WP-10 | `docs/web/17_CERTIFICACAO_WP10_TRILHA_AUDITORIA.md` | **IMPLEMENTADO E AUTOTESTADO** (21/09) — trilha de auditoria, bloco definido por reconciliação de CURRENT, mesma autorização do WP-09; revisão de FM QA Engineer pendente |
| Certificação do WP-11 | `docs/web/18_CERTIFICACAO_WP11_METRICAS_HONESTAS.md` | **IMPLEMENTADO E AUTOTESTADO** (21/09) — métricas honestas da campanha, bloco definido por reconciliação de CURRENT, mesma autorização do WP-09/10; revisão de FM QA Engineer pendente |
| Registro de quarentena do mobile | `docs/evidence/MOBILE_QUARENTENA_ARQUITETURAL_20260919.md` | **CONCLUÍDO** (19/09) |
| Decisões do Diretor | `docs/04_DECISOES_DO_DIRETOR.md` | 4 resolvidas, 5 abertas |
| Arquitetura Mestra V2 | `docs/05_ARQUITETURA_V2.md` | PROPOSTA |
| Modelo lógico de dados | `docs/06_MODELO_DADOS.md` | PROPOSTA (v0.1) |
| Threat model | `docs/07_THREAT_MODEL.md` | **PARCIAL** (T-02 coberta por código) |
| Capability Matrix | `docs/08_CAPABILITY_MATRIX.md` | **PARCIAL** v0.2 |
| ADR-0013 credenciais | `docs/09_ADR_0013_...md` | PROPOSTA |
| Plano aqui vs PC | `docs/10_PLANO_AQUI_VS_PC.md` | Vivo |
| Contratos JSON Schema (4) | `contracts/*.schema.json` | Sintaxe validada |
| OpenAPI do BFF | `contracts/bff-openapi.yaml` | Rascunho (`1.0.0-draft`); implementação em `backend/api/` conferida campo a campo (P-14) |
| Connector Hub + eventos | `contracts/connector-hub-e-eventos.md` | PROPOSTA — corrigida em 28/08 (P-15); catálogo de eventos (Seção 2) agora também formalizado como AsyncAPI 3.0 (B9, 29/08) |
| **Catálogo de eventos AsyncAPI** | `contracts/events.asyncapi.yaml` | **CONCLUÍDO** (29/08) — 24 eventos, verificado por script próprio |
| **Catálogo de erros em linguagem de usuário** | `docs/09_CATALOGO_ERROS_USUARIO.md` | **CONCLUÍDO** (03/09, atualizado para v1.1.0 em 04/09) — 21 códigos cobertos, 10/10 valores de `ConnectorErrorCode` mapeados |
| Análise de lacunas reais do backend | `docs/evidence/GAP_ANALISE_BACKEND_CAMPAIA_20260827.md` | CONCLUÍDO |
| Evidência da camada BFF/API | `docs/evidence/EVIDENCIA_BFF_API_20260827.md` | CONCLUÍDO |
| Evidência do fechamento da P-12 | `docs/evidence/EVIDENCIA_P12_20260827.md` | CONCLUÍDO |
| Evidência do fechamento da P-14 | `docs/evidence/EVIDENCIA_P14_20260828.md` | CONCLUÍDO |
| Evidência do fechamento da P-15 | `docs/evidence/EVIDENCIA_P15_20260828.md` | CONCLUÍDO |
| Evidência do achado e remediação: código da P-14 nunca salvo no Drive (P-16) | `docs/evidence/EVIDENCIA_REMEDIACAO_P14_20260828.md` | CONCLUÍDO |
| Evidência do fechamento da persistência real (opção 1 do Diretor) | `docs/evidence/EVIDENCIA_PERSISTENCIA_20260828.md` | CONCLUÍDO |
| Evidência do fechamento do B9 (AsyncAPI) | `docs/evidence/EVIDENCIA_B9_ASYNCAPI_20260829.md` | CONCLUÍDO |
| Evidência do fechamento do B10 (catálogo de erros) | `docs/evidence/EVIDENCIA_B10_CATALOGO_ERROS_20260903.md` | CONCLUÍDO |
| Evidência do fechamento da P-19 (gap de mapeamento HTTP no Connector Hub) | `docs/evidence/EVIDENCIA_P19_FIX_20260904.md` | CONCLUÍDO |
| Evidência do fechamento do B5 (motor de otimização e pacing) | `docs/evidence/EVIDENCIA_B5_OTIMIZACAO_PACING_20260904.md` | CONCLUÍDO |
| Evidência do fechamento do B7 (DDL e migrations do Postgres) | `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` | CONCLUÍDO |
| **Repositório Git + CI (C1)** | `https://github.com/faabio3131/CampaIA` (privado); cópias locais em `Desktop/CAMPAIA/campaia_repo.zip` e `Desktop/CAMPAIA/campaia.bundle` no PC do Diretor | **100% OPERACIONAL** (05/09) — CI (`.github/workflows/backend-tests.yml`) rodando e verde a cada push |
| Evidência da correção do CI (C1) | `docs/evidence/EVIDENCIA_C1_CI_HTTPX_FIX_20260905.md` | CONCLUÍDO (05/09) |
| **Especificação das telas do app (B8)** | `docs/product/13_ESPECIFICACAO_TELAS_APP.md` | **CONCLUÍDO** (05/09) — F1–F10, pesquisa de mercado de concorrentes, fluxo completo de nova campanha, Modo Manual/Automático de autonomia |
| Decisões do Diretor de 05/09 (autonomia Nível 2 no MVP) | `docs/product/DECISOES_DIRETOR.md` item 5 | CONCLUÍDO (05/09) — citações literais do Diretor, propagado para `CAMPAIA_PRODUCT_CHARTER.md` e `OUT_OF_SCOPE.md` |
| Decisões do Diretor de 27/08 (D-03, D-05, D-06, ADR-0010, marcos Google Ads/CNPJ) | `docs/evidence/DIRETOR_DECISOES_D03_D05_D06_D09_20260827.md`, `DIRETOR_DECISAO_D08_INFRAESTRUTURA_20260827.md`, `PESQUISA_VALORES_EXATOS_GOOGLE_CLOUD_20260827.md`, `DIRETOR_DECISAO_ADR0010_WORKFLOW_20260827.md`, `DIRETOR_MARCO_GOOGLE_ADS_MANAGER_CRIADO_20260827.md`, `DIRETOR_ATUALIZACAO_CNPJ_FM_20260827.md` | CONCLUÍDO (27/08) — reconciliadas com este painel em 05/09 |

---

## Pendências técnicas abertas

| ID | Pendência | Efeito se confirmada |
|---|---|---|
| P-04 | Marca CAMPAIA: INPI, domínio, lojas | Bloqueia identidade visual e submissão às lojas |
| P-08 | Meta: Advantage+ Shopping/App não criáveis pela Marketing API desde a v25.0 (fonte secundária) | Remove tipos de campanha do conjunto oferecível |
| P-09 | WhatsApp: mudanças de preço em 01/08/2026 e 01/10/2026 | Preço vira dado versionado com data de vigência |
| P-10 | Documentação da Meta que exige login | Confirmação definitiva exige acesso autenticado |
| P-12 | ~~`ai_gateway.py`, `ai_simulator.py`, `sanitizer.py`, `agents.py` sem teste automatizado confirmado no Drive~~ | **FECHADA em 27/08** — 88 testes reais, `docs/evidence/EVIDENCIA_P12_20260827.md` |
| P-13 | Camada BFF/API construída sobre Starlette, não FastAPI (bloqueio de rede a `pypi.org` no sandbox) | Se FastAPI for exigido especificamente (ex.: docs OpenAPI automáticas), precisa de ambiente com acesso a índice de pacotes Python |
| P-14 | ~~Implementação da API seguiu paráfrase do contrato, não o YAML de `bff-openapi.yaml` linha a linha~~ | **FECHADA em 28/08** — 17 divergências encontradas e corrigidas, `docs/evidence/EVIDENCIA_P14_20260828.md` |
| P-15 | ~~`contracts/connector-hub-e-eventos.md` descrevia a interface do conector no presente, como se já implementada para 3 plataformas reais~~ | **FECHADA em 28/08** — corrigido para contrato-alvo + nota de status real, `docs/evidence/EVIDENCIA_P15_20260828.md` |
| P-16 | ~~O código corrigido da P-14 nunca foi de fato enviado ao Google Drive~~ | **FECHADA em 28/08** — 11+3 arquivos reenviados e byte a byte verificados; contagem de domínio corrigida (233→237); `docs/evidence/EVIDENCIA_REMEDIACAO_P14_20260828.md` |
| P-17 | ~~Catálogo de eventos caracterizado como tendo "22 eventos" em registros anteriores deste projeto (inclusive resumos de sessão); a tabela-fonte real (`connector-hub-e-eventos.md`, Seção 2) tem 23 linhas, uma das quais agrupa 2 eventos distintos, totalizando 24~~ | **FECHADA em 29/08**, descoberta durante a extração programática do catálogo para o B9 (AsyncAPI); nenhum evento foi adicionado/removido — apenas a contagem foi corrigida; `docs/evidence/EVIDENCIA_B9_ASYNCAPI_20260829.md` |
| P-18 | Tentativa de instalar `@asyncapi/cli` (npm) e de baixar o meta-schema JSON oficial do AsyncAPI 3.0 (GitHub raw) para validação de referência — ambas bloqueadas por `403 Forbidden` da política de rede do sandbox | Verificação do B9 feita por script de validação próprio (sintaxe YAML real, cobertura 1:1, integridade de `$ref`, fidelidade de schema, exemplo validável), não pela ferramenta de referência oficial; documentado com transparência em `docs/evidence/EVIDENCIA_B9_ASYNCAPI_20260829.md`. Não bloqueia o fechamento do bloco, mas fica registrado como limitação de ambiente |
| P-19 | ~~3 dos 10 códigos de `ConnectorErrorCode` (`campaia_core/connectors.py`) — `AUTH_EXPIRED`, `VALIDATION_REJECTED`, `PARTIAL_FAILURE` — não têm entrada em `STATUS_BY_CODE` (`api/errors.py`); achado durante o B10, confirmado que `VALIDATION_REJECTED` já é levantado por código real em `saga.py:191`~~ | **FECHADA em 04/09** — sob autorização do Diretor ("pode corrigir não vamos deixar nenhum erro para trás"): 3 entradas adicionadas a `STATUS_BY_CODE` (`AUTH_EXPIRED`→401, `VALIDATION_REJECTED`→422, `PARTIAL_FAILURE`→207), com racional documentado; 8 novos testes provam a correção via `from_domain_error()`; 237+80=317 testes, zero regressão; catálogo B10 atualizado para v1.1.0 em sincronia; `docs/evidence/EVIDENCIA_P19_FIX_20260904.md`. Anomalia não relacionada, documentada e não corrigida: discrepância de 1 byte em `api/routes_approvals.py` (metadado do Drive vs. conteúdo real), root-caused como externa a este trabalho |
| P-20 | ~~Upload do documento de evidência do B5 divergiu em 1 byte do original local (13344 vs. 13343); root-cause por download/diff programático revelou uma corrupção real de conteúdo (um "à" virou "a"), causada por um erro de transcrição meu ao reproduzir a string base64 na chamada da ferramenta de upload — não um comportamento do Drive~~ | **FECHADA em 04/09** — arquivo local corrigido, recodificado e reenviado; a versão corrigida foi verificada por download/diff byte a byte completo (não apenas contagem de tamanho) antes de ser aceita como correta; versão com erro trashada no Drive; `docs/evidence/EVIDENCIA_B5_OTIMIZACAO_PACING_20260904.md`. Reforça a disciplina já em vigor no projeto: contagem de bytes sozinha não basta — divergência de tamanho exige investigação por diff de conteúdo, nunca apenas reenvio |
| P-11 | Preço exato do Memorystore/Redis (Google Cloud) para a região São Paulo (`southamerica-east1`) — a página oficial de preços é renderizada por JavaScript com seletor de região; buscas e fetch de página só retornam a tabela da região padrão (Iowa) | Orçamento de infraestrutura (D-08) fica completo em todos os demais itens (Cloud SQL, Pub/Sub, Cloud Workflows, Secret Manager, Cloud Storage já confirmados — `docs/evidence/PESQUISA_VALORES_EXATOS_GOOGLE_CLOUD_20260827.md`), exceto este; não bloqueia nenhum bloco de construção, só o orçamento final |
| P-21 | Este painel (`v15` e anteriores) listava D-03, D-05, D-06, D-08 e D-09 como "Bloqueios" em aberto desde pelo menos 04/09/2026, quando na verdade o Diretor já as havia decidido em 27/08/2026 (evidências correspondentes existiam no Drive, mas o painel nunca foi atualizado para refletir isso) | **FECHADA em 05/09** — descoberto durante a reconstrução do repositório C1 (ao baixar `docs/evidence/` por completo, os 6 arquivos de decisão de 27/08 foram lidos na íntegra); comunicado ao Diretor com transparência; painel corrigido nesta versão (ver "Decisões registradas" e "Situação atual") |
| P-22 | App mobile (`mobile/`) nunca teve `flutter analyze`/`flutter test` executados de fato, em nenhum ambiente documentado até 19/09/2026 (SDK ausente, confirmado novamente nesta reconciliação) | Bloqueia qualquer declaração de prontidão do mobile; mobile permanece em quarentena arquitetural até validação real com SDK, conforme `docs/evidence/MOBILE_QUARENTENA_ARQUITETURAL_20260919.md` |
| P-23 | Repositório canônico do CampaIA definido nesta reconciliação (`faabio3131/f-m-tecnologia-campaia`); `faabio3131/CampaIA` não deve receber mais nenhum commit a partir de 19/09/2026 | Risco: se alguém continuar commitando na fonte histórica por hábito, uma nova divergência se reabre; mitigação em ADR-0015 |
| P-24 | ~~System Design da aplicação Web real (Ponto Zero Web) ainda não produzido~~ | **RESOLVIDA em 19/09** — produzido em `docs/web/`, ver P-25 para o que ainda falta |
| P-25 | ~~System Design Web produzido mas nenhuma ADR aprovada (0016–0019); nenhum Work Package pode começar~~ | **FECHADA em 19/09** — ADR-0016–0019 aprovadas pelo Diretor Fábio Aluizio da Silva (PR #4 mergeada, `main`@`41b521f`); WP-01 implementado nesta mesma data. Esta linha já estava desatualizada dentro da própria v20 deste painel (a tabela "Decisões registradas" da mesma versão já registrava as 4 ADRs como `APROVADA`) — corrigido aqui para não perpetuar a divergência |
| P-26 | Nenhum adaptador real de Google Ads/Meta/WhatsApp existe; integrações reais dependem de terceiros (homologação, sandbox) | Bloqueia os Work Packages de publicação sandbox em diante; não bloqueia os WP-01 a WP-05 |
| P-27 | Autenticação real ainda não existe (`api/deps.py` usa token fixo de teste) | Maior superfície de risco do roadmap; WP-02 é o Work Package que resolve, condicionado à escolha do provedor de identidade — **WP-01 (concluído) explicitamente não alterou este estado**, por desenho (ver `docs/web/08_CERTIFICACAO_WP01_FUNDACAO_WEB.md` §18) |
| P-28 | ~~CI da PR do WP-01 ainda não confirmado no momento em que este painel foi escrito~~ | **RESOLVIDA** — CI confirmado verde (Backend + Frontend) no HEAD certificado da PR #5; Gate 2 aprovado |
| P-29 | Revisão de segurança humana (FM Security Engineer) do WP-02/WP-03 (autenticação, sessão e tenancy Web real) não realizada nesta execução — apenas autorização de execução do Diretor | Gate 3 e Gate 4 não podem ser declarados formalmente aprovados enquanto esta revisão não ocorrer; WP-02/WP-03 estão implementados e extensamente autotestados (155 testes de API), mas isso não substitui revisão humana de uma superfície de autenticação/tenancy real |
| P-30 | ~~CORS explícito por ambiente e teste dedicado de step-up via sessão Web (ambos itens do roadmap original do WP-02) não implementados nesta execução~~ | **PARCIALMENTE FECHADA em 21/09** — CORS explícito por ambiente implementado (`CAMPAIA_WEB_ORIGIN`, `backend/api/main.py`), descoberto como bloqueio real (não apenas item de checklist) pela suíte E2E cross-stack do WP-03, 9 testes novos (`test_cross_origin.py`); ver `docs/web/10_CERTIFICACAO_WP03_TENANCY_SHELL.md` §4. **Ainda em aberto**: teste dedicado de step-up via sessão Web genérico (distinto do teste de invalidação de step-up na troca de tenant, que existe e passa) |
| P-31 | Item do roadmap do WP-03 "seletor de tenant/unidade" implementado só parcialmente: tenant, sim (testado ponta a ponta); unidade, não — nenhuma identidade de teste tem mais de uma unidade dentro do mesmo tenant, então não há UI real disso para exercitar. Domínio já suporta (`Principal.business_unit_ids: frozenset`, confirmado por leitura direta, não alterado) | Não bloqueia WP-04 em diante; deve ser fechado quando uma identidade multi-unidade real existir para testar contra |
| P-32 | ~~Contrato (`contracts/bff-openapi.yaml`) não listava `ApprovalRequest.requested_by`, embora o backend sempre o retorne (`api/helpers.py serialize_approval`)~~ | **FECHADA em 21/09** — campo adicionado aditivamente ao contrato; achado durante a construção do WP-05 (`ApprovalDecisionCard.tsx`); ver `docs/web/12_CERTIFICACAO_WP05_BRIEFING_APROVACAO.md` §2 |
| P-33 | Escopo do WP-06 (e além) não detalhado pelo roadmap original (`06_ROADMAP_WORK_PACKAGES.md` §3 só cita candidatos, sem Work Package formal) — o CURRENT que permitiria defini-lo com precisão só existe agora que o WP-01 a WP-05 estão implementados | Bloqueia início de um 3º bloco autorizado pelo Diretor ("pode avançar mais 3 blocos") até reconciliação de CURRENT e definição formal do escopo do WP-06 |
| P-34 | `web/playwright.crossstack.config.ts` corrigido para `workers: 1` no WP-05, após um achado real: `fullyParallel: false` só serializa testes DENTRO de um arquivo de spec, não entre arquivos — com o número padrão de workers, `tenant-switch.spec.ts` e `onboarding.spec.ts`/`briefing-approval-journey.spec.ts` rodando em paralelo contra o mesmo backend real (`AppState` compartilhado) corromperam estado de sessão/cookie um do outro de forma intermitente | Sem essa correção, o job `crossstack-e2e` do CI (que não passa `--workers` explicitamente) teria risco real de flake intermitente, não relacionado a nenhuma regressão de código — corrigido antes de se materializar em CI |
| P-35 | ~~`contracts/bff-openapi.yaml`'s `ApprovalRequest` não listava `kind` nem `amount`, e `POST /approvals` nunca estivera documentado, apesar dos três já serem reais e usados desde o WP-05~~ | **FECHADA em 21/09** — os 3 corrigidos aditivamente no WP-06, achado durante a construção de `BudgetPanel.tsx`; ver `docs/web/13_CERTIFICACAO_WP06_ALTERACAO_ORCAMENTO.md` §2 |
| P-36 | ~~`Campaign.budget` (`total_amount`/`daily_cap`/`spent_to_date`) é tipado `number` pelo contrato, mas a resposta real serializa esses campos `Decimal` como STRING JSON — confirmado por teste de backend (`"500"`, não `500`). Corrigido apenas no ponto de uso do WP-06 (`BudgetPanel.tsx`, via `Number(...)`); a divergência de tipo em si permanece em todo outro campo `Decimal` da API, não auditada exaustivamente~~ | **FECHADA em 22/09/2026** (missão de fechamento integral, Etapa A1) — decisão tomada: preservar a semântica real (string decimal, sem perda de precisão), nunca converter para `number`. Backend: `BudgetResponse` agora tipa os 3 campos `str` explicitamente (mesmo padrão já usado por `ApprovalResponse.amount` desde o WP-06), construído via `str(Decimal(...))` em `serialize_campaign`. Contrato: os 3 campos de `Campaign.budget`, `ApprovalRequest.amount` (request) e `PATCH .../budget`'s `daily_cap` agora declaram `type: string`; o campo `total_amount` do `PATCH .../budget` — que o contrato aceitava mas o backend real sempre recusava (`BudgetPatchRequest`, `extra="forbid"`, achado adicional encontrado durante este fechamento) — foi removido do schema, fechando essa divergência também. Frontend: `BudgetPanel.tsx` não roteia mais nenhum valor monetário por `Number(...)` nos payloads de rede (propor e aplicar enviam a string original do usuário/da resposta, nunca reconvertida); a comparação de igualdade que decide o estado "aplicado" permanece via `Number(...)`, por ser uma decisão de UI local, nunca enviada à rede. Novo teste de regressão de precisão (`test_decimal_precision_survives_propose_approve_apply_as_a_string`, valor "599.99", sem representação binária exata em IEEE-754) confirma round-trip byte-a-byte propose→approve→apply→GET. Regressão completa sem quebra: 267 domínio, 179 API (178+1), 135 Vitest, contrato sincronizado |
| P-37 | ~~Com a implementação do WP-06, a autorização "pode avançar mais 3 blocos" do Diretor está integralmente executada (WP-04, WP-05, WP-06)~~ | **SUPERADA em 21/09** — o Diretor concedeu nova autorização, distinta da de "3 blocos", para continuar a construção durante o bloqueio de cota do CI (ver P-38); o WP-07 foi definido e implementado sob essa nova autorização |
| P-38 | Cota gratuita de minutos do GitHub Actions da conta esgotada — **FATO CONFIRMADO pelo Diretor** em 21/09/2026 ("a minha cota gratuita no github foi alcançada e os testes CI creio que não será possivel concluir"), consistente com a assinatura já observada em todo run desde o WP-03 (`runner_id: 0`, conclusão em ~2-4s) | Nenhuma PR desta missão pode ter seu CI remoto confirmado verde enquanto a cota não renovar (dia 31, segundo o Diretor); não é regressão de código. O Diretor autorizou continuar a construção enquanto isso ("vamos continuar trabalhando na construção..."), com os testes locais seguindo obrigatórios a cada passo — decisão registrada em "Decisões registradas" |
| P-39 | ~~Achado do WP-07: outros campos reais de `CampaignResponse` (`brief`, `plan_version`, `connection_id`, `last_synced_at`) permanecem ausentes de `contracts/bff-openapi.yaml`'s `Campaign`, mesma classe dos achados já fechados um a um (`business_unit_id` incluído, fechado nesta mesma versão)~~ | **FECHADA em 22/09/2026** — os 4 campos corrigidos aditivamente nesta auditoria de reconciliação, confirmados reais por leitura direta de `api/models.py CampaignResponse` e por chamada HTTP real (`GET /campaigns/{id}` via sessão de teste); `npm run contracts:generate`/`contracts:check` confirmaram sincronia; `npx vitest run` 135/135 sem regressão. Nenhum consumidor precisou de mudança de código — correção puramente aditiva ao schema |
| P-40 | Achado do WP-08: `PAUSED → ACTIVE` (retomar uma campanha pausada) é uma transição real e guardada no domínio (`campaia_core/states.py _guard_resume`), mas nenhuma rota HTTP existe para acioná-la, e `campaia_core/connectors.py`'s `AdsConnector` Protocol não define nenhuma operação canônica de retomada (só `validate_draft`, `publish`, `pause`) | Uma campanha pausada (por kill switch ou, futuramente, pelo próprio `POST /campaigns/{id}/pause`) não pode ser reativada por nenhuma rota existente hoje; fechar isso de verdade exige adicionar uma operação canônica nova ao Protocol dos conectores — expansão arquitetural real, não autorizada pela reconciliação do WP-08, decisão para quem assumir esse trabalho |
| P-41 | Achado do WP-08: nenhuma tela do Web app aciona `POST /campaigns/{id}/publish` (confirmado pelo próprio comentário preexistente em `campaigns/[campaignId]/page.tsx`, não introduzido por este bloco) | Nenhum caminho puramente de navegador consegue levar uma campanha a um estado pausável (`APPROVED`/`PUBLISHING`/`ACTIVE`/`OPTIMIZING`); limita o alcance de qualquer E2E cross-stack que dependa de um estado pausável (ex.: o escopo CAMPAIGN do kill switch) ao que pode ser alcançado via chamadas HTTP diretas em teste de backend, não via browser real |
| P-42 | Achado do WP-09: `contracts/bff-openapi.yaml`'s `Capability` não declarava `provider`, `country` nem `api_version`, apesar de `CapabilityResponse` (`api/models.py`) sempre devolvê-los | **FECHADA em 21/09** — os 3 campos corrigidos aditivamente no WP-09; `npm run contracts:generate`/`contracts:check` confirmaram sincronia; ver `docs/web/16_CERTIFICACAO_WP09_DESCONECTAR_E_CAPACIDADES.md` §2 |
| P-43 | Seletor de unidade de negócio (business unit) dentro do mesmo tenant: `SwitchTenantRequest` (`api/models.py`) só aceita `tenant_id`, nenhum campo de unidade; não existe rota alguma para trocar de unidade sem trocar de tenant. Distinto da pendência já registrada (P-31, sobre falta de identidade de teste multi-unidade) — esta é sobre a ausência da própria rota, não apenas de dado de teste | Bloqueia a construção de um seletor de unidade real: expor essa troca exigiria estender `SwitchTenantRequest`/`state.switch_tenant()` com um campo de unidade opcional — mudança real de comportamento de sessão, não uma simples exposição Web de rota já pronta como todo WP-06 a WP-11; não construído nesta reconciliação por exigir uma decisão de produto/segurança própria sobre a semântica de troca de unidade |
| P-44 | ~~Achado do WP-10: `web/src/app/onboarding/page.tsx` calculava `canFinishOnboarding` a partir de `connections.length > 0` (lista bruta, incluindo conexões revogadas) em vez de `activeConnections.length > 0` — equivalentes antes do WP-09 introduzir desconexão (status `REVOKED`), deixaram de ser equivalentes depois; uma conexão existente apenas em forma revogada habilitava incorretamente "Concluir Onboarding" com zero canais de fato conectados~~ | **FECHADA em 21/09** — corrigido por causa raiz usando `activeConnections.length > 0` (já calculada na mesma página para a busca de capacidades do WP-09); novo teste Vitest de regressão cobre exatamente esse estado; encontrado pela própria limpeza de estado do novo spec E2E do WP-10 (conectar e desconectar `WHATSAPP`), não por auditoria preventiva; ver `docs/web/17_CERTIFICACAO_WP10_TRILHA_AUDITORIA.md` §2 |
| P-45 | ~~Achado do WP-11: `contracts/bff-openapi.yaml`'s `InsightSeries` não declarava `note`, apesar de `InsightSeriesResponse` (`api/models.py`) sempre devolvê-lo~~ | **FECHADA em 21/09** — o campo corrigido aditivamente no WP-11; `npm run contracts:generate`/`contracts:check` confirmaram sincronia; ver `docs/web/18_CERTIFICACAO_WP11_METRICAS_HONESTAS.md` §2. Último achado real de contrato conhecido nesta sequência de blocos (WP-06 a WP-11); nenhum outro campo real ausente do contrato foi encontrado durante a construção do WP-11 |

---

## Dependências externas

| Dependência | Necessária para | Estado |
|---|---|---|
| Google Cloud project + OAuth client | Fase 5 | Não iniciada |
| Developer token Google Ads (Test → Explorer → Basic) | Fase 5 | Não iniciada — Basic: alvo de ~5 dias úteis |
| Meta App + Business portfolio + verificação | Fase 6 | Não iniciada |
| Meta: permissões + Marketing API Access Tier | Fase 6 produção | Exige ≥500 chamadas em 15 dias com erro <15% |
| WhatsApp Business Account + número + templates | Fase 7 | Não iniciada |
| Chaves OpenAI e Gemini | Fase 4 | D-06 resolvida (27/08 — modelo híbrido: franquia inclusa + créditos extras pagos); aquisição das chaves ainda não iniciada |
| Nuvem + orçamento de infraestrutura | Fase 2 | D-08 resolvida (27/08 — Google Cloud, São Paulo); orçamento exato pendente só do preço do Memorystore/Redis em São Paulo (P-11) |
| Apple Developer + Google Play | Fase 12 | Não iniciada |

Nenhum item acima foi solicitado, obtido ou simulado.

**Caminho crítico:** o tier da Meta exige histórico real de chamadas com erro baixo. A qualidade do adaptador
Meta é pré-requisito da aprovação, não refinamento posterior.

---

## Registro de blocos executados

| Data | Bloco | Fase | Resultado | Evidência |
|---|---|---|---|---|
| 25/08/2026 | Reconhecimento + Plano Mestre inicial | 0 → 1 | Diagnóstico, painel, plano, 6 ADRs, 7 decisões abertas | `docs/` |
| 25/08/2026 | Decisões D-02/D-04 + camada de arquitetura | 1 | Arquitetura V2, contratos, modelo de dados, threat model | `docs/`, `contracts/` |
| 25/08/2026 | OpenAPI do BFF + Capability Matrix v0.2 | 1 | ADR-0007 aprovada; API contratada e checada | checagens C1–C3 |
| 25/08/2026 | ADR-0013 + núcleo determinístico | 3 | 6 módulos; 40 testes | saída do unittest |
| 25/08/2026 | Provider Simulator + Saga multicanal | 3 | 9 módulos; 64 testes; D-07 resolvida | `10_PLANO_AQUI_VS_PC.md` |
| 25/08/2026 | Cópia para o Google Drive | — | 29 arquivos, tamanhos conferidos byte a byte | Pasta CAMPAIA |
| 26/08/2026 | B6 — permissões RBAC/ABAC | 3 | `permissions.py` + 32 testes; ADR-0014 | 96/96 aprovados |
| 26/08/2026 | B4 — webhooks, outbox e inbox | 3 | `webhooks.py`, `outbox.py` + 27 testes; T-02 coberta | 123/123 aprovados |
| 26/08/2026 | B2 — AI Gateway com provedor simulado | 3 → 4 | `ai_gateway.py`, `ai_simulator.py` + 31 testes | — |
| 26/08/2026 | B1 — agentes + sanitizador de PII | 4 | `sanitizer.py`, `agents.py` + 31 testes | — |
| 26/08/2026 | Sincronização com o Drive | — | 4 módulos + painel; política de salvamento por bloco adotada | Pasta CAMPAIA |
| 27/08/2026 | Auditoria de código: confirmação do B3 e mapeamento de lacunas reais | 3/4 | `reconciliation.py` confirmado completo (26 testes) | `docs/evidence/GAP_ANALISE_BACKEND_CAMPAIA_20260827.md` |
| 27/08/2026 | Decisão do Diretor: painel corrigido + BFF/API priorizado | — | Diretor: "sim siga sua sugestão eu aprovo" | `docs/evidence/GAP_ANALISE_BACKEND_CAMPAIA_20260827.md` |
| 27/08/2026 | Camada BFF/API construída e testada (Starlette) | 1 → 3 | 19 arquivos novos; 42 testes próprios; 145 testes de domínio confirmados inalterados | `docs/evidence/EVIDENCIA_BFF_API_20260827.md` |
| 27/08/2026 | Fechamento da P-12: testes de `ai_gateway`, `ai_simulator`, `sanitizer`, `agents` | 4 | 4 arquivos de teste novos; 88 testes; 233 testes de domínio no total | `docs/evidence/EVIDENCIA_P12_20260827.md` |
| 28/08/2026 | Decisão do Diretor: conferir API contra contrato literal | — | Diretor: "faça assim segundo a sua recomendação B" | `docs/evidence/EVIDENCIA_P14_20260828.md` |
| 28/08/2026 | Conferência campo a campo `api/` vs. YAML literal (P-14) | 3 | 15 divergências confirmadas por leitura direta do código e do YAML decodificado | `docs/evidence/EVIDENCIA_P14_20260828.md` |
| 28/08/2026 | Decisão do Diretor: corrigir tudo | — | Diretor: "corrigir tudo" | `docs/evidence/EVIDENCIA_P14_20260828.md` |
| 28/08/2026 | Correção das 15 divergências + 2 encontradas na reverificação independente | 3 | 64 testes de API (era 42); 233 testes de domínio confirmados inalterados | `docs/evidence/EVIDENCIA_P14_20260828.md` |
| 28/08/2026 | Decisão do Diretor: corrigir a P-15 antes de avançar, depois seguir para persistência e blocos pendentes | — | Diretor: "fazer essa correção primeiro antes de avançar e não deixar nada para tras, e depois na sequencia as opções 1 e a 2 pode executar dessa forma" | `docs/evidence/EVIDENCIA_P15_20260828.md` |
| 28/08/2026 | Fechamento da P-15: correção da documentação do Connector Hub | 1 | Achado real identificado (tempo presente na frase de abertura) e corrigido; nenhuma alteração de código | `docs/evidence/EVIDENCIA_P15_20260828.md` |
| 28/08/2026 | Início da opção 1 do Diretor (persistência real) via agente delegado; reverificação independente do resultado | 2/3 | Reverificação direta revelou que a base restaurada era pré-P-14 | — |
| 28/08/2026 | Achado P-16: código da P-14 nunca havia sido salvo no Drive | — | Prova forense por metadados de data do Drive; comunicado ao Diretor de forma transparente e imediata | `docs/evidence/EVIDENCIA_REMEDIACAO_P14_20260828.md` |
| 28/08/2026 | Remediação da P-16: reenvio dos 11+3 arquivos genuinamente corrigidos ao Drive | 3 | Todos byte a byte verificados; contagem de domínio corrigida de 233 para 237 | `docs/evidence/EVIDENCIA_REMEDIACAO_P14_20260828.md` |
| 28/08/2026 | Persistência real (opção 1) — segunda tentativa, sobre a base local já corrigida | 2/3 | Novo `api/db.py` integrado; nenhum arquivo de rota alterado; 8 novos testes; verificação independente completa | `docs/evidence/EVIDENCIA_PERSISTENCIA_20260828.md` |
| 28/08/2026 | Upload da persistência ao Drive, imediatamente após a verificação | 3 | `main.py`, `state.py`, `repositories.py` substituídos + `db.py`/`test_persistence.py` novos; baseline 237+72=309 testes | `docs/evidence/EVIDENCIA_PERSISTENCIA_20260828.md` |
| 28/08/2026 | Instrução de processo do Diretor: sempre apresentar recomendação marcada entre as opções | Diretor: "vc é o eng senior sempre de as opções com a marcação de recomendo na melhor entre as opções" | — |
| 28-29/08/2026 | Recomendação de engenharia: priorizar B9 (AsyncAPI) entre B5/B7/B8/B9/B10; pesquisa dos artefatos de contrato existentes | — | Download e decodificação de `event-envelope.schema.json`, `campaign-brief.schema.json`, `campaign-plan.schema.json`, `ai-gateway.schema.json`, `connector-hub-e-eventos.md` | `contracts_ref/` (local) |
| 29/08/2026 | Construção do AsyncAPI 3.0 (`contracts/events.asyncapi.yaml`); descoberta e correção da contagem 22→24 (P-17) durante extração programática do catálogo | 1/3 | 24 canais/operações/mensagens; envelope reproduzido campo a campo; tentativas de validação via AsyncAPI CLI e meta-schema oficial bloqueadas por política de rede (P-18) | `docs/evidence/EVIDENCIA_B9_ASYNCAPI_20260829.md` |
| 29/08/2026 | Verificação independente do B9 via script próprio; upload ao Drive; reconciliação de uma discrepância real de bytes (3 bytes) entre Drive e disco, investigada com `diff` antes de fechar | 3 | `events.asyncapi.yaml` (25480 bytes) e `validate_events_asyncapi.py` (7287 bytes) confirmados byte a byte; script reexecutado do zero após correção, `ALL CHECKS PASSED` | `docs/evidence/EVIDENCIA_B9_ASYNCAPI_20260829.md` |
| 03/09/2026 | Decisão do Diretor: aprovar B10 como próximo bloco | — | Diretor: "b10" | — |
| 03/09/2026 | Pesquisa da taxonomia real de erros: leitura direta de `campaia_core/errors.py`, `api/errors.py`, `campaia_core/connectors.py`, `campaia_core/permissions.py` no Drive | 3 | Confirmação de 18 códigos ativos, 8 erros de domínio, 3 `DenialCode`; descoberta do gap de 3 códigos de `ConnectorErrorCode` (P-19), com `VALIDATION_REJECTED` confirmado via `grep` como levantado de fato em `saga.py:191` | `docs/evidence/EVIDENCIA_B10_CATALOGO_ERROS_20260903.md` |
| 03/09/2026 | Construção e verificação independente de `docs/09_CATALOGO_ERROS_USUARIO.md` | 3 | 18 mensagens de erro em português; script Python confirmou 18/18 de cobertura exata e o gap de 3 códigos, sem diferença em relação ao documento | `docs/evidence/EVIDENCIA_B10_CATALOGO_ERROS_20260903.md` |
| 03/09/2026 | Upload ao Drive; detecção e correção de um erro real de transcrição (3 bytes a mais na 1ª tentativa, causado por um erro de digitação meu no parâmetro de upload, não no arquivo local) via verificação byte a byte | 3 | 1ª tentativa (`fileId 1w1O...`) trashada; upload final via `base64Content` bateu exatamente 12603=12603 | `docs/evidence/EVIDENCIA_B10_CATALOGO_ERROS_20260903.md` |
| 04/09/2026 | Decisão do Diretor: corrigir a P-19, sem deixar nenhuma inconsistência entre código e documentação | — | Diretor: "pode corrigir não vamos deixar nenhum erro para trás" | `docs/evidence/EVIDENCIA_P19_FIX_20260904.md` |
| 04/09/2026 | Restauração local dos 48 arquivos do backend a partir do Drive via agente delegado; reverificação independente do relatório do agente | 2/3 | Agente inicialmente interrompido por rate limit relatou de forma confusa; cobrado por relatório preciso, revelou que 10 de 48 arquivos ainda estavam byte-incorretos apesar de "prontos" reportado antes; corrigidos via decodificação programática | `docs/evidence/EVIDENCIA_P19_FIX_20260904.md` |
| 04/09/2026 | Confirmação independente da linha de base (execução direta, não apenas relato do agente) | — | 237 (domínio) + 72 (API/persistência) = 309 testes, 100% aprovados, antes de qualquer alteração | `docs/evidence/EVIDENCIA_P19_FIX_20260904.md` |
| 04/09/2026 | Correção da P-19 em `api/errors.py` (3 entradas em `STATUS_BY_CODE`) + 8 novos testes em `tests_api/test_errors_p19.py` provando a correção via `from_domain_error()` | 3 | 237+80=317 testes, 100% aprovados, zero regressão; anomalia de 1 byte em `routes_approvals.py` investigada e documentada como externa, não corrigida (fora do escopo) | `docs/evidence/EVIDENCIA_P19_FIX_20260904.md` |
| 04/09/2026 | Atualização do catálogo B10 (`docs/09_CATALOGO_ERROS_USUARIO.md`) para v1.1.0, refletindo a correção da P-19 em vez de descrevê-la como pendente | 3 | 12603→14668 bytes | `docs/evidence/EVIDENCIA_P19_FIX_20260904.md` |
| 04/09/2026 | Upload dos 3 artefatos ao Drive (`api/errors.py`, `tests_api/test_errors_p19.py`, catálogo v1.1.0), todos byte-verificados na primeira tentativa | 3 | 4509=4509, 3718=3718, 14668=14668 | `docs/evidence/EVIDENCIA_P19_FIX_20260904.md` |
| 04/09/2026 | Decisão do Diretor: construir B5, seguindo a recomendação apresentada ao final do fechamento da P-19 | — | Diretor: "siga sua recomendação" | `docs/evidence/EVIDENCIA_B5_OTIMIZACAO_PACING_20260904.md` |
| 04/09/2026 | Pesquisa de escopo antes de escrever código: descoberta de uma árvore de documentação de produto até então não consultada (`campaia/docs/product/`), revelando a distinção F8.1 (sugerir) vs. F8.2 (executar automaticamente, fora de escopo do MVP) já registrada na Fase 0 | — | `FUNCTIONAL_REQUIREMENTS.md`, `CAMPAIA_PRODUCT_CHARTER.md`, `OUT_OF_SCOPE.md` | `docs/evidence/EVIDENCIA_B5_OTIMIZACAO_PACING_20260904.md` |
| 04/09/2026 | Pergunta explícita ao Diretor sobre o escopo do B5, em vez de decisão unilateral, dado que um motor autoexecutável contrariaria uma decisão de escopo já registrada | — | Diretor: "Motor que só sugere/calcula (Recomendado)" | `docs/evidence/EVIDENCIA_B5_OTIMIZACAO_PACING_20260904.md` |
| 04/09/2026 | Construção de `pacing.py` e `optimizer.py`, após confirmar (leitura completa de `autonomy.py`, `policy.py`, `states.py`, `budget.py`) que não havia sobreposição com código existente | 3 | 2 módulos novos (7854 + 6927 bytes); 26 novos testes (14 pacing + 12 optimizer), incluindo prova real via `evaluate_autonomy()` de que nenhuma sugestão se autoexecuta | `docs/evidence/EVIDENCIA_B5_OTIMIZACAO_PACING_20260904.md` |
| 04/09/2026 | Re-execução completa da suíte (não apenas confiar no relato da sessão anterior) | — | 263 (domínio) + 80 (API/persistência) = 343 testes, 100% aprovados, zero regressão | `docs/evidence/EVIDENCIA_B5_OTIMIZACAO_PACING_20260904.md` |
| 04/09/2026 | Upload dos 4 artefatos do B5 ao Drive, todos byte-verificados | 3 | `pacing.py` 7854=7854, `optimizer.py` 6927=6927, `test_pacing.py` 5561=5561, `test_optimizer.py` 7888=7888 | `docs/evidence/EVIDENCIA_B5_OTIMIZACAO_PACING_20260904.md` |
| 04/09/2026 | Achado de processo, comunicado com transparência: 1ª tentativa de upload do documento de evidência do B5 divergiu em 1 byte (13344 local vs. 13343 no Drive); investigação por download/diff programático revelou uma corrupção real de conteúdo — um "à" (acento grave, crase) virou "a" — causada por um erro de transcrição meu ao reproduzir a string base64 na chamada da ferramenta, não um comportamento do Drive | — | Readback e diff byte a byte via Python confirmaram a causa exata antes de qualquer correção | `docs/evidence/EVIDENCIA_B5_OTIMIZACAO_PACING_20260904.md` |
| 04/09/2026 | Remediação: arquivo local corrigido, nova codificação, upload da versão corrigida verificado por download/diff completo (não apenas contagem de bytes) batendo exatamente; versão com erro trashada no Drive | 3 | Versão final 13349=13349, conteúdo idêntico byte a byte ao original local confirmado por diff | `docs/evidence/EVIDENCIA_B5_OTIMIZACAO_PACING_20260904.md` |
| 04/09/2026 | Decisão do Diretor: construir B7 (DDL e migrations do Postgres), aceitando a ressalva de verificação limitada dada ao final do fechamento do B5 | — | Diretor: "sim concordo" | `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` |
| 04/09/2026 | Pesquisa de escopo delegada antes de escrever qualquer DDL: leitura completa de `campaia_core/`, `api/db.py`, `api/repositories.py`; busca por artefatos Postgres já existentes (nenhum encontrado); leitura de `campaia/docs/product/` e `TENANT_ISOLATION.md` | — | Achado: `docs/06_MODELO_DADOS.md` nunca foi de fato escrito; maioria dos módulos de domínio nunca foi persistida; saídas do B5 são cálculos puros, não geram tabela; D-03 segue condicionando o formato de multi-tenancy | `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` |
| 04/09/2026 | Duas perguntas explícitas ao Diretor sobre o escopo do B7 (DDL completo vs. só do persistido; pausar por D-03 vs. construir extensível), em vez de decisão unilateral, dada a ambiguidade real de escopo encontrada na pesquisa | — | Diretor: "DDL apenas do que já é persistido (Recomendado)" + "Construir para o caso simples, deixando extensível (Recomendado)" | `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` |
| 04/09/2026 | Construção de `db/001_initial_schema.sql` (7 tabelas, RLS, CHECK, FK, chave composta) e `db/verify_ddl_postgres.sh` | 3 | 2 arquivos novos; nenhuma alteração em `campaia_core/` ou `api/` | `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` |
| 04/09/2026 | Verificação de disponibilidade de Postgres real nesta sandbox por investigação direta (`which pg_ctlcluster`, `pg_lsclusters`), em vez de aceitar como definitiva a própria ressalva dada ao Diretor antes de começar o bloco | — | Descoberta: Postgres 16 genuinamente instalado (`postgresql-16`, cluster `main`, porta 5432); iniciado via `service postgresql start`; achado comunicado ao Diretor com a mesma transparência de qualquer outro, como correção de uma ressalva própria, não como problema | `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` |
| 04/09/2026 | Execução real do script de verificação contra o Postgres real: schema aplica sem erro, RLS isola tenant-a/tenant-b e nega acesso sem contexto (falha fechado), CHECK e FK rejeitam valores inválidos, chave composta de idempotency rejeita duplicata | 3 | `ALL CHECKS PASSED`; bug próprio encontrado e corrigido antes de aceitar o resultado (`psql -tAc` com múltiplas instruções retornava `"SET\n1"` em vez de `"1"`, corrigido com `| tail -n1`); script reexecutado 2x seguidas para reprodutibilidade, zero rastro deixado no servidor | `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` |
| 04/09/2026 | Re-execução completa da suíte Python (B7 é puramente SQL/shell, sem alteração de código Python) | — | 263 (domínio) + 80 (API/persistência) = 343 testes, 100% aprovados, zero regressão | `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` |
| 04/09/2026 | Correção de uma ressalva desatualizada no próprio comentário de cabeçalho de `001_initial_schema.sql` ("não há Postgres real disponível aqui", escrita antes da descoberta) antes do upload; reverificação completa (2 execuções adicionais do script) após a edição puramente textual, para não aceitar "é só um comentário" como dispensa de reverificação | 3 | Tamanho do arquivo mudou de 15402 para 15577 bytes; `ALL CHECKS PASSED` confirmado novamente | `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` |
| 04/09/2026 | Criação da pasta `backend/db/` no Drive (não existia; irmã de `campaia_core/`, `api/`, `tests/`, `tests_api/`) e upload dos 2 artefatos de código + do documento de evidência, todos via `textContent` (não `base64Content`), cada um verificado por download completo + diff byte a byte (não apenas tamanho) | 3 | `001_initial_schema.sql` 15577=15577, `verify_ddl_postgres.sh` 5961=5961, evidência final 14494=14494 — todos com diff de conteúdo confirmando match exato | `docs/evidence/EVIDENCIA_B7_DDL_POSTGRES_20260904.md` |
| 05/09/2026 | Decisão do Diretor: construir C1 (repositório Git + CI) e, em sequência, resolver as decisões então listadas como bloqueio | Diretor: "faça a opção 1 e depois na sequencia a opção 2 para não ficar nada pendente para trás" | — |
| 05/09/2026 | Reconstrução do repositório completo a partir do Drive: `backend/` reconciliado arquivo a arquivo (achado real: `api/routes_approvals.py` tinha indentação incorreta em `create_approval` — 3 espaços em vez de 4 no `state.audit.append(...)`, mascarada porque Python não exige indentação dentro de parênteses; corrigida a partir da cópia do Drive), `contracts/` (8 arquivos) e `docs/` + `docs/evidence/` (38 arquivos) baixados e verificados byte a byte | 2 → 3 | 343/343 testes, zero regressão, após a correção; achado anterior (P-19, "anomalia de 1 byte... root-caused como externa") corrigido como o defeito real que era, não mais atribuído ao Drive | Commit inicial do repositório `campaia_repo/` |
| 05/09/2026 | Descoberta, durante a leitura completa de `docs/evidence/` para a reconstrução: 6 documentos de decisão do Diretor datados de 27/08/2026 (D-03, D-05, D-06, ADR-0010, marco do Google Ads, atualização do CNPJ) já resolviam a maior parte do que este painel listava como "Bloqueios" desde então, sem que o painel jamais tivesse sido corrigido para refletir isso (P-21) | — | Achado comunicado ao Diretor com a mesma transparência de qualquer outro | `docs/evidence/DIRETOR_DECISOES_D03_D05_D06_D09_20260827.md` e demais |
| 05/09/2026 | Construção de `.gitignore`, `backend/requirements.txt`, `contracts/requirements.txt` e do workflow `.github/workflows/backend-tests.yml` (2 suítes de teste + validador AsyncAPI) | 2 | — | — |
| 05/09/2026 | Verificação do workflow de CI antes de aceitá-lo como correto: achado real de portabilidade em `contracts/validate_events_asyncapi.py` (caminhos absolutos hardcoded para `/home/claude/...`, que não existiriam num checkout limpo do GitHub Actions nem no PC do Diretor); corrigido para caminhos relativos ao próprio arquivo (`Path(__file__).resolve().parent`); revalidado (24/24 eventos, exit code 0) | 2 → 3 | Simulação do CI em venv limpa confirmou que o único obstáculo restante era o bloqueio de rede a `pypi.org` deste sandbox de teste (não um problema do repositório); versões fixadas de `pydantic==2.13.3` e `starlette==1.0.0` confirmadas como reais e publicadas via busca na web antes de aceitar o `requirements.txt` como correto | — |
| 05/09/2026 | `git init`, commit inicial (106 arquivos, mensagem detalhando os 2 defeitos corrigidos), `git bundle create --all` e `zip` do working tree; ambos verificados de fato (`git bundle verify`, clone de teste do bundle rodando as 343 verificações de teste de novo, `unzip -t` sem erros) antes de aceitar como corretos | 3 | `git fsck --full` sem erros; clone a partir do bundle reproduz exatamente os 106 arquivos rastreados e passa 343/343 | Commit `21faaab` |
| 05/09/2026 | Entrega ao PC do Diretor via ponte de dispositivo: `campaia_repo.zip` e `campaia.bundle` enviados e gravados em `Desktop/CAMPAIA/` (pasta nova — não confundir com `Desktop/F M TECNOLOGIA`, que é de outro projeto do Diretor, o app de comida) | 3 | Tamanhos conferidos após a gravação: 327399 e 312722 bytes, idênticos aos originais | `Desktop/CAMPAIA/campaia_repo.zip`, `Desktop/CAMPAIA/campaia.bundle` |
| 05/09/2026 | Reconciliação do painel (opção 2): D-03, D-05, D-06, D-08 e D-09 marcadas como resolvidas (com data e citação literal do Diretor de 27/08), P-11 e P-21 adicionadas às pendências técnicas, B8 marcada como desbloqueada | 1 | v16 substitui v15 no Drive | `01_PAINEL_EXECUCAO_v16_VIGENTE.md` |
| 05/09/2026 | Diretor cria o repositório GitHub e autentica `gh`/`git` na própria máquina (login por navegador, device code flow) — nenhuma credencial passou por mim; push do repositório completo confirmado com sucesso | — | `git push` retornou "Everything up-to-date" / branch main rastreando origin/main | Repositório `https://github.com/faabio3131/CampaIA` |
| 05/09/2026 | Diretor reporta erro real no CI ("erro de CI dependência e revalidação"); diagnóstico feito via `gh run view --log-failed` direto no terminal do Diretor (já autenticado), não por navegação visual no site | — | Log real do Actions aponta `ModuleNotFoundError: No module named 'httpx'` — dependência de `starlette.testclient` ausente de `backend/requirements.txt`, mascarada localmente porque a sandbox já tinha `httpx` instalado por outro motivo | `docs/evidence/EVIDENCIA_C1_CI_HTTPX_FIX_20260905.md` |
| 05/09/2026 | Confirmação da versão real do `httpx` no PyPI antes de fixar (0.28.1, mesma disciplina já aplicada a `pydantic`/`starlette`); correção de `backend/requirements.txt`; 343/343 testes re-executados localmente, zero regressão; commit e push direto do terminal do Diretor | 3 | `git push` bem-sucedido (`ab4173a..3f2c18d main -> main`) | `docs/evidence/EVIDENCIA_C1_CI_HTTPX_FIX_20260905.md` |
| 05/09/2026 | Confirmação de que a execução seguinte do CI no GitHub passou de fato, não apenas presumida | 3 | `gh run view --json status,conclusion` → `{"conclusion": "success", "status": "completed"}` | `docs/evidence/EVIDENCIA_C1_CI_HTTPX_FIX_20260905.md` |
| 05/09/2026 | Upload da correção ao Drive (trash do `requirements.txt` antigo + novo), verificado byte a byte (download + diff + SHA256) | 3 | SHA256 idêntico entre local e Drive | `backend/requirements.txt` (Drive) |
| 05/09/2026 | Diretor pede para seguir com B8; pesquisa real de mercado antes de fechar a especificação, a pedido explícito do Diretor ("Você como engenheiro Sênior?... Como que são os atuais concorrentes?") | — | Diretor: "importante agora no momento é a funcionalidade. Depois nós vamos dar aquele talento num visual premium final. Depois que tiver tudo pronto testado." | `docs/product/13_ESPECIFICACAO_TELAS_APP.md` |
| 05/09/2026 | Pesquisa de concorrentes reais via WebSearch/WebFetch (Madgicx, Revealbot, Smartly.io, AdCreative.ai, Meta Advantage+) e incorporação de 2 melhorias concretas na especificação (geração multi-formato de criativo já na etapa de criação; breakdown de métricas por variação individual) mais 1 insight de posicionamento (aprovação humana obrigatória como diferencial) | — | Fontes citadas no documento (Pipeboard, Get Ryze, Zeely, 1ClickReport) | `docs/product/13_ESPECIFICACAO_TELAS_APP.md` §0 |
| 05/09/2026 | Diretor introduz mudança de escopo: oferecer Modo Manual E Modo Automático (com limite definido pelo cliente) como diferencial competitivo | — | Diretor: "Sobre nós deixarmos tudo com, autorização humana... Mas também vamos ter a opção do cliente querer escolher deixar no automático... assim nos coloca à frente, está concorrente." | `docs/product/DECISOES_DIRETOR.md` item 5 |
| 05/09/2026 | Pergunta explícita ao Diretor antes de decidir unilateralmente, dado que isso reabre uma restrição já registrada em `OUT_OF_SCOPE.md` (Nível 2/3 de autonomia fora do MVP) | — | Diretor confirma trazer o Nível 2 para o MVP, com a salvaguarda de senha de administrador: "vamos colocar isso agora em funcionamento porém pensando na segurança... precisará ser validada por senha de adm... provaremos que ele inseriu a senha" | `docs/product/DECISOES_DIRETOR.md` item 5 |
| 05/09/2026 | Propagação da decisão de forma consistente por todos os documentos afetados, corrigindo contradições que uma edição isolada teria deixado (`CAMPAIA_PRODUCT_CHARTER.md` §9, `OUT_OF_SCOPE.md`, `13_ESPECIFICACAO_TELAS_APP.md` §1/§8/§9/§11) | 1/3 | Busca sistemática por "F8.2", "execução automática", "Fora de escopo" no documento B8 para não deixar nenhuma menção contraditória | `docs/product/CAMPAIA_PRODUCT_CHARTER.md`, `docs/product/OUT_OF_SCOPE.md`, `docs/product/13_ESPECIFICACAO_TELAS_APP.md` |
| 05/09/2026 | Pergunta de fechamento sobre a única ambiguidade restante: campanha nova também seria coberta pelo Modo Automático (senha + limite), ou continuaria sempre exigindo aprovação manual distinta? | — | Diretor escolhe "Campanha nova sempre manual (Recomendado)" — a senha + limite autorizam apenas otimização de campanhas já aprovadas, nunca a criação/publicação inicial | `docs/product/DECISOES_DIRETOR.md` item 5, seção "Esclarecimento adicional" |
| 05/09/2026 | Descoberta e correção de uma lacuna de processo: os 5 documentos de produto (`CAMPAIA_PRODUCT_CHARTER.md`, `FUNCTIONAL_REQUIREMENTS.md`, `OUT_OF_SCOPE.md`, `NON_FUNCTIONAL_REQUIREMENTS.md`, `DECISOES_DIRETOR.md`) existiam apenas no sandbox, nunca haviam sido persistidos no Google Drive | — | Achado comunicado com transparência; documentos criados na pasta `docs/product/` do Drive (fileId `1ydfjOmemNxYjEwLa_kQdaT1yLUfLcLnf`), com verificação byte a byte (SHA256) e correção de uma falha de corrupção de acentuação Unicode via `base64Content` (resolvida trocando para `textContent`) | Pasta `docs/product/` no Drive |
| 05/09/2026 | Upload das versões finais de `13_ESPECIFICACAO_TELAS_APP.md` e `DECISOES_DIRETOR.md` ao Drive (trash-old+create-new), verificados byte a byte (download completo + diff + SHA256, não apenas tamanho) | 3 | SHA256 idêntico entre local e Drive em ambos os arquivos | Pasta `docs/product/` no Drive |
| 05/09/2026 | Commit dos artefatos de produto no repositório Git local (`campaia_repo/`) | 3 | `docs: especificações de telas B8 e decisões do diretor` — 6 arquivos, 1707 inserções | Commit `59b8c0b` |
| 05/09/2026 | Inspeção das rotas HTTP reais (`backend/api/routes_*.py`, `main.py`) contra o contrato literal (`contracts/bff-openapi.yaml`), a pedido do Diretor, sem alterar nenhum arquivo nesta etapa | — | Nenhuma rota duplicada; cobertura 1:1 entre implementação e contrato, exceto `POST /approvals` (desvio já documentado em comentário no próprio código) | — |
| 05/09/2026 | Achado real (não documentado como "achado" no código): `BudgetPatchRequest` (`api/models.py`) declarava o campo como `new_daily_cap`, mas o contrato exige `daily_cap` — com `extra="forbid"`, um cliente seguindo o contrato literal seria rejeitado | — | Confirmado por leitura direta do YAML (`required: [daily_cap, approval_id]`) e do model; nenhum alias existente reconciliava os dois nomes | `contracts/bff-openapi.yaml`, `backend/api/models.py` |
| 05/09/2026 | Correção autorizada pelo Diretor: alias `daily_cap`→`new_daily_cap` com `populate_by_name=True`, documentado como achado 18 seguindo o padrão de comentários já usado no arquivo | 3 | 80/80 testes de API confirmados sem regressão (`unittest discover -s tests_api`); verificação manual adicional confirmando que ambos os nomes (`daily_cap` e `new_daily_cap`) validam e que `extra="forbid"` continua rejeitando campos desconhecidos | Commit `8eee393` — "fix(api): suporte a daily_cap no BudgetPatchRequest via alias" |
| 05/09/2026 | Criação de `.claude/commands/system-design.md` e `.claude/commands/task-manager.md` (skills customizadas de arquitetura e de execução anti-alucinação), com conteúdo exato fornecido pelo Diretor | — | Ambos os arquivos criados sem alteração de conteúdo em relação ao especificado | Commit `50fcdf0` — "chore(claude): adiciona skills customizadas de system design e task manager" |
| 05/09/2026 | Commits separados confirmados: `docs/product/` (`59b8c0b`) e `.claude/` (`50fcdf0`); árvore de trabalho conferida limpa via `git status` | 3 | `git status` → "nothing to commit, working tree clean" | Commits `59b8c0b`, `50fcdf0` |
| 19/09/2026 | Auditoria completa e somente leitura dos dois repositórios (`faabio3131/CampaIA` e `faabio3131/f-m-tecnologia-campaia`), comparando ancestralidade, commits, árvore de arquivos e diff byte a byte de todos os caminhos compartilhados | — | Ancestral comum confirmado (`21faaab`, `ab4173a`, SHAs idênticos); 106/107 arquivos compartilhados byte-idênticos; único arquivo divergente: `backend/api/models.py` (alias `daily_cap`) | Auditoria "CAMPAIA — CURRENT, Reconciliação e Definição do Ponto Zero Web", mesma sessão |
| 19/09/2026 | Decisão do Diretor: repositório canônico é `faabio3131/f-m-tecnologia-campaia`; `faabio3131/CampaIA` vira fonte histórica somente leitura | Diretor, prompt "RECONCILIAÇÃO CONTROLADA DO REPOSITÓRIO CANÔNICO" | — | ADR-0015 |
| 19/09/2026 | Pré-flight: HEADs de ambos os repositórios reconfirmados idênticos aos esperados antes de qualquer alteração (`f-m-tecnologia-campaia` @ `1fe8051`, `CampaIA` @ `fba7fcc`); worktrees limpas; ausência do bloco fiscal e presença do fix `daily_cap`/governança/`docs/product/` no canônico reconfirmadas | — | `git rev-parse HEAD` / `origin/main` em ambos os repositórios | — |
| 19/09/2026 | Criação da branch `reconciliation/campaia-ponto-zero-web` a partir da `main` canônica atualizada | — | — | — |
| 19/09/2026 | SHAs completos resolvidos e diffs completos inspecionados dos 3 commits do bloco fiscal antes de importar (`bb2b84d`, `55f5b95`, `bdebbc3`) — cada um altera exatamente 1 arquivo novo, sem escopo estranho; merge `fba7fcc` não introduz nada além disso | — | `git show --stat` de cada commit; `git diff bdebbc3 fba7fcc --stat` vazio | — |
| 19/09/2026 | Leitura integral de `fiscal_handoff.py` e confirmação de que nenhuma referência a ele existe em outro lugar do backend, antes de importar — confirma a natureza fail-closed e o desacoplamento de campanha/orçamento já declarados na PR de origem | — | `grep -rn "fiscal_handoff" backend/` sem resultados fora dos 2 arquivos próprios | — |
| 19/09/2026 | Importação do bloco fiscal por cherry-pick (não cópia manual), preservando autoria e histórico original | 3 | Cherry-pick limpo, sem conflitos; commits `e7f648d`, `05aab64`, `d940949` no repositório canônico, autoria `faabio3131` preservada | — |
| 19/09/2026 | Confirmação do fix `daily_cap` já presente e coerente no canônico (modelo, contrato, API); busca por lacuna de cobertura de teste antes de decidir se adicionava algo | — | Achado real: nenhum teste existente enviava o nome de campo literal do contrato (`daily_cap`) ao endpoint de patch de orçamento — toda a cobertura usava `new_daily_cap` | `backend/api/models.py`, `backend/tests_api/*.py` |
| 19/09/2026 | Adição do teste de regressão `test_budget_patch_accepts_contract_daily_cap_alias`, cobrindo exatamente a lacuna encontrada, via cliente HTTP real (não apenas unitário de modelo) | 3 | Teste executado isoladamente: OK | Commit `3a0f277` — "test(api): cover contract literal daily_cap alias on budget patch" |
| 19/09/2026 | Registro formal de quarentena arquitetural do app mobile — SDK Flutter/Dart confirmado ausente também neste ambiente (`which flutter dart`); SDK não instalado para forçar uma validação que esta reconciliação não pediu; registrado como NÃO VERIFICADO | — | `which flutter dart` → ambos ausentes | Commit `80b2f1e`; `docs/evidence/MOBILE_QUARENTENA_ARQUITETURAL_20260919.md` |
| 19/09/2026 | Redação da ADR-0015, seguindo o padrão documental descoberto (arquivo próprio, como ADR-0013 — não reaproveitando o lote fechado de `docs/03_ADR_INICIAIS.md`); numeração verificada contra colisão antes de escrever | — | `ADR-0014` já reservada no painel v18 (linhas 55/210/295) para o tema de Autorização RBAC/ABAC, ainda não escrita; `ADR-0015` confirmada livre | Commit `577b51c`; correção de numeração no commit `4d99150` |
| 19/09/2026 | Redação desta versão do painel (v19), a partir de v18, confirmando que v19 é de fato a próxima versão antes de criá-la (nenhum `v19` pré-existente encontrado em nenhum lugar do repositório) | — | `grep -rl "v19"` vazio antes da criação deste arquivo | `01_PAINEL_EXECUCAO_v19_VIGENTE.md` |
| 19/09/2026 | Correção mínima solicitada pelo Diretor na PR de reconciliação: `ADR-0014`→`ADR-0015` no doc de quarentena do mobile (única referência incorreta entre 5 ocorrências de `ADR-0014` nos 7 arquivos da PR, as outras 4 legítimas por serem sobre RBAC/ABAC); remoção de linguagem de "autorizado" prematura sobre o System Design Web em 2 pontos do painel v19 | — | Diff revisado; CI verde no novo HEAD | Commit `45226cf` |
| 19/09/2026 | Pré-flight da missão de Ponto Zero Web: HEADs de `main` reconfirmados (`c121f7c`, incluindo PR#3 mergeada com correção de linguagem `7ec1f6b` feita fora desta sessão, inspecionada e confirmada benigna antes de prosseguir); PRs confirmadas mergeadas; CI do run #10 confirmado verde; branch `architecture/campaia-ponto-zero-web` criada a partir da `main` confirmada | — | `git log 1fe8051..c121f7c`; `mcp__github__list_pull_requests`/`actions_list` | — |
| 19/09/2026 | CURRENT reconstruído por leitura direta de código-fonte (não por citação de painel): `states.py`, `autonomy.py`, `policy.py`, `budget.py`, `saga.py`, `permissions.py`, `connectors.py`, início de `ai_gateway.py`, `api/main.py`, DDL, Product Charter, NFR | — | Leitura integral dos arquivos citados | `docs/web/01_CURRENT_E_MATRIZ_CURRENT_TARGET.md` |
| 19/09/2026 | Divergência encontrada e registrada, não corrigida silenciosamente: Product Charter declara "Natureza: Aplicativo mobile SaaS independente", anterior à Lei Web First | — | Leitura de `CAMPAIA_PRODUCT_CHARTER.md` §1 | ADR-0016 |
| 19/09/2026 | Produção de `docs/web/` (8 documentos): matriz CURRENT×TARGET, requisitos, arquitetura (bounded contexts, 7 diagramas Mermaid, fluxos críticos, modelo de dados, autenticação/tenancy), IA/integrações/segurança/resiliência (matriz STRIDE), testes/CI-CD/migração, roadmap com 5 Work Packages detalhados, certificação | — | Arquivos criados e revisados | Commits `d8a733c`, `b35833e`, `fe7cec6`, `a4a36ec`, `9a5935e` |
| 19/09/2026 | Numeração de ADR verificada antes de escrever (0014/0015 já ocupadas, confirmado por `grep`); 4 ADRs propostas (0016 Web First vs. Charter, 0017 framework de frontend, 0018 autenticação/sessão, 0019 implantação TARGET sobre D-08 já aprovada), todas deixadas em `PROPOSTA` | — | `grep -rohE "ADR-[0-9]{4}"` antes de numerar | Commit `a4a36ec` |
| 19/09/2026 | Matriz de validação real reexecutada no HEAD final desta missão (não reaproveitando números de sessões anteriores): 267 testes de domínio, 81 de API, AsyncAPI 24/24, DDL/RLS contra Postgres 16 real (reiniciado nesta sessão) | 3 | Todas as 4 execuções com saída capturada nesta sessão | `docs/web/07_CERTIFICACAO_PONTO_ZERO_WEB.md` |
| 19/09/2026 | Verificações de escopo antes de certificar: nenhuma ADR nova marcada `APROVADA`; nenhuma declaração de produção/homologação; `mobile/` confirmado intocado (`git diff --stat -- mobile/` vazio); diff da branch inteiramente dentro de `docs/`; secrets varridos | — | `grep`/`git diff` conforme documentado na certificação | `docs/web/07_CERTIFICACAO_PONTO_ZERO_WEB.md` |
| 19/09/2026 | Confirmação de que v20 é de fato a próxima versão do painel antes de criá-la | — | `grep -rl "v20"` vazio antes da criação deste arquivo | `01_PAINEL_EXECUCAO_v20_VIGENTE.md` |
| 19/09/2026 | CI do GitHub Actions confirmado `completed`/`success` no HEAD `30903fa` (run #13, correção documental de contradição D-08/cronologia/WP-02/painel/descrição da PR) | 3 | `mcp__github__actions_list` | Run `35467414236` |
| 19/09/2026 | Aprovação arquitetural por autorização humana atual e explícita do Diretor Fábio Aluizio da Silva ("PROMPT MESTRE — APROVAÇÃO ARQUITETURAL E MERGE CONTROLADO"): ADR-0016, ADR-0017, ADR-0018 e ADR-0019 alteradas de `PROPOSTA` para `APROVADA`, preservando histórico, alternativas e riscos originais; pendências reais mantidas sem falsa resolução | — | Header/Decisão/Pendências de cada ADR revisados individualmente | `docs/14_ADR_0016...md`, `docs/15_ADR_0017...md`, `docs/16_ADR_0018...md`, `docs/17_ADR_0019...md` |
| 19/09/2026 | Reconciliação literal do Product Charter e da NFR com a Lei Web First, conforme autorizado: frase de natureza do Charter (mobile SaaS independente → Web SaaS com mobile em quarentena, referenciando ADR-0016 aprovada) e §8 da NFR (Mobile deixa de ser primário, Navegadores passam a "linha principal do produto"); marcos de capacidade "Fase 12" fora do §8 preservados por serem roadmap de negócio, não framing arquitetural | — | Grep dirigido nos dois arquivos antes de editar, distinguindo frases incompatíveis de menções legítimas | `docs/product/CAMPAIA_PRODUCT_CHARTER.md`, `docs/product/NON_FUNCTIONAL_REQUIREMENTS.md` |
| 19/09/2026 | Propagação documental da aprovação: `docs/web/00_INDICE_PONTO_ZERO_WEB.md` e `docs/web/07_CERTIFICACAO_PONTO_ZERO_WEB.md` atualizados para refletir ADRs `APROVADA`, sem declarar merge como certificado antes de ele ocorrer de fato (mesma disciplina de não-autorreferência já aplicada nesta missão) | — | Revisão de cada seção afetada | `docs/web/00_INDICE_PONTO_ZERO_WEB.md`, `docs/web/07_CERTIFICACAO_PONTO_ZERO_WEB.md` |
| 19/09/2026 | Merge controlado da PR #4 confirmado: método `merge` (merge commit, mesmo padrão das PRs #1–#3), vinculado ao HEAD certificado `8554f20`; CI pós-merge confirmado `completed`/`success` em `main`@`41b521f` | — | `mcp__github__merge_pull_request`, `mcp__github__actions_list` | Merge commit `41b521fed83b87d8cd0df3d1def2fd26f9af8379` |
| 19/09/2026 | Pré-flight do WP-01: confirmado `origin/main`@`41b521f` (esperado), PR #4 `MERGED`, CI pós-merge verde, branch `feat/campaia-wp01-web-foundation` inexistente local/remota, nenhuma PR aberta concorrente, worktree limpa antes de criar a branch | — | `git ls-remote`, `mcp__github__pull_request_read`, `mcp__github__actions_list`, `mcp__github__list_branches`, `mcp__github__list_pull_requests` | Branch criada com divergência zero de `main` |
| 19/09/2026 | Scaffold real do frontend Web: `npx create-next-app` (Next.js 16.3.5, React 19.2.8, TypeScript estrito, App Router, ESLint), removidos `CLAUDE.md`/`AGENTS.md` gerados pelo scaffold (evitar duplicar a governança canônica da raiz) e os SVGs de exemplo | — | `npm run build` bem-sucedido logo após o scaffold | Diretório `web/` criado |
| 19/09/2026 | Tipos do contrato gerados deterministicamente de `contracts/bff-openapi.yaml` via `openapi-typescript` (`web/src/contracts/bff-openapi.generated.ts`); fixture local fictícia tipada contra `Me` (`web/src/fixtures/me.local.ts`); scripts de verificação de drift e de fronteiras de segurança implementados com lógica compartilhada entre CLI e testes (evitando spawn de subprocesso dentro do test runner, que o sandbox bloqueava) | — | `npm run contracts:check`, `npm run boundary:check`, `tests/boundaries.test.ts` | `web/scripts/`, `web/scripts/lib/` |
| 19/09/2026 | Design tokens mínimos e 5 componentes (`Button`, `Card`, `Badge`, `PageContainer`, `StatusPanel`) construídos e usados de fato pela tela de fundação; violação real de acessibilidade encontrada (`landmark-unique` — dois `StatusPanel` com o mesmo `aria-labelledby`) e corrigida na origem (id de heading derivado do título, não suprimida) | — | `npm run test` (jest-axe), revisão de diff | `web/src/components/`, `web/src/styles/tokens.css` |
| 19/09/2026 | 15 testes Vitest (unitário/componente/acessibilidade/fronteiras) e 4 testes Playwright (smoke desktop+mobile, sem login) escritos e verdes; Chromium pré-instalado do ambiente usado via `CAMPAIA_CHROMIUM_PATH` (versão pinada de `@playwright/test` não coincidia com a revisão pré-instalada, resolvido apontando `executablePath` em vez de baixar um novo binário) | 3 | `npm run test`, `npm run test:e2e` | `web/tests/`, `web/e2e/` |
| 19/09/2026 | Reprodutibilidade real confirmada: `rm -rf node_modules .next && npm ci && npm run check` verde a partir de checkout limpo; achado real durante essa verificação — `tsc --noEmit` sozinho falhava (`Cannot find name 'LayoutProps'`) porque os tipos de rota do App Router só existem após `next typegen`/`next build`; corrigido adicionando `next typegen` como pré-requisito do script `typecheck`, não suprimindo o erro | 3 | `npm ci` + `npm run check` a partir de árvore limpa | `web/package.json` |
| 19/09/2026 | CI do frontend criado (`.github/workflows/frontend-tests.yml`): checkout, Node 22, `npm ci`, lint, typecheck, contract-drift, boundary-check, testes, build, instalação do Chromium do Playwright, E2E; `backend-tests.yml` confirmado inalterado (`git diff` vazio) | — | Revisão do arquivo, `git diff --stat .github/workflows/backend-tests.yml` | `.github/workflows/frontend-tests.yml` |
| 19/09/2026 | Regressão completa do backend/contratos reexecutada no HEAD desta execução, sem nenhuma alteração em `backend/campaia_core/`, `backend/api/`, `backend/db/` ou `contracts/`: 267 testes de domínio, 81 de API, AsyncAPI 24/24 — nenhuma regressão | 3 | `python3 -m unittest discover -s tests -v`, `-s tests_api -t .`, `python3 validate_events_asyncapi.py` | `docs/web/08_CERTIFICACAO_WP01_FUNDACAO_WEB.md` |
| 19/09/2026 | Secrets varridos em todo `web/` (exceto `node_modules`/`.next`) e no novo workflow — nenhum encontrado; confirmado que `mobile/`, `backend/campaia_core/`, `backend/api/`, `backend/db/` e `contracts/` permanecem intocados (`git diff --stat` vazio para cada um) | — | `grep` dirigido + `git diff --stat` | — |
| 19/09/2026 | Documentação reconciliada: `docs/web/06_ROADMAP_WORK_PACKAGES.md` (WP-01 marcado implementado), `docs/web/05_TESTES_CICD_MIGRACAO.md` (CI real do frontend refletido), `docs/web/00_INDICE_PONTO_ZERO_WEB.md` (referência cruzada ao novo doc 08, sem reabrir o veredito da missão anterior), `docs/web/08_CERTIFICACAO_WP01_FUNDACAO_WEB.md` (novo, certificação completa do WP-01), `web/README.md` (novo) | — | Revisão de cada arquivo | Arquivos citados |
| 19/09/2026 | Confirmação de que v21 é de fato a próxima versão do painel antes de criá-la; P-25 (stale desde a v20, já contradita pela própria tabela "Decisões registradas" da mesma versão) fechada nesta versão, não perpetuada silenciosamente | — | `grep -rl "v21"` vazio antes da criação deste arquivo | `01_PAINEL_EXECUCAO_v21_VIGENTE.md` |
| 20/09/2026 | Pré-flight da missão V1: reconfirmado `origin/main`@`41b521f`, PR #5 verde (Backend run `35483418972`, Frontend run `35483419038`, ambos `completed`/`success` em `d2ac3a5`), sem PR aberta concorrente | — | `git ls-remote`, `mcp__github__pull_request_read`, `mcp__github__actions_list` | HEAD `d2ac3a5` confirmado |
| 20/09/2026 | Etapa A: corrigidas inconsistências reais em `docs/web/08_CERTIFICACAO_WP01_FUNDACAO_WEB.md` (escopo do diff declarado como "só web/" quando o próprio commit já tocava docs/painel; placeholders "Ver relatório final" resolvidos com o CI real então conhecido) e no painel v21 (parágrafo de recomendação desatualizado, ainda dizendo que o System Design não havia começado) | — | Releitura de cada seção afetada | Commit `d2ac3a5` |
| 20/09/2026 | Etapa B: criada `feat/campaia-v1-complete-saas` a partir do HEAD corrigido e verde da PR #5; aberta PR #6 (Draft) com dependência explícita registrada na descrição | — | `git ls-remote`, `mcp__github__create_pull_request` | PR #6 |
| 20/09/2026 | Achado real (não hipotético): `AppState._seed_tokens()` era chamado incondicionalmente por todo `create_app()`, incluindo o `app` de nível de módulo em `api/main.py` — os tokens fixture do WP-01 estavam sempre alcançáveis, em qualquer ambiente, desde antes desta sessão. Corrigido com fail-closed de dois sinais independentes (`enable_test_auth_fixtures` + `CAMPAIA_ENV=test|local_dev`); `tests_api/test_helpers.py` e `test_persistence.py` atualizados para opt-in explícito, sem alterar seu comportamento observável | 3 | 89/89 testes de API preexistentes continuaram verdes após a mudança | `backend/api/state.py`, `backend/api/main.py` |
| 20/09/2026 | Cliente OIDC provider-neutral real (PKCE RFC 7636, state, nonce, verificação de ID token via JWKS) e provedor de identidade de teste real (discovery/authorize/token/JWKS spec-compliant, picker HTML, fail-closed pelo mesmo interruptor dos fixtures) implementados e verificados manualmente ponta a ponta antes de formalizar em teste | — | Fluxo completo dirigido via `TestClient` real contra a app real, sem mock de `oidc.py`/`routes_auth.py` | `backend/api/oidc.py`, `backend/api/test_idp.py` |
| 20/09/2026 | Achado de design corrigido durante a implementação: `PyJWKClient` (biblioteca padrão do PyJWT) faz sua própria busca HTTP real do JWKS, incompatível com transporte ASGI in-process — geraria uma tentativa de conexão de rede real a um host que só existe dentro do processo de teste, bloqueada pela política de rede do sandbox. Corrigido buscando o JWKS explicitamente com o mesmo cliente `httpx` usado para a troca de token (real para provedor real, `ASGITransport` para o provedor de teste), passando o JWKS já obtido para `verify_id_token` | — | Reprodução do erro real (`connect_rejected`), causa raiz identificada e corrigida, fluxo completo revalidado | `backend/api/oidc.py`, `backend/api/routes_auth.py` |
| 20/09/2026 | Sessão real (cookies `HttpOnly`/`Secure`/`SameSite=Lax`), CSRF por double-submit cookie, logout com revogação server-side implementados; `require_auth` passou a checar sessão real primeiro, com fallback fail-closed para o Bearer fixture (preservando os 89 testes preexistentes sem alteração) | 3 | Fluxo completo de login→CSRF→cross-tenant→logout verificado manualmente contra a app real antes de formalizar em teste | `backend/api/deps.py`, `backend/api/csrf.py`, `backend/api/routes_auth.py` |
| 20/09/2026 | 45 novos testes automatizados de API escritos e verdes: 18 unitários de OIDC (PKCE, state/nonce, verificação de ID token com 8 casos negativos incluindo assinatura adulterada e assinada por chave diferente), 19 de sessão HTTP completa (login real por 6 identidades fixture, sessão ausente/desconhecida/adulterada/expirada/revogada, CSRF ausente/incorreto/correto, isolamento cross-tenant via sessão, replay de state e de código de autorização, fail-closed ponta a ponta), 8 de fail-closed dos fixtures. 267 de domínio e AsyncAPI 24/24 reconfirmados sem regressão | 3 | `python3 -m unittest discover -s tests_api -t .` → 126/126; `-s tests` → 267/267; `validate_events_asyncapi.py` → 24/24 | `backend/tests_api/test_{oidc_unit,auth_session,auth_fail_closed}.py` |
| 20/09/2026 | Contrato atualizado aditivamente: 3 rotas novas (`/auth/login`, `/auth/callback`, `/auth/logout`) e esquema `cookieAuth`, sem alterar nenhuma rota de negócio existente; tipos do frontend regenerados e drift reconfirmado ausente | — | `python3 -c "import yaml; ..."` validação de sintaxe; `npm run contracts:check` | `contracts/bff-openapi.yaml`, `web/src/contracts/bff-openapi.generated.ts` |
| 20/09/2026 | Fronteira de segurança do frontend evoluída de "zero chamadas de rede" para uma allowlist explícita de 2 arquivos (`session.ts` leitura server-only; `LogoutButton.tsx` logout client-side protegido por CSRF) — decisão registrada e justificada, não uma flexibilização silenciosa; todo o resto de `web/src`, incluindo a tela `/` do WP-01, permanece em zero chamadas de rede, reconfirmado por `npm run boundary:check` | — | `npm run boundary:check` (21 arquivos, 0 violações) | `web/scripts/lib/security-boundaries.mjs` |
| 20/09/2026 | Página `/account` (3 estados: não configurado, sem sessão, com sessão real), `session.ts` (leitura server-side) e `LogoutButton.tsx` (logout client-side) implementados; build de produção confirma `/account` corretamente dinâmica (`ƒ`) quando `NEXT_PUBLIC_CAMPAIA_BFF_ORIGIN` está configurado no build, e estaticamente degradada quando não está — verificado com os dois builds | 3 | `npm run build` com e sem a variável de ambiente, comparando a marcação de rota | `web/src/app/account/`, `web/src/lib/session.ts`, `web/src/components/LogoutButton.tsx` |
| 20/09/2026 | 6 novos testes de frontend (Vitest: CSRF header correto/ausente/erro no `LogoutButton`; 3 estados do `AccountPage`) e 2 novos testes E2E Playwright (`/account` degradado, desktop+mobile) escritos e verdes | 3 | `npm run test` → 21/21; `npm run test:e2e` → 6/6 | `web/tests/{logout-button,account-page}.test.tsx`, `web/e2e/account.spec.ts` |
| 20/09/2026 | Secrets varridos em todo o diff do WP-02 — nenhum real encontrado (únicos literais não-credenciais, `dev-secret` de teste e `test-idp-internal-secret-not-a-real-credential`, cujo próprio nome já declara sua natureza, confiado apenas dentro do mesmo processo); confirmado que `mobile/`, `backend/campaia_core/`, `backend/db/` permanecem intocados | — | `grep` dirigido + `git diff --stat` | — |
| 20/09/2026 | Documentação reconciliada: `docs/web/06_ROADMAP_WORK_PACKAGES.md` (WP-02 marcado implementado, com as pendências reais explicitadas — revisão de segurança humana, CORS, step-up — não escondidas), `docs/web/00_INDICE_PONTO_ZERO_WEB.md` (referência ao novo doc 09), `docs/web/09_CERTIFICACAO_WP02_AUTENTICACAO_SESSAO_WEB.md` (novo), `web/README.md` (WP-02 documentado: variável de ambiente, 3 estados de `/account`, fronteira de segurança atualizada) | — | Revisão de cada arquivo | Arquivos citados |
| 20/09/2026 | Confirmação de que v22 é de fato a próxima versão do painel antes de criá-la; P-28 (CI do WP-01 pendente de confirmação) fechada nesta versão; P-29 e P-30 abertas explicitamente para as pendências reais do WP-02 (revisão de segurança humana; CORS e step-up via sessão Web) | — | `grep -rl "v22"` vazio antes da criação deste arquivo | `01_PAINEL_EXECUCAO_v22_VIGENTE.md` |
| 20/09/2026 | Pergunta direta do Diretor sobre execução completa do prompt anterior; resposta honesta de que apenas WP-01/WP-02 estavam prontos, 15 dos 17 Work Packages não iniciados, e que a revisão de segurança humana exigida pelo próprio Gate 3 do WP-02 não havia ocorrido | — | Diretor: *"referente ao prompt anterior vc executou 100% dele?"* | — |
| 20-21/09/2026 | Decisão do Diretor de continuar a execução sequencial dos Work Packages restantes, seguindo o mesmo método rigoroso já usado no WP-01/WP-02 | — | Diretor: *"sim continue e siga as tarefas do prompt na sequencia um após o outro"* | — |
| 21/09/2026 | CURRENT reconstruído por leitura direta antes de implementar: `backend/campaia_core/permissions.py` confirmado intocável e já suficiente para "troca de unidade" (`Principal.business_unit_ids: frozenset[str] \| None`, plural); só "troca de tenant" (`Principal.tenant_id`, singular) exigia mecanismo novo, inteiramente na camada de sessão | — | Leitura integral de `permissions.py` antes de qualquer edição | `backend/campaia_core/permissions.py` |
| 21/09/2026 | Claim opcional `campaia_memberships` implementada em `backend/api/oidc.py` (`Membership`, `_parse_memberships`, validação de que o membership ativo aparece na lista quando presente) e identidade de teste `multi_tenant_owner` adicionada a `backend/api/test_idp.py` | 3 | Verificado por import direto antes de construir em cima | `backend/api/oidc.py`, `backend/api/test_idp.py` |
| 21/09/2026 | `SessionRecord.memberships`, `TokenPrincipal.with_membership`, `AppState.switch_tenant` (reemissão de sessão + invalidação de step-up em todos os tenants do usuário) implementados em `backend/api/state.py`/`backend/api/oidc.py`; `require_web_session` novo em `backend/api/deps.py` (memberships/troca nunca via fixture Bearer) | 3 | 146/146 testes de API preexistentes reconfirmados sem regressão antes de prosseguir | `backend/api/state.py`, `backend/api/deps.py` |
| 21/09/2026 | Rotas `GET /session/memberships`/`POST /session/switch-tenant` (`backend/api/routes_session.py`) implementadas e conectadas em `backend/api/main.py`; contrato atualizado aditivamente (`Membership`, `SessionMemberships`, `SwitchTenantInput`, resposta `PermissionDenied`) | — | `python3 -c "import yaml; ..."` validação de sintaxe; `npm run contracts:generate`/`contracts:check` sem drift | `backend/api/routes_session.py`, `contracts/bff-openapi.yaml` |
| 21/09/2026 | 20 novos testes de backend escritos e verdes: 12 de troca de tenant (`test_tenant_switch.py`) + 8 de parsing de memberships (`test_oidc_unit.py::TestMemberships`); 126+20=146 reconfirmado antes de seguir para a correção de CORS (mais 9 testes, ver linha abaixo) | 3 | `python3 -m unittest discover -s tests_api -t .` → 146/146 | `backend/tests_api/{test_tenant_switch,test_oidc_unit}.py` |
| 21/09/2026 | Frontend: `LoadingState`/`ErrorState`/`EmptyState` (estados padronizados), `TenantSwitcher` (terceiro ponto client-side de rede, protegido por CSRF), `/dashboard` (shell autenticado) implementados; tipos novos (`Membership`, `SessionMemberships`) adicionados a `web/src/contracts/types.ts`; allowlist de fronteira de segurança estendida para 3 arquivos | 3 | `npm run typecheck`, `npm run boundary:check` (32 arquivos, 0 violações) | `web/src/components/{TenantSwitcher,LoadingState,ErrorState,EmptyState}.tsx`, `web/src/app/dashboard/` |
| 21/09/2026 | 15 novos testes Vitest (estados padronizados, `TenantSwitcher`, `/dashboard` em cada estado) + `e2e/dashboard.spec.ts` (smoke sem backend, padrão WP-01/02) escritos e verdes | 3 | `npm run test` → 36/36; `npm run test:e2e` → 8/8 | `web/tests/{dashboard-page,tenant-switcher}.test.tsx`, `web/e2e/dashboard.spec.ts` |
| 21/09/2026 | Construção da primeira suíte E2E cross-stack real deste projeto (`web/playwright.crossstack.config.ts`, `web/e2e-crossstack/tenant-switch.spec.ts`): backend real (`uvicorn`) + frontend real (`next dev`) sobre HTTPS autoassinado, duas origens distintas em `127.0.0.1` — a topologia que `web/README.md` já documentava desde o WP-01, nunca antes exercitada por um navegador real | — | `pip install uvicorn`, geração de certificado autoassinado via `openssl` sob demanda (nunca versionado) | `web/playwright.crossstack.config.ts` |
| 21/09/2026 | Achado real, não hipotético, exposto pela primeira execução da suíte cross-stack: `/auth/callback`/`/auth/logout` redirecionavam o navegador para a origem do próprio backend (não a do frontend), e nenhum CORS existia — ambos bloqueavam o login/troca de tenant reais contra duas origens de fato diferentes, exatamente a topologia documentada. Reproduzido e confirmado (404 real; preflight OPTIONS sem headers CORS) antes de corrigir | — | Logs reais do navegador (`page.on("response")`), reprodução isolada de cada falha | — |
| 21/09/2026 | Correção: variável de ambiente nova `CAMPAIA_WEB_ORIGIN` (opcional) — `_frontend_url` (`backend/api/routes_auth.py`) resolve o redirect pós-login/logout contra a origem real do frontend; `_build_middleware` (`backend/api/main.py`) habilita `CORSMiddleware` permitindo só essa origem exata (`allow_credentials=True`, nunca curinga). Ausente, comportamento idêntico ao anterior — confirmado por 9 novos testes (`test_cross_origin.py`) e pela regressão completa permanecendo verde | 3 | `python3 -m unittest discover -s tests_api -t .` → 155/155; `-s tests` → 267/267; `validate_events_asyncapi.py` → 24/24 | `backend/tests_api/test_cross_origin.py` |
| 21/09/2026 | Dois achados reais do próprio harness de teste (não do produto), expostos ao rodar a suíte cross-stack pela primeira vez, corrigidos antes de confiar no resultado: `next dev` bloqueia por padrão recursos cross-origin de HMR/hidratação em `127.0.0.1` (corrigido com `allowedDevOrigins`, sem efeito em produção) e o fetch server-side do Node recusava o certificado autoassinado mesmo com `NODE_EXTRA_CA_CERTS` (não honrado pelo dev server do Turbopack nesta versão; resolvido com `NODE_TLS_REJECT_UNAUTHORIZED=0`, escopado só a este processo de teste) | — | Reprodução isolada de cada falha com logging direto antes de corrigir; `next dev` também gerava `AGENTS.md`/`CLAUDE.md` indesejados, desabilitado via `agentRules: false` | `web/next.config.ts` |
| 21/09/2026 | Confirmação real, com um navegador de verdade: login completo lista os 2 memberships corretamente; troca de tenant muda tenant/papéis sem misturar o painel de sessão ativa (reforço de isolamento cross-tenant no nível de UI); troca rotaciona o cookie de sessão no servidor — os 3 testes cross-stack passam | 3 | `npm run test:e2e:crossstack` → 3/3 | `web/e2e-crossstack/tenant-switch.spec.ts` |
| 21/09/2026 | Job de CI dedicado ao E2E cross-stack criado (`crossstack-e2e`, `.github/workflows/frontend-tests.yml`), isolado do job principal para não acoplar o gate rápido de lint/typecheck/build a um harness de duplo processo | — | Validação de sintaxe YAML (`python3 -c "import yaml; ..."`) | `.github/workflows/frontend-tests.yml` |
| 21/09/2026 | Regressão completa final reexecutada no HEAD desta execução: 267 domínio, 155 API, AsyncAPI 24/24, 36 Vitest, 8 E2E de fumaça (desktop+mobile), 3 E2E cross-stack — todos verdes, zero regressão | 3 | Todos os comandos executados nesta sessão, saída capturada | `docs/web/10_CERTIFICACAO_WP03_TENANCY_SHELL.md` |
| 21/09/2026 | Documentação reconciliada: `docs/web/06_ROADMAP_WORK_PACKAGES.md` (WP-03 marcado implementado, achado de CORS/redirect documentado), `docs/web/00_INDICE_PONTO_ZERO_WEB.md` (referência ao novo doc 10), `docs/web/10_CERTIFICACAO_WP03_TENANCY_SHELL.md` (novo), `web/README.md` (WP-03 documentado: `/dashboard`, `TenantSwitcher`, `CAMPAIA_WEB_ORIGIN`, E2E cross-stack) | — | Revisão de cada arquivo | Arquivos citados |
| 21/09/2026 | Confirmação de que v23 é de fato a próxima versão do painel antes de criá-la; P-30 parcialmente fechada (CORS); P-31 aberta para o seletor de unidade ainda não exercitado | — | `grep -rl "v23"` vazio antes da criação deste arquivo | `01_PAINEL_EXECUCAO_v23_VIGENTE.md` |
| 21/09/2026 | CI do WP-03 falhou nas 3 checagens após o push da PR #6, sem nenhuma falha reproduzível localmente (clone limpo + venv limpo + `npm install` limpo, todos os comandos de CI executados manualmente com sucesso total); logs brutos inacessíveis por bloqueio de política de rede do sandbox (domínios de blob storage do Actions rejeitados); 1 re-execução permitida usada, falhou mais rápido ainda, com metadados de runner vazios (`runner_id: 0`), indicando falha de infraestrutura/quota do GitHub Actions, não regressão de código; comentário diagnóstico postado na PR #6 pedindo verificação humana de billing/quota | — | `curl`/`WebFetch` para logs brutos → `connect_rejected`/`EGRESS_BLOCKED`; reprodução local completa → 100% verde | Comentário na PR #6 |
| 21/09/2026 | Decisão do Diretor de autorizar avanço de mais 3 blocos, mesmo com o bloqueio de CI do WP-03 ainda não resolvido (julgado, com evidência, como bloqueio externo de infraestrutura e não de código) | — | Diretor: *"pode avançar mais 3 blocos"* | — |
| 21/09/2026 | CURRENT reconstruído por leitura direta antes de implementar o WP-04: `docs/web/06_ROADMAP_WORK_PACKAGES.md` §3 confirma que apenas WP-01 a WP-05 estão detalhados no roadmap, e que "Publicação sandbox" está bloqueada externamente (credenciais reais de provedor); grep por `ConnectionRepository.create()` em todo `backend/api/` e `backend/tests_api/` revelou que `POST /connections/oauth/start`, embora contratado e testado desde o WP-01/02, nunca resultava em uma `Connection` persistida de fato — o `.create()` só era chamado diretamente em setup de teste, nunca por nenhuma rota HTTP real | — | `grep -rn "connections.create"` dirigido antes de qualquer edição | `backend/api/routes_connections.py`, `backend/api/repositories.py` |
| 21/09/2026 | Correção do gap real: `PendingOAuth` (espelhando o padrão `PendingLogin` do WP-02) implementado em `backend/api/state.py` (`create_pending_oauth`/`pop_pending_oauth`, TTL 600s, vinculado a tenant, uso único); nova rota `POST /connections/oauth/complete` implementada em `backend/api/routes_connections.py`/`backend/api/main.py`, fechando o ciclo start→complete com persistência real | 3 | Escrita orientada a teste; 146+7=153 confirmado antes de seguir | `backend/api/state.py`, `backend/api/routes_connections.py` |
| 21/09/2026 | Achado real de bug de produção, capturado antes de qualquer deploy pela própria disciplina de escrita de testes: violação da disciplina de fechamento de idempotência em `oauth_complete` — o consumo de estado de uso único (`pop_pending_oauth`) ocorria fora do closure protegido por `IdempotencyStore.execute`, fazendo uma repetição legítima com a mesma `Idempotency-Key` falhar por "estado já usado"; corrigido movendo o consumo para dentro do closure `_do_create` | 3 | `test_replay_with_same_idempotency_key_does_not_create_a_second_connection` falhou, expôs o bug, corrigido, reexecutado verde | `backend/api/routes_connections.py`, `backend/tests_api/test_oauth_complete.py` |
| 21/09/2026 | Contrato atualizado aditivamente (`POST /connections/oauth/complete`, `OAuthCompleteRequest`) em `contracts/bff-openapi.yaml`; `BrandProfile`/`Connection` adicionados a `web/src/contracts/types.ts`; 7 novos testes de backend (`test_oauth_complete.py`) escritos e verdes | 3 | `npm run contracts:generate`/`contracts:check` sem drift; `python3 -m unittest discover -s tests_api -t .` → 162/162 | `contracts/bff-openapi.yaml`, `backend/tests_api/test_oauth_complete.py` |
| 21/09/2026 | Frontend do onboarding construído: `web/src/lib/session.ts` refatorado para um helper interno compartilhado (`getWithSessionCookie`) com duas novas funções (`getServerBrandProfiles`, `getServerConnections`); `BrandKitForm.tsx` e `ConnectAccountCard.tsx` (4º e 5º pontos client-side de rede, ambos protegidos por CSRF; `ConnectAccountCard` é o primeiro código de frontend a enviar `X-Step-Up-Token`, com marcador explícito `web-ui-connect-account-button-clicked` em vez de fingir reautenticação real) implementados; `/onboarding` (Server Component) implementado com a regra de negócio `canFinishOnboarding = connections.length > 0`; registro de empresa/unidade de negócio (spec §3.2/§3.3) e upload de logo confirmados fora de escopo (nenhum endpoint de backend existe para isso) | 3 | Leitura direta de `13_ESPECIFICACAO_TELAS_APP.md` §3/§4 e `grep -rn "onboarding" backend/ contracts/` (zero resultados) antes de construir | `web/src/components/{BrandKitForm,ConnectAccountCard}.tsx`, `web/src/app/onboarding/` |
| 21/09/2026 | Correções reais encontradas durante a escrita de testes de frontend: teste do `BrandKitForm` inicialmente enviava campos HTML5 `required` totalmente vazios, bloqueando o evento `submit` antes do handler React rodar (corrigido digitando um único espaço, exercitando a validação `.trim()` do próprio componente); `npm run build` (mas não `npm run typecheck` isoladamente, executado antes) capturou erro de tipo real em dois arquivos de teste (`status: "ACTIVE"` inferido como `string` solto em vez do union literal gerado) — corrigido com `as const` | — | `npm run build` (tsc completo) → erro real capturado e corrigido | `web/tests/{brand-kit-form,connect-account-card,onboarding-page}.test.tsx` |
| 21/09/2026 | 17 novos testes Vitest + `e2e/onboarding.spec.ts` (smoke sem backend) + `e2e-crossstack/onboarding.spec.ts` (3 testes reais: nada conectado inicialmente, conectar 1 canal simulado habilita "Concluir Onboarding", Brand Kit persiste após reload) escritos e verdes; allowlist de fronteira de segurança estendida para 5 arquivos | 3 | `npm run test` → 53/53; `npm run test:e2e` → 10/10; `npm run test:e2e:crossstack` → 6/6 | `web/tests/*.test.tsx`, `web/e2e/onboarding.spec.ts`, `web/e2e-crossstack/onboarding.spec.ts` |
| 21/09/2026 | Regressão completa final reexecutada no HEAD desta execução: 267 domínio, 162 API, AsyncAPI 24/24, 53 Vitest, 10 E2E de fumaça, 6 E2E cross-stack — todos verdes, zero regressão em `campaia_core`/`db`/`permissions.py` | 3 | Todos os comandos executados nesta sessão, saída capturada | `docs/web/11_CERTIFICACAO_WP04_ONBOARDING_BRAND_KIT.md` |
| 21/09/2026 | Documentação reconciliada: `docs/web/06_ROADMAP_WORK_PACKAGES.md` (WP-04 marcado implementado, gap real documentado), `docs/web/00_INDICE_PONTO_ZERO_WEB.md` (referência ao novo doc 11), `docs/web/11_CERTIFICACAO_WP04_ONBOARDING_BRAND_KIT.md` (novo), `web/README.md` (WP-04 documentado: onboarding, Brand Kit, Connect Accounts, regra de conclusão) | — | Revisão de cada arquivo | Arquivos citados |
| 21/09/2026 | Confirmação de que v24 é de fato a próxima versão do painel antes de criá-la; escopo do WP-06 confirmado como não detalhado no roadmap (§3), exigindo reconciliação de CURRENT própria antes de qualquer implementação — registrado como próxima pendência antes do 3º bloco autorizado pelo Diretor | — | `grep -rl "v24"` vazio (exceto este próprio arquivo) antes da criação | `01_PAINEL_EXECUCAO_v24_VIGENTE.md` |
| 21/09/2026 | Push do WP-04 (7 commits) confirmado na branch remota; CI da PR #6 falhou novamente com a mesma assinatura de infraestrutura já diagnosticada (conclusão em 2s, `runner_id: 0`/`runner_name: ""` em todos os 3 jobs); segundo comentário diagnóstico postado na PR #6, sem gastar a segunda re-execução (a primeira já havia sido gasta na falha do WP-03 com assinatura idêntica, sem valor diagnóstico adicional em repetir) | — | `git push`; `get_check_runs`/`get_workflow_job` via API do GitHub | Comentário na PR #6 |
| 21/09/2026 | CURRENT reconstruído por leitura direta antes de implementar o WP-05: `backend/api/main.py` confirmou que as 6 rotas do escopo (`POST /briefs`, `GET/POST /campaigns/{id}/plan[/regenerate]`, `POST .../validate`, `GET/POST /approvals`, `POST /approvals/{id}/decision`) já existiam desde blocos anteriores; `backend/api/deps.py require_auth` confirmado como aceitando tanto sessão Web real quanto Bearer fixture, nas duas ordens — sem gap equivalente ao do WP-04, mas nunca antes exercitado por uma sessão Web real (só por fixtures) | — | Leitura direta de `routes_campaigns.py`, `routes_approvals.py`, `deps.py` antes de qualquer edição | `backend/api/{main,deps,routes_campaigns,routes_approvals}.py` |
| 21/09/2026 | Frontend da jornada construído: `web/src/lib/session.ts` estendido com 4 leituras novas; tipos `Campaign`/`PolicyDecision`/`ApprovalRequest`/`CampaignPlan` adicionados a `types.ts`; `BriefForm.tsx`, `PlanPanel.tsx`, `ValidationPanel.tsx`, `ApprovalDecisionCard.tsx` (6º a 9º pontos client-side de rede, todos protegidos por CSRF) implementados; `/campaigns`, `/campaigns/[campaignId]`, `/approvals` implementados; navegação do `/dashboard` atualizada (link para as novas telas, substituindo o placeholder "Nenhuma funcionalidade de negócio ainda" desatualizado desde o WP-04) | 3 | `npm run typecheck`/`npm run boundary:check` a cada componente novo | `web/src/{lib/session.ts,contracts/types.ts,components/{BriefForm,PlanPanel,ValidationPanel,ApprovalDecisionCard}.tsx,app/{campaigns,approvals}/}` |
| 21/09/2026 | Achado real de contrato, capturado pelo próprio TypeScript ao tipar `ApprovalDecisionCard.tsx`: `ApprovalRequest.requested_by` ausente de `contracts/bff-openapi.yaml`, embora `api/helpers.py serialize_approval` sempre o retorne — corrigido aditivamente no contrato; `npm run contracts:generate`/`contracts:check` confirmaram sincronia | — | Erro real de `tsc` ao referenciar `approval.requested_by`; investigação por leitura do YAML e do serializer | `contracts/bff-openapi.yaml` |
| 21/09/2026 | Achado real de backend capturado por teste de sessão Web real (não apenas Bearer fixture, primeira vez que esta jornada foi exercitada assim): `ValidationPanel.tsx`'s `handleValidate()` não enviava `X-CSRF-Token` em `POST .../validate` — `CSRFMiddleware` exige o header em toda mutação autenticada por sessão, sem exceção por semântica de rota; corrigido adicionando o header | 3 | `test_briefing_approval_web_session.py` falhou com `403 PERMISSION_DENIED` na chamada de `/validate`, expôs o bug, corrigido, reexecutado verde | `web/src/components/ValidationPanel.tsx`, `backend/tests_api/test_briefing_approval_web_session.py` |
| 21/09/2026 | Achado real de identidade de teste incorreta no mesmo arquivo: proponente inicial (`marketer`) não tinha `APPROVAL_DECIDE` no domínio, então a autoaprovação falhava com `PERMISSION_DENIED` genérico em vez de `SEPARATION_OF_DUTIES` — corrigido trocando o proponente para `owner` (detém todas as permissões), isolando corretamente a rejeição por segregação de funções que o critério de aceitação do roadmap exige | — | Leitura de `campaia_core/permissions.py ROLE_PERMISSIONS`; reexecução do teste após a troca | `backend/tests_api/test_briefing_approval_web_session.py` |
| 21/09/2026 | Achado real de frontend, exposto pela primeira execução real do E2E cross-stack contra o backend de verdade: `PlanPanel.tsx` renderizava `plan.output` como texto simples; o schema real do agente `strategist` (`campaia_core/agents.py`) é um objeto estruturado (`objetivo`, `funil`, `canais`, `justificativa`) — React lançou "Objects are not valid as a React child"; corrigido tipando `StrategistPlanOutput` em `types.ts` e renderizando cada campo em uma `<dl>` | — | Execução real do E2E cross-stack, erro capturado no console do navegador/servidor | `web/src/{contracts/types.ts,components/PlanPanel.tsx}` |
| 21/09/2026 | 37 novos testes Vitest + `e2e/{campaigns,approvals}.spec.ts` (smoke sem backend) + `e2e-crossstack/briefing-approval-journey.spec.ts` (jornada completa de dois usuários reais: `owner` propõe, tenta autoaprovar — recusado, visível — `approver` em um `BrowserContext` separado decide de verdade) escritos e verdes; achado real de infraestrutura de teste corrigido no mesmo bloco: dois specs cross-stack rodando em workers separados contra o mesmo backend compartilhado corrompiam estado de sessão intermitentemente — `playwright.crossstack.config.ts` fixado em `workers: 1`; allowlist de fronteira de segurança estendida para 9 arquivos | 3 | `npm run test` → 90/90; `npm run test:e2e` → 14/14; `npm run test:e2e:crossstack` → 7/7 (serial, reproduzido também sob paralelismo padrão para confirmar o flake antes de corrigir) | `web/tests/*.test.tsx`, `web/e2e/{campaigns,approvals}.spec.ts`, `web/e2e-crossstack/briefing-approval-journey.spec.ts`, `web/playwright.crossstack.config.ts` |
| 21/09/2026 | Regressão completa final reexecutada no HEAD desta execução: 267 domínio, 164 API, AsyncAPI 24/24, 90 Vitest, 14 E2E de fumaça, 7 E2E cross-stack — todos verdes, zero regressão em `campaia_core`/`db`/`permissions.py`/`routes_campaigns.py`/`routes_approvals.py` | 3 | Todos os comandos executados nesta sessão, saída capturada | `docs/web/12_CERTIFICACAO_WP05_BRIEFING_APROVACAO.md` |
| 21/09/2026 | Documentação reconciliada: `docs/web/06_ROADMAP_WORK_PACKAGES.md` (WP-05 marcado implementado, Gate 5 fechado tecnicamente, os 3 achados reais documentados), `docs/web/00_INDICE_PONTO_ZERO_WEB.md` (referência ao novo doc 12), `docs/web/12_CERTIFICACAO_WP05_BRIEFING_APROVACAO.md` (novo), `web/README.md` (WP-05 documentado: briefing, estratégia, validação, fila de aprovação) | — | Revisão de cada arquivo | Arquivos citados |
| 21/09/2026 | Confirmação de que v25 é de fato a próxima versão do painel antes de criá-la; escopo do WP-06 permanece não definido pelo roadmap — reconciliação de CURRENT para defini-lo é a próxima pendência antes do 3º bloco autorizado pelo Diretor ("pode avançar mais 3 blocos") | — | `grep -rl "v25"` vazio (exceto este próprio arquivo) antes da criação | `01_PAINEL_EXECUCAO_v25_VIGENTE.md` |
| 21/09/2026 | Push do WP-05 (6 commits) confirmado na branch remota; CI da PR #6 falhou novamente com a mesma assinatura de infraestrutura já diagnosticada duas vezes (conclusão em ~2s, `runner_id: 0`); terceiro comentário diagnóstico postado na PR #6, sem gastar a segunda re-execução (mesma justificativa das vezes anteriores: repetir não teria valor diagnóstico adicional) | — | `git push`; `get_check_runs` via API do GitHub | Comentário na PR #6 |
| 21/09/2026 | Definição do escopo do WP-06 por reconciliação de CURRENT, antes de qualquer implementação: `docs/web/06_ROADMAP_WORK_PACKAGES.md` §3 revisitada — dos candidatos citados (publicação sandbox, reconciliação, métricas, orçamento, otimização, hardening, acessibilidade, observabilidade, segurança formal, staging, produção controlada), apenas "orçamento (Web sobre API já pronta)" está tecnicamente desbloqueado sem decisão humana nova ou credencial externa; leitura direta de `routes_campaigns.py`'s `patch_budget` e `campaia_core/budget.py`'s `validate_change` confirmou uma rota real, já testada, pronta para receber camada Web reutilizando a fila de aprovação do WP-05 | — | Leitura direta de `PATCH /campaigns/{id}/budget` e `BudgetEngine.validate_change` antes de escrever a seção do roadmap | `backend/api/routes_campaigns.py`, `backend/campaia_core/budget.py` |
| 21/09/2026 | Seção WP-06 escrita em `docs/web/06_ROADMAP_WORK_PACKAGES.md` (objetivo, escopo, fora de escopo, dependências, segurança, critérios de aceitação, testes, riscos, rollback, gate, definição de pronto, autorização) e commitada separadamente, ANTES de qualquer implementação — mesma disciplina usada para reconstruir CURRENT antes de mudar, aplicada à própria definição de escopo de um bloco não previsto no roadmap original | — | Revisão do texto antes do commit | `docs/web/06_ROADMAP_WORK_PACKAGES.md` |
| 21/09/2026 | Achados reais de contrato, encontrados antes de escrever qualquer chamada de rede do frontend (ao projetar `BudgetPanel.tsx` contra o schema `ApprovalRequest` gerado): `kind` (já descrito no próprio código do backend como "extra, not in the contract") e `amount` (nunca sequer mencionado) ausentes do contrato apesar de sempre devolvidos por `serialize_approval`; `POST /approvals` (implementado e usado desde o WP-05) nunca estivera documentado — os 3 corrigidos aditivamente na mesma área do arquivo | — | Leitura direta de `contracts/bff-openapi.yaml`'s `ApprovalRequest` e `api/models.py ApprovalResponse` antes de qualquer componente | `contracts/bff-openapi.yaml` |
| 21/09/2026 | `BudgetPanel.tsx` construído: exibe orçamento atual, propõe alteração (`POST /approvals`, `kind: BUDGET_CHANGE`), aplica uma vez aprovada (`PATCH /campaigns/{id}/budget`, CSRF + step-up + idempotency-key); decisão continua acontecendo na fila `/approvals` já existente, sem tela nova; integrado a `/campaigns/[campaignId]` | 3 | `npm run typecheck`/`npm run boundary:check` a cada etapa | `web/src/components/BudgetPanel.tsx`, `web/src/app/campaigns/[campaignId]/page.tsx` |
| 21/09/2026 | Achado real de correção, encontrado ao integrar `BudgetPanel` à tela existente: `hasPendingApproval` (calculado em `page.tsx`, usado por `ValidationPanel`) não filtrava por `kind` — uma aprovação `BUDGET_CHANGE` pendente passaria a desabilitar incorretamente o botão "Solicitar aprovação" de `ValidationPanel`, que é sobre publicação, não orçamento. Bug latente desde o WP-05, nunca manifestado porque toda campanha só podia ter aprovações `PUBLISH` até este bloco | — | Leitura do próprio código de `page.tsx` ao adicionar o segundo `kind` de aprovação por campanha | `web/src/app/campaigns/[campaignId]/page.tsx` |
| 21/09/2026 | Achado real de tipo, capturado por teste de backend (primeira comparação numérica real sobre `budget.daily_cap`, nunca feita antes): `Campaign.budget` (`total_amount`/`daily_cap`/`spent_to_date`) é tipado `number` pelo contrato mas serializa como STRING JSON na resposta real (`"500"`, não `500`) — testes anteriores só verificavam o conjunto de chaves do objeto, nunca comparavam valores. Quebrava a lógica de "aprovação já aplicada" de `BudgetPanel.tsx` (`Number(...) !== string` é sempre `true`), escondendo permanentemente o botão "Aplicar alteração"; corrigido coerindo os dois lados via `Number(...)` no ponto de uso; divergência de tipo em si (contrato × runtime) registrada como pendência real (P-36), não corrigida — mudança de escopo maior | 3 | `test_propose_approve_apply_via_real_session_two_distinct_users` falhou com `'500' != 500.0`, expôs o bug, corrigido, reexecutado verde | `web/src/components/BudgetPanel.tsx`, `backend/tests_api/test_budget_change_web_session.py` |
| 21/09/2026 | 2 novos testes de backend via sessão Web real (jornada completa propor→aprovar→aplicar com dois usuários distintos; variação além do limite percentual recusada com `422 BUDGET_LIMIT` mesmo já aprovada) + 9 novos testes Vitest (`BudgetPanel`) escritos e verdes; `data-testid` estável adicionado a `ApprovalDecisionCard.tsx` (por `kind`+`campaign_id`) após um E2E cross-stack real revelar ambiguidade de seletor (`getByText("APPROVED")` casando duas aprovações distintas na mesma fila compartilhada entre specs) | 3 | `python3 -m unittest discover -s tests_api` → 166/166; `npm run test` → 99/99 | `backend/tests_api/test_budget_change_web_session.py`, `web/tests/budget-panel.test.tsx`, `web/src/components/ApprovalDecisionCard.tsx` |
| 21/09/2026 | `e2e-crossstack/budget-change.spec.ts` (jornada completa de dois usuários reais: propor → aprovar em `BrowserContext` separado → aplicar → orçamento exibido reflete o valor real) escrito; achado real de locator E2E corrigido durante a escrita: `getByText("BRL 1000")` sem `exact: true` casava também `"BRL 10000"` (substring) e o texto combinado do resumo do briefing — corrigido com `exact: true` | 3 | `npm run test:e2e:crossstack` → 8/8 (isolado e junto com os demais specs, incluindo reprodução da ambiguidade de fila compartilhada antes de corrigir com `data-testid`) | `web/e2e-crossstack/budget-change.spec.ts` |
| 21/09/2026 | Regressão completa final reexecutada no HEAD desta execução: 267 domínio, 166 API, AsyncAPI 24/24, 99 Vitest, 14 E2E de fumaça, 8 E2E cross-stack — todos verdes, zero regressão em `campaia_core`/`db`/`permissions.py`/`routes_campaigns.py`/`routes_approvals.py`/`models.py` | 3 | Todos os comandos executados nesta sessão, saída capturada | `docs/web/13_CERTIFICACAO_WP06_ALTERACAO_ORCAMENTO.md` |
| 21/09/2026 | Documentação reconciliada: `docs/web/06_ROADMAP_WORK_PACKAGES.md` (WP-06 marcado implementado, os 4 achados reais documentados), `docs/web/00_INDICE_PONTO_ZERO_WEB.md` (referência ao novo doc 13), `docs/web/13_CERTIFICACAO_WP06_ALTERACAO_ORCAMENTO.md` (novo), `web/README.md` (WP-06 documentado: propor, decidir na fila existente, aplicar) | — | Revisão de cada arquivo | Arquivos citados |
| 21/09/2026 | Confirmação de que v26 é de fato a próxima versão do painel antes de criá-la; com o WP-06, a autorização "pode avançar mais 3 blocos" do Diretor está integralmente executada — nenhum bloco além do WP-06 foi definido; qualquer trabalho futuro exige nova autorização explícita do Diretor E nova reconciliação de CURRENT, registrado como P-37 | — | `grep -rl "v26"` vazio (exceto este próprio arquivo) antes da criação | `01_PAINEL_EXECUCAO_v26_VIGENTE.md` |
| 21/09/2026 | Diretor informa o fato confirmado do bloqueio de CI: cota gratuita de minutos do GitHub Actions esgotada | — | Diretor: *"a minha cota gratuita no github foi alcançada e os testes CI creio que não será possivel concluir"* | Registrado como P-38 |
| 21/09/2026 | Diretor concede nova autorização, distinta de "pode avançar mais 3 blocos": continuar a construção enquanto a cota não renova (dia 31), com os "testes necessários" (confirmação do CI remoto) adiados para depois disso — testes locais seguem obrigatórios a cada passo, nunca suspensos | — | Diretor: *"a cota será renovada no dia 31 então vamos continuar trabalhando na construção e fazer tudo que for possível sem atrasar o termino e no final faremos os testes necessários"* | "Decisões registradas" desta versão |
| 21/09/2026 | CURRENT reconstruído por leitura direta de código-fonte antes de definir o escopo do WP-07: `backend/api/routes_autonomy.py` e `backend/campaia_core/autonomy.py` confirmam `GET/PUT /autonomy` já implementadas e já testadas, reutilizando a mesma máquina de aprovação dos WP-05/06 (`kind: AUTONOMY_CHANGE`); `AUTONOMY_CHANGE` confirmada dentro da lista fechada `ALWAYS_REQUIRE_HUMAN`, implementando diretamente a decisão do Diretor sobre Modo Manual/Automático (`docs/product/DECISOES_DIRETOR.md` item 5) | — | Leitura direta de `routes_autonomy.py`, `autonomy.py`, `tests_api/test_smoke_endpoints.py` antes de escrever a seção do roadmap | `backend/api/routes_autonomy.py`, `backend/campaia_core/autonomy.py` |
| 21/09/2026 | Seção WP-07 escrita em `docs/web/06_ROADMAP_WORK_PACKAGES.md` (objetivo, escopo, achado real de restrição de backend documentado — `campaign_id` obrigatório mesmo para `AUTONOMY_CHANGE` tenant-wide —, fora de escopo, dependências, segurança, critérios de aceitação, testes, riscos, rollback, gate, definição de pronto, autorização) e commitada separadamente, ANTES de qualquer implementação | — | Revisão do texto antes do commit | `docs/web/06_ROADMAP_WORK_PACKAGES.md` |
| 21/09/2026 | `AutonomyPanel.tsx` construído: exibe nível atual, teto contratado, gatilhos `always_require_human`; propõe alteração (`POST /approvals`, `kind: AUTONOMY_CHANGE`), aplica uma vez aprovada (`PUT /autonomy`, CSRF + step-up + idempotency-key); níveis acima do teto desabilitados, nunca escondidos; integrado a `/dashboard` | 3 | `npm run typecheck` a cada etapa | `web/src/components/AutonomyPanel.tsx`, `web/src/app/dashboard/page.tsx` |
| 21/09/2026 | Achado real bloqueando `npm run typecheck`/`npm run build`, encontrado ao escrever `autonomy-panel.test.tsx`: um fixture `Campaign` totalmente tipado (`const CAMPAIGN: Campaign = {...}`, diferente dos fixtures anteriores, sem anotação de tipo explícita) revelou `business_unit_id` como propriedade desconhecida — comparação direta com `backend/api/models.py CampaignResponse` confirmou o campo como real e sempre devolvido, apenas nunca declarado no contrato; corrigido aditivamente, junto com `AutonomySettings.max_level_allowed` (achado anterior, ao projetar o componente) | 3 | `npm run typecheck` falhou com `TS2353`, causa raiz investigada (comparação com fixture `Campaign` de teste anterior, sem anotação de tipo), corrigida no contrato, reexecutado verde | `contracts/bff-openapi.yaml`, `web/tests/autonomy-panel.test.tsx` |
| 21/09/2026 | 2 novos testes de backend via sessão Web real (jornada completa propor→aprovar→aplicar, nível 1→0; nível acima do teto aceito na proposta/aprovação mas recusado com `422 VALIDATION_FAILED` na aplicação) + 10 novos testes Vitest (`AutonomyPanel`) + 1 novo teste em `dashboard-page.test.tsx` escritos e verdes | 3 | `python3 -m unittest discover -s tests_api` → 168/168; `npm run test` → 110/110 | `backend/tests_api/test_autonomy_change_web_session.py`, `web/tests/autonomy-panel.test.tsx` |
| 21/09/2026 | `e2e-crossstack/autonomy-change.spec.ts` (jornada completa de dois usuários reais: propor em `/dashboard` → aprovar em `BrowserContext` separado → aplicar → nível exibido reflete o valor real) escrito e verde isoladamente; suíte cross-stack completa revelou uma regressão real em `briefing-approval-journey.spec.ts` (WP-05): duas asserções `getByText("APPROVED")` sem escopo ficaram ambíguas com o card `APPROVED` que o novo spec deixa na mesma fila compartilhada — causa raiz investigada e corrigida (delimitação por `data-testid`, mesma disciplina do WP-06), não mascarada nem pulada | 3 | `npm run test:e2e:crossstack` → 9/9 (após a correção; 8/9 antes, com a falha de modo estrito diagnosticada e corrigida) | `web/e2e-crossstack/autonomy-change.spec.ts`, `web/e2e-crossstack/briefing-approval-journey.spec.ts` |
| 21/09/2026 | Regressão completa final reexecutada no HEAD desta execução: 267 domínio, 168 API, AsyncAPI 24/24, 110 Vitest, 9 E2E cross-stack — todos verdes, zero regressão em `campaia_core`/`db`/`permissions.py`/`routes_autonomy.py`/`routes_approvals.py`/`models.py` | 3 | Todos os comandos executados nesta sessão, saída capturada | `docs/web/14_CERTIFICACAO_WP07_NIVEL_AUTONOMIA.md` |
| 21/09/2026 | Documentação reconciliada: `docs/web/06_ROADMAP_WORK_PACKAGES.md` (WP-07 marcado implementado, achados reais documentados), `docs/web/00_INDICE_PONTO_ZERO_WEB.md` (referência ao novo doc 14), `docs/web/14_CERTIFICACAO_WP07_NIVEL_AUTONOMIA.md` (novo), `web/README.md` (WP-07 documentado: propor, decidir na fila existente, aplicar) | — | Revisão de cada arquivo | Arquivos citados |
| 21/09/2026 | Confirmação de que v27 é de fato a próxima versão do painel antes de criá-la; com o WP-07, a nova autorização do Diretor para continuar a construção durante o bloqueio de cota do CI foi integralmente executada — nenhum bloco além do WP-07 foi definido; qualquer trabalho futuro exige nova autorização explícita do Diretor E nova reconciliação de CURRENT | — | `grep -rl "v27"` vazio (exceto este próprio arquivo) antes da criação | `01_PAINEL_EXECUCAO_v27_VIGENTE.md` |
| 21/09/2026 | Diretor reafirma explicitamente a autorização de continuar a construção sob o mesmo bloqueio de cota do CI, servindo de base para definir e implementar o WP-08 | — | Diretor: *"sim provalvelmente as cotas não vai permitir rodar mais testes mas vamos continuar a construção eu autorizo prosseguir e no final iremos auditar e corrigir o que for necessário"* | "Decisões registradas" desta versão |
| 21/09/2026 | CURRENT reconstruído por leitura direta de código-fonte antes de definir o escopo do WP-08: `backend/api/routes_campaigns.py`'s `kill_switch`, `backend/campaia_core/states.py`'s `apply_kill_switch`/`PAUSABLE_STATES` e `contracts/bff-openapi.yaml`'s `/kill-switch` confirmam a rota já implementada e já testada; investigação do candidato alternativo ("pausar campanha") revela uma lacuna arquitetural real: `PAUSED → ACTIVE` é uma transição guardada real, mas nenhuma rota HTTP a expõe, e `AdsConnector` Protocol não define nenhuma operação canônica de retomada — kill switch escolhido por ser unidirecional por definição, sem esse problema | — | Leitura direta de `routes_campaigns.py`, `states.py`, `connectors.py` antes de escrever a seção do roadmap | `backend/api/routes_campaigns.py`, `backend/campaia_core/{states,connectors}.py` |
| 21/09/2026 | Seção WP-08 escrita em `docs/web/06_ROADMAP_WORK_PACKAGES.md` (objetivo, escopo, achado real de restrição arquitetural documentado — sem rota de retomada —, decisão de restringir a Web aos escopos CAMPAIGN/TENANT, fora de escopo, dependências, segurança, critérios de aceitação, testes, riscos, rollback, gate, definição de pronto, autorização) e commitada separadamente, ANTES de qualquer implementação | — | Revisão do texto antes do commit | `docs/web/06_ROADMAP_WORK_PACKAGES.md` |
| 21/09/2026 | Achado real de contrato, encontrado ao projetar `KillSwitchPanel.tsx` (antes de escrever qualquer chamada de rede): a resposta 202 de `/kill-switch` não tinha schema nenhum definido em `contracts/bff-openapi.yaml`, apesar de sempre devolver `{scope, affected_campaign_ids}` (`routes_campaigns.py _do_kill_switch`, confirmado por leitura direta) — corrigido aditivamente (`KillSwitchResult`) | — | Leitura direta do contrato e do handler antes de qualquer componente | `contracts/bff-openapi.yaml` |
| 21/09/2026 | `KillSwitchPanel.tsx` construído: escopo (CAMPAIGN/TENANT), motivo obrigatório, seletor de campanha; aciona `POST /kill-switch` (CSRF + Idempotency-Key, SEM X-Step-Up-Token, refletindo o próprio desenho do domínio); resultado exibido a partir da resposta real, sem recarregar a página; integrado a `/dashboard` | 3 | `npm run typecheck` a cada etapa | `web/src/components/KillSwitchPanel.tsx`, `web/src/app/dashboard/page.tsx` |
| 21/09/2026 | 3 novos testes de backend via sessão Web real (escopo CAMPAIGN pausa de verdade uma campanha `APPROVED`/publicada, chegando lá pelo mesmo fluxo completo do WP-05, sem step-up; escopo TENANT sobre campanha DRAFT devolve lista vazia; identidade sem `KILL_SWITCH` recusada com 403) + 9 novos testes Vitest (`KillSwitchPanel`) + 1 novo em `dashboard-page.test.tsx` escritos e verdes | 3 | `python3 -m unittest discover -s tests_api` → 171/171; `npm run test` → 120/120 | `backend/tests_api/test_kill_switch_web_session.py`, `web/tests/kill-switch-panel.test.tsx` |
| 21/09/2026 | Achado real de alcance de teste, encontrado ao investigar como levar uma campanha a um estado pausável via browser: nenhuma tela do Web app aciona `POST /campaigns/{id}/publish` (confirmado pelo comentário preexistente em `campaigns/[campaignId]/page.tsx`, não introduzido por este bloco) — nenhum caminho puramente de navegador chega a um estado pausável; `e2e-crossstack/kill-switch.spec.ts` escrito para o único resultado real alcançável via UI (escopo TENANT, "nenhuma campanha afetada"), documentado explicitamente como tal, em vez de forçar um teste que a UI não suporta hoje | 3 | `npm run test:e2e:crossstack` → 10/10 | `web/e2e-crossstack/kill-switch.spec.ts` |
| 21/09/2026 | Regressão completa final reexecutada no HEAD desta execução: 267 domínio, 171 API, AsyncAPI 24/24, 120 Vitest, 10 E2E cross-stack — todos verdes, zero regressão em `campaia_core`/`db`/`permissions.py`/`routes_campaigns.py`/`models.py` | 3 | Todos os comandos executados nesta sessão, saída capturada | `docs/web/15_CERTIFICACAO_WP08_PARADA_EMERGENCIA.md` |
| 21/09/2026 | Documentação reconciliada: `docs/web/06_ROADMAP_WORK_PACKAGES.md` (WP-08 marcado implementado, achados reais documentados), `docs/web/00_INDICE_PONTO_ZERO_WEB.md` (referência ao novo doc 15), `docs/web/15_CERTIFICACAO_WP08_PARADA_EMERGENCIA.md` (novo), `web/README.md` (WP-08 documentado: acionar, escopos, fora de escopo) | — | Revisão de cada arquivo | Arquivos citados |
| 21/09/2026 | Confirmação de que v28 é de fato a próxima versão do painel antes de criá-la; com o WP-08, a autorização do Diretor de continuar a construção durante o bloqueio de cota do CI segue integralmente exercitada — nenhum bloco além do WP-08 foi definido; qualquer trabalho futuro exige nova autorização explícita do Diretor E nova reconciliação de CURRENT | — | `grep -rl "v28"` vazio (exceto este próprio arquivo e a certificação do WP-08, que já o cita corretamente como referência futura) antes da criação | `01_PAINEL_EXECUCAO_v28_VIGENTE.md` |
| 21/09/2026 | Diretor autoriza construir mais 3 blocos, reafirmando a continuidade da construção durante o bloqueio de cota do CI | — | Diretor: *"sim provalvelmente as cotas não vai permitir rodar mais testes mas vamos continuar a construção eu autorizo prosseguir e no final iremos auditar e corrigir o que for necessário"*, seguido de *"pode sim construa mais 3 blocos"* | "Decisões registradas" desta versão |
| 21/09/2026 | CURRENT reconstruído por leitura direta de código-fonte antes de definir o escopo dos WP-09/10/11: `routes_connections.py` (`revoke_connection`, `connection_capabilities`), `routes_audit.py` (`list_audit_events`) e `routes_campaigns.py` (`get_insights`) confirmam as 3 rotas já implementadas e já testadas, nenhuma exposta na Web até então | — | Leitura direta dos 3 arquivos antes de escrever as 3 seções do roadmap | `backend/api/routes_{connections,audit,campaigns}.py` |
| 21/09/2026 | 3 seções (WP-09, WP-10, WP-11) escritas em `docs/web/06_ROADMAP_WORK_PACKAGES.md` (cada uma com objetivo, escopo, achados reais de contrato já conhecidos, fora de escopo, dependências, segurança, critérios de aceitação, testes, riscos, rollback, gate, definição de pronto, autorização) e commitadas juntas, ANTES de qualquer implementação | — | Revisão do texto antes do commit | `docs/web/06_ROADMAP_WORK_PACKAGES.md` |
| 21/09/2026 | Achado real de contrato, encontrado ao projetar o texto das capacidades: `Capability` (`contracts/bff-openapi.yaml`) não declarava `provider`, `country` nem `api_version`, apesar de `CapabilityResponse` sempre devolvê-los — corrigido aditivamente | — | Leitura direta do contrato e de `api/models.py CapabilityResponse` antes de qualquer componente | `contracts/bff-openapi.yaml` |
| 21/09/2026 | Decisão de design revisada durante a implementação, antes de qualquer commit: a consulta de capacidades seria a primeira chamada GET client-side desta aplicação (documentado assim na definição de escopo original); revisitada em favor do padrão já estabelecido (toda leitura passa por `session.ts`, server-side) — `getServerConnectionCapabilities` adicionada a `session.ts`, `onboarding/page.tsx` busca capacidades de cada conexão ativa e repassa como prop | — | Revisão da própria decisão antes de escrever `ConnectAccountCard.tsx` | `web/src/lib/session.ts`, `web/src/app/onboarding/page.tsx` |
| 21/09/2026 | `ConnectAccountCard.tsx` estendido: botão "Desconectar" (`DELETE /connections/{id}`, CSRF + step-up + idempotency-key) e exibição da lista real de capacidades recebida via prop | 3 | `npm run typecheck` a cada etapa | `web/src/components/ConnectAccountCard.tsx` |
| 21/09/2026 | 2 novos testes de backend via sessão Web real (conectar → capacidades reais → desconectar, confirmado por `GET /connections` refletindo `status: REVOKED`; identidade sem `CONNECTION_MANAGE` recusada) + 4 novos testes Vitest (`ConnectAccountCard`) + 1 novo em `onboarding-page.test.tsx` escritos e verdes | 3 | `python3 -m unittest discover -s tests_api` → 173/173; `npm run test` → 125/125 | `backend/tests_api/test_connection_lifecycle_web_session.py`, `web/tests/connect-account-card.test.tsx` |
| 21/09/2026 | Achado real de teste E2E, encontrado ao rodar o spec estendido: a primeira versão do novo teste usava o provider GOOGLE_ADS e falhou por timeout, porque um teste anterior no mesmo arquivo (`onboarding.spec.ts`, WP-04) já conecta GOOGLE_ADS e nunca desconecta, e o backend real é compartilhado por todo o arquivo (`workers: 1`) — mesma causa raiz já documentada como P-34. Corrigido usando o provider META, que permanece genuinamente desconectado até este teste | 3 | `npm run test:e2e:crossstack` → falha isolada reproduzida, causa raiz identificada, corrigida, reexecutado verde (4/4 no arquivo, 11/11 na suíte completa) | `web/e2e-crossstack/onboarding.spec.ts` |
| 21/09/2026 | Regressão completa final reexecutada no HEAD desta execução: 267 domínio, 173 API, AsyncAPI 24/24, 125 Vitest, 11 E2E cross-stack — todos verdes, zero regressão em `campaia_core`/`db`/`permissions.py`/`routes_connections.py`/`models.py` | 3 | Todos os comandos executados nesta sessão, saída capturada | `docs/web/16_CERTIFICACAO_WP09_DESCONECTAR_E_CAPACIDADES.md` |
| 21/09/2026 | Documentação reconciliada: `docs/web/06_ROADMAP_WORK_PACKAGES.md` (WP-09 marcado implementado, achados reais documentados), `docs/web/00_INDICE_PONTO_ZERO_WEB.md` (referência ao novo doc 16), `docs/web/16_CERTIFICACAO_WP09_DESCONECTAR_E_CAPACIDADES.md` (novo), `web/README.md` (WP-09 documentado: desconectar, capacidades, decisão de design revisada) | — | Revisão de cada arquivo | Arquivos citados |
| 21/09/2026 | Confirmação de que v29 é de fato a próxima versão do painel antes de criá-la; com o WP-09, a autorização do Diretor de construir mais 3 blocos segue em execução (WP-10 e WP-11 ainda pendentes) — nenhum bloco além do WP-11 foi definido | — | `grep -rl "v29"` vazio (exceto este próprio arquivo) antes da criação | `01_PAINEL_EXECUCAO_v29_VIGENTE.md` |
| 21/09/2026 | `web/src/app/audit/page.tsx` construído (Server Component puro): lista `GET /audit-events` (quando, quem, ação, alvo bruto), mais recente primeiro, filtro `campaign_id` opcional; `getServerAuditEvents` adicionada a `session.ts`; link "Trilha de auditoria" adicionado ao card de navegação de `/dashboard` | 3 | `npm run typecheck` a cada etapa | `web/src/app/audit/page.tsx`, `web/src/lib/session.ts`, `web/src/app/dashboard/page.tsx` |
| 21/09/2026 | 6 novos testes Vitest (`audit-page.test.tsx`) escritos e verdes antes de qualquer teste de backend ou E2E, cobrindo config ausente, sem sessão, erro de leitura, lista vazia, ordenação mais-recente-primeiro, filtro `campaign_id` com nota visível | 3 | `npx vitest run` → 131/131 (125 pré-existentes + 6 novos) | `web/tests/audit-page.test.tsx` |
| 21/09/2026 | 3 novos testes de backend via sessão Web real escritos e verdes: eventos reais de outro bloco (conectar via WP-04) aparecem na trilha com `actor_id`/`target`/`actor_kind` corretos; filtro `campaign_id` restringe corretamente, excluindo `OAUTH_START`; identidade sem `AUDIT_VIEW` recusada com 403 | 3 | `python3 -m unittest discover -s tests_api` → 176/176 (173 pré-existentes + 3 novos) isolado e na suíte completa | `backend/tests_api/test_audit_trail_web_session.py` |
| 21/09/2026 | `e2e-crossstack/audit-trail.spec.ts` (novo spec dedicado) escrito: conectar WHATSAPP (provider genuinamente desconectado em toda a suíte até este spec) → ver eventos reais em `/audit` → desconectar, restaurando o estado compartilhado. Verde isoladamente (1/1) | 3 | `npx playwright test -c playwright.crossstack.config.ts e2e-crossstack/audit-trail.spec.ts` → 1/1 | `web/e2e-crossstack/audit-trail.spec.ts` |
| 21/09/2026 | Achado real de bug pré-existente, encontrado ao rodar a suíte cross-stack completa pela primeira vez com o spec novo: os dois primeiros testes de `onboarding.spec.ts` (WP-04) falharam — `web/src/app/onboarding/page.tsx` calculava `canFinishOnboarding` a partir de `connections.length > 0` (lista bruta, incluindo revogadas) em vez de `activeConnections.length > 0`; equivalentes antes do WP-09 introduzir o status `REVOKED`, deixaram de ser equivalentes depois. Investigado por causa raiz (não contornado ajustando o spec novo), corrigido usando `activeConnections.length > 0`, com 1 novo teste Vitest de regressão cobrindo exatamente esse estado. Registrado como P-44 | 3 | `npx playwright test -c playwright.crossstack.config.ts` → 10/12 antes da correção (falha real reproduzida), 12/12 depois; `npx vitest run` → 132/132 (131 + 1 regressão) | `web/src/app/onboarding/page.tsx`, `web/tests/onboarding-page.test.tsx` |
| 21/09/2026 | Regressão completa final reexecutada no HEAD desta execução (`cceb331`): 267 domínio, 176 API, AsyncAPI 24/24, 132 Vitest, 12 E2E cross-stack — todos verdes, zero regressão em `campaia_core`/`db`/`permissions.py`/`routes_audit.py`/`models.py`; `contracts/bff-openapi.yaml` intocado (nenhum achado de contrato neste bloco) | 3 | Todos os comandos executados nesta sessão, saída capturada | `docs/web/17_CERTIFICACAO_WP10_TRILHA_AUDITORIA.md` |
| 21/09/2026 | Documentação reconciliada: `docs/web/06_ROADMAP_WORK_PACKAGES.md` (WP-10 marcado implementado, achados reais documentados, incluindo o bug de `onboarding/page.tsx`), `docs/web/00_INDICE_PONTO_ZERO_WEB.md` (referência ao novo doc 17), `docs/web/17_CERTIFICACAO_WP10_TRILHA_AUDITORIA.md` (novo), `web/README.md` (WP-10 documentado: visualizar, filtro, achado do bug) | — | Revisão de cada arquivo | Arquivos citados |
| 21/09/2026 | Confirmação de que v30 é de fato a próxima versão do painel antes de criá-la; com o WP-10, a autorização do Diretor de construir mais 3 blocos está a dois terços executada (WP-09 e WP-10 concluídos, WP-11 ainda pendente) — nenhum bloco além do WP-11 foi definido | — | `grep -rl "v30"` vazio (exceto este próprio arquivo) antes da criação | `01_PAINEL_EXECUCAO_v30_VIGENTE.md` |
| 21/09/2026 | Achado real de contrato, encontrado ao ler `InsightSeriesResponse` (`api/models.py`) contra o schema `InsightSeries` antes de qualquer componente: o campo `note` (já conhecido na reconciliação de CURRENT deste bloco) confirmado sempre presente na resposta real, mas ausente do contrato — corrigido aditivamente | — | Leitura direta do contrato e do modelo antes de qualquer componente | `contracts/bff-openapi.yaml` |
| 21/09/2026 | Nova seção "Métricas" construída em `web/src/app/campaigns/[campaignId]/page.tsx`: nota real quando `points` vazio (sempre, hoje), pontos reais quando não; markup puro dentro do Server Component já existente, nenhum novo Client Component; `getServerInsights` adicionada a `session.ts` | 3 | `npm run typecheck` a cada etapa | `web/src/app/campaigns/[campaignId]/page.tsx`, `web/src/lib/session.ts` |
| 21/09/2026 | 3 novos testes Vitest (`campaign-detail-page.test.tsx`) escritos e verdes: nota real exibida quando `points` está vazio, pontos reais exibidos quando não está, estado de erro quando a leitura falha | 3 | `npx vitest run` → 135/135 (132 pré-existentes + 3 novos) | `web/tests/campaign-detail-page.test.tsx` |
| 21/09/2026 | 2 novos testes de backend via sessão Web real escritos e verdes: placeholder honesto com `note` real via sessão real (confirma que o campo recém-corrigido no contrato chega ao cliente); `Permission.CAMPAIGN_VIEW` é genuinamente ampla (toda role a possui, confirmado por leitura direta de `permissions.py`) — identidade `marketer`, não a criadora da campanha, também lê os insights | 3 | `python3 -m unittest discover -s tests_api` → 178/178 (176 pré-existentes + 2 novos) isolado e na suíte completa | `backend/tests_api/test_insights_web_session.py` |
| 21/09/2026 | `e2e-crossstack/insights.spec.ts` (novo spec dedicado) escrito: submeter um brief real → ver a nota honesta real na seção "Métricas" de `/campaigns/{id}`; `exact:true` usado no locator do heading para evitar ambiguidade com o próprio texto do objetivo do brief ("Testar métricas honestas via E2E", que contém a substring "métricas"). Verde isoladamente (1/1) | 3 | `npx playwright test -c playwright.crossstack.config.ts e2e-crossstack/insights.spec.ts` → 1/1 | `web/e2e-crossstack/insights.spec.ts` |
| 21/09/2026 | Regressão completa final reexecutada no HEAD desta execução (`50fa627`): 267 domínio, 178 API, AsyncAPI 24/24, 135 Vitest, 13 E2E cross-stack — todos verdes, zero regressão em `campaia_core`/`db`/`permissions.py`/`routes_campaigns.py`/`models.py`. Diferente do WP-10, nenhum bug de UI pré-existente adicional foi encontrado nesta regressão | 3 | Todos os comandos executados nesta sessão, saída capturada | `docs/web/18_CERTIFICACAO_WP11_METRICAS_HONESTAS.md` |
| 21/09/2026 | Documentação reconciliada: `docs/web/06_ROADMAP_WORK_PACKAGES.md` (WP-11 marcado implementado, achados reais documentados), `docs/web/00_INDICE_PONTO_ZERO_WEB.md` (referência ao novo doc 18), `docs/web/18_CERTIFICACAO_WP11_METRICAS_HONESTAS.md` (novo), `web/README.md` (WP-11 documentado: visualizar, achado do contrato) | — | Revisão de cada arquivo | Arquivos citados |
| 21/09/2026 | Confirmação de que v31 é de fato a próxima versão do painel antes de criá-la; com o WP-11, a autorização do Diretor de construir mais 3 blocos ("pode sim construa mais 3 blocos") está integralmente executada (WP-09, WP-10, WP-11) — nenhum bloco além do WP-11 foi definido; qualquer trabalho futuro exige nova autorização explícita do Diretor E nova reconciliação de CURRENT | — | `grep -rl "v31"` vazio (exceto este próprio arquivo) antes da criação | `01_PAINEL_EXECUCAO_v31_VIGENTE.md` |
